# 阅读页流畅度体检 - 2026-07-09

## 范围

- Android 原生阅读页：`ReadView`、`PageDelegate`、`ReadBook`、`TextChapterLayout`。
- Web 服务阅读页：`BookChapter.vue`、`BookPageReader.vue`、`ChapterContent.vue`、`bookStore.ts`。
- 本轮只做静态体检和优化清单，不改运行时代码，不做动画方案替换。

## 当前结论

当前阅读页已经有一批基础流畅度保护：

- 原生横向翻页动画帧调度已走 `postInvalidateOnAnimation()`，能对齐系统 vsync。
- 原生阅读会加载当前章、下一章、上一章；网络书还会按 `preDownloadNum` 做更远章节预下载。
- 原生进度 UI 更新有 200ms 节流，保存阅读进度落到后台 executor。
- Web 阅读页已有本地阅读记录 800ms 节流、远端进度 60s 节流、页面隐藏时 beacon 保存。
- Web 书本翻页已有相邻章节预取，跨章动画会在同一个 `BookPageReader` 舞台里分页目标章。
- Web 书本翻页动画期间会挂起重分页请求，避免图片加载或字体就绪打断动画。

更可能影响体感的点不是单一动画公式，而是下面几类：

- 绘制：原生横向翻页开始或补帧时会截图/录制当前页和目标页，复杂页面、图片页或开启优化渲染时成本更高。
- 分页：原生章节排版和 Web 书本模式分页都可能在切章、字号/宽度变化、图片加载后触发较重工作。
- 网络：目标章未命中本地/预取缓存时，跨章会等待 `getBookContent` 或书源下载。
- 保存：Web 已节流；原生章节切换仍会立即保存并刷新菜单，通常不是首要瓶颈，但需要实测确认。
- 测量：当前代码里没有发现专门的 JankStats、FrameMetrics 或 performance mark/measure 基线，后续优化缺少可比较指标。

## 原生阅读页观察

- `ReadView.dispatchDraw()` 每帧先画子 View，再交给 `pageDelegate.onDraw()` 和 `autoPager.onDraw()`；`computeScroll()` 同时驱动翻页和自动翻页。
- `PageDelegate.startScroll()` / `computeScroll()` 会在动画开始和每次 scroller 推进时请求下一帧；横向翻页通过 `HorizontalPageDelegate.invalidateOnAnim()` 使用 `postInvalidateOnAnimation()`。
- `HorizontalPageDelegate.setDirection()` 会立即 `setBitmap()`；`PageDelegate.postInvalidate()` 在横向动画运行中也可能再次 `setBitmap()`。
- `SimulationPageDelegate.setBitmap()` 会截图当前页和目标页到 Bitmap；`CoverPageDelegate.setBitmap()` 也会截图目标页/当前页。
- `ReadBook.loadContent(resetPageOffset)` 会加载当前章、后一章、前一章；切到下一章/上一章时会继续加载相邻章并 `saveRead()`。
- `ReadBook.preDownload()` 对非本地书会根据 `preDownloadNum` 向前/向后下载更远章节，默认配置读取值是 10。
- `TextChapterLayout` 图片下载完成后会刷新受影响页面并在当前章回调 `contentLoadFinish()`，图片章节需要单独测。

## Web 阅读页观察

- `BookChapter.vue` 对 `readingBook` 本地持久化有 800ms 防抖式合并，隐藏页面时会 flush。
- 滚动模式使用 `IntersectionObserver` 上报段落进度，重复进度会跳过，远端保存同章内走 60s throttle。
- 无限滚动会在接近底部时预取下一章，命中预取时用 `requestAnimationFrame` 追加章节，避免全局 loading mask。
- 书本模式会预取相邻章节，跨章时优先复用 `prefetchedChapters` 或已加载 `chapterData`。
- `BookPageReader.scheduleRepaginate()` 已避免动画中直接取消翻页；动画结束后再决定是否补分页。
- `bookPagination.ts` 的分页是 DOM 测量驱动，长段落拆分和图片页仍可能是主线程热点。

## 可落地优化清单

### P0: 先补可复测指标

1. Android 增加 Debug-only 阅读页帧耗时采样：围绕 `ReadView.dispatchDraw()`、`PageDelegate.computeScroll()`、`ReadBook.loadContent()`、`contentLoadFinish()` 记录耗时分桶，只在 `BuildConfig.DEBUG` 或日志开关开启时启用。
2. Web 增加 Debug-only `performance.mark/measure`：覆盖章节请求、分页、跨章动画准备、动画完成、进度保存请求。
3. 固定回归场景：普通纯文本、超长段落、含图片章节、网络书跨章未缓存、网络书跨章已预取、Web 滚动无限追加、Web 书本跨章。

验收：能输出同一套场景下的耗时数据，而不是只靠肉眼判断。

### P1: 原生动画路径减负

1. 检查 `PageDelegate.postInvalidate()` 运行期是否造成重复 `setBitmap()`；如果实测有高频截图，应改成只在页面内容或方向变化时刷新录制内容。
2. 对 `SimulationPageDelegate` 和 `CoverPageDelegate` 分别统计截图耗时；图片页和优化渲染开启状态要单独测。
3. 动画结束的 `stopScroll()` 当前仍普通 `invalidate()`，可实测是否需要对横向动画也收口到 vsync 帧请求。

验收：快速连续翻页、取消翻页、图片页翻页不出现重影，帧耗时 P95 有下降。

### P1: 跨章和预取体验

1. 原生端记录跨章时 `curTextChapter == null` 的次数；如果命中率低，优先调整当前章相邻预加载触发时机，而不是改动画。
2. 网络书跨章未缓存时，区分“网络等待”和“分页等待”的耗时；网络等待优先靠预取/下载策略，分页等待才改排版。
3. Web 书本模式已做相邻预取，下一步只需补命中率和失败率日志，避免过度扩大缓存。

验收：已预取跨章基本无 loading mask；未预取跨章能明确知道卡在网络还是分页。

### P2: 分页和图片刷新

1. 原生 `TextChapterLayout` 图片下载后刷新受影响页，建议只统计并验证，不先改逻辑；图片章节容易引入错页或空白回归。
2. Web `bookPagination.ts` 长段落拆分可能造成主线程长任务，可先用 measure 记录分页耗时和页面数，再决定是否分片分页。
3. 字号、行距、宽度、字体变化引发全量重分页，属于正确行为；优化方向应是状态提示和分片，而不是跳过分页。

验收：配置变化后页码和进度仍正确，图片加载不打断动画或造成错位。

### P2: 保存链路去抖

1. Web 当前保存策略基本合理：本地 800ms、远端同章 60s、跨章立即保存、隐藏时 beacon。短期不建议再改。
2. 原生 `ReadBook.saveRead()` 已同步更新内存字段、后台写库。若实测切章卡顿，应先确认 `upMenuView()`、`curPageChanged()` 和 UI 刷新耗时，再考虑合并保存。

验收：切后台、进程杀死恢复、Web 与 App 同时阅读时进度不倒退。

## 建议执行顺序

1. 先做 Debug-only 指标，不改变用户行为。
2. 用固定场景跑一轮基线，给出绘制/分页/网络/保存四类耗时。
3. 若绘制高，优先处理重复截图/录制；若分页高，处理长段落和图片刷新；若网络高，处理预取命中率；若保存高，再看进度保存合并。
4. 每次只动一类路径，并保留滚动模式、无动画模式、Web 资产同步作为回归闸门。

## P0 执行记录

- 2026-07-09: 已新增 Android `ReaderPerformance`，覆盖慢帧绘制、动画推进、截图录制、内容取回和排版完成耗时。
- 2026-07-09: 已新增 Web `readerPerformance`，覆盖章节请求、书本分页、跨章目标章分页、翻页完成和进度保存耗时。
- 2026-07-09: Web 生产构建默认不输出性能日志，可用 `localStorage.legadoReaderPerf = '1'` 或 URL 参数 `readerPerf` 打开。
- 2026-07-09: Web 性能日志显式开启后会通过 `saveReaderLog` 回传手机；手机端仍需打开“记录日志”才会落到 App 日志文件。

## 后续未做

- 未跑真机帧率或浏览器性能面板。
- 尚未根据真实耗时数据执行 P1/P2 优化。
