# Android 14+/17 适配踩坑记录

> 最新修订: 2026-07-08 00:30 CST
> 最近三次修订:
> - 2026-07-08 00:30 CST: 修复 Android 16 WebService 在 `dataSync` 前台服务 24 小时累计 6 小时限额耗尽后，启动服务或网络变化刷新通知再次调用 `ServiceCompat.startForeground(... DATA_SYNC)` 抛 `ForegroundServiceStartNotAllowedException: Time limit already exhausted...` 导致崩溃。`WebService` 首次提升前台服务后改用 `notificationManager.notify` 刷新通知；捕获系统拒绝前台化后关闭 Web 服务开关、写 AppLog/toast 并 `stopSelf()`；`onTimeout` 走同一停服路径。`scripts/check-debug.sh` 通过。
> - 2026-06-29 23:00 CST: 修复切后台后再打开阅读页偶发进度少一页。`onPause` 保存进度前先调用 `pageDelegate.abortAnim()` 强制完成进行中的翻页动画，避免 `durChapterPos` 仍是翻页前位置被持久化；`saveRead` 中 `book` 进度字段更新移到 executor 外同步执行，消除与同在 onPause 启动的 `syncProgress` 异步竞争导致上传旧进度；`onResume` 防御性检查动画悬空并强制完成。`./gradlew :app:compileAppDebugKotlin --no-daemon` 通过。同时调整文档维护规则：满 15 条整体归档重开，不再来回挪动单条条目。
> - 2026-06-29 14:30 CST: 针对接口与模块"有具体数值/范围"的参数补边界校验，防止外部传入负值/越界导致崩溃或数据错乱。Web API 入口 `BookController.saveBookProgress` 校验 `BookProgress.durChapterIndex < 0` 与 `durChapterPos == Int.MIN_VALUE`，章节数为 0 时拒绝保存，越界时 `coerceIn(0, chapterCount-1)` 兜底并用修正后的 `Book` 进度上传 WebDAV；`BookController.getBookContent` 用 `toIntOrNull()` 安全解析并拒绝 `index < 0`。`ReadBook.setProgress/openChapter` 加 `durChapterIndex < 0` 校验（`openChapter` 改 `in 0 until chapterSize`）；`skipToPage/setPageIndex` 加页码负值校验；`initData` 在 `simulatedChapterSize > 0` 时按 `simulatedChapterSize` 越界重置（对齐 `ReadMangaViewModel.initMangaData` 既有逻辑，并补 AppLog）。`AudioPlay.resetData` 对 `book.durChapterIndex` 用 `coerceIn(0, simulatedChapterSize-1)` 校正，`upDurChapter` 的 `chapterList?.get(idx)` 改 `getOrNull(idx)`，避免 `IndexOutOfBoundsException`。`./gradlew :app:compileAppDebugKotlin --no-daemon` 通过。注意：`Book.durChapterPos < 0` 是项目既有语义（章节最后一页标记），不视为非法值，校验仅拒绝 `Int.MIN_VALUE` 与外部入口的负值，`ReadBook.initData` 复用 `book.durChapterPos * (if (book.durChapterPos<0)-1 else 1)` 翻转。
>
> 项目: 阅读 (legado) Android 电子书阅读器
> 最近更新: 2026-07-08
> 目标: targetSdk=37, compileSdk=37, minSdk=26

---

## 文档维护规则

- 本文件最多保留 15 条适配/构建/运行时问题的详细记录。
- 当达到 15 条后需要新增下一条时，把当前整个文档改名为 `适配踩坑记录-YYYY_MM.md`（按归档时的年月）移入 `文档归档/`，然后新建空的 `适配踩坑记录.md` 重新开始记录。不再来回挪动单条条目。
- 主文件保留长期有效规则和归档索引，避免后续 AI 读取时被过期细节干扰。
- 时间敏感结论必须写明记录日期、验证日期、适用 SDK/依赖/系统版本；超过 30 天、target/compile SDK 改变、核心依赖升级、系统规则变化时重新验证。

## 归档索引

- `文档归档/适配踩坑记录-2026-05_06.md`: 早期 Gradle 下载/代理问题、Release 构建附录、前台服务启动入口统一、提交前拦截本地文件、本地 Debug 编译验证脚本等；2026-06-29 前归档。
- 当前主文件即 2026-06 起的记录，尚未归档；满 15 条后整体改名为 `适配踩坑记录-2026-06.md` 归档。

## 长期有效规则

- Gradle/AGP: AGP 9 使用 `androidComponents`，split APK 取 ABI 时比较 `it.filterType.name() == "ABI"`；不要恢复旧 `applicationVariants`。
- 编译验证: Kotlin/代码改动后优先跑 `scripts/check-debug.sh`；生成 Debug APK 跑 `./gradlew :app:assembleDebug --no-daemon`。
- minSdk=26: 兼容分支不要删除；旧 API 调用需要按 API level 分支并局部 `@Suppress("DEPRECATION")`。
- 前台服务: 启动入口用 `ContextCompat.startForegroundService()`；服务内必须用 `ServiceCompat.startForeground(this@Service, id, notification, type)` 并声明 `foregroundServiceType`。Android 16+ `dataSync` 24 小时累计限额耗尽后，再次前台化会抛 `ForegroundServiceStartNotAllowedException`；WebService 这类长挂服务必须捕获并停服。
- WebDAV: 日志不得包含账号、密码、Authorization header、URL query/fragment；进度下载写入前必须校验章节范围。本地书可读性校验只在真正加载内容时做（`ReadableRequired`），仅同步进度（`downloadAllBookProgress`/`restoreBookProgressOnly`/`syncProgress`/`syncBookProgress`）用 `RangeOnly`，不得触发 SAF 目录权限检查。`durChapterPos < 0` 是章节最后一页持久化标记，进入阅读态或比较远近时必须使用有效正数位置。
- SAF 本地书: 备份恢复后不同设备的 `content://.../tree/...` 可能失效；不要自动猜路径替换，必须提示用户重新选择书籍目录。
- Web 前端: 修改 `modules/web/src` 后必须同步 `app/src/main/assets/web/`，否则 APK 不生效。

---

## 最近 15 条记录

### 0.3 Android 16 WebService dataSync 限额耗尽后崩溃

**记录日期**: 2026-07-08
**验证环境**: targetSdk=37, compileSdk=37, minSdk=26；日志设备 vivo V2458A / Android 16 (SDK 36) / versionName=3.26.070302
**文件**: `WebService.kt`, `strings.xml`, `strings.xml (values-zh)`

**现象**:
- 用户提供两份崩溃日志，均为 `ForegroundServiceStartNotAllowedException: Time limit already exhausted for foreground service type dataSync`。
- 路径 1: `WebService.onStartCommand()` → `upWebServer()` → `startForegroundNotification()` → `ServiceCompat.startForeground(... DATA_SYNC)`。
- 路径 2: `NetworkChangedListener.onAvailable()` 网络变化回调 → `WebService.startForegroundNotification()`。

**根因**:
- Android 15+ 对后台 `dataSync` / `mediaProcessing` 前台服务按类型统计 24 小时累计 6 小时限额，Android 16 设备上 WebService 长挂最容易耗尽 `dataSync` 配额。
- `BaseService.onTimeout()` 能在系统通知超时后 `stopSelf()`，但 WebService 内部仍可能在服务销毁前或用户重新启动时再次调用 `ServiceCompat.startForeground(... DATA_SYNC)`。
- 配额耗尽后再次前台化会直接抛 `ForegroundServiceStartNotAllowedException`；原实现未捕获，导致启动路径和网络变化刷新通知路径都可能崩溃。

**修复**:
- `WebService` 增加 `foregroundNotificationStarted`，首次前台化成功后，后续地址变化只用 `notificationManager.notify()` 刷新通知，避免重复调用 `startForeground()`。
- 捕获系统拒绝前台化的 `ForegroundServiceStartNotAllowedException`，识别 `Time limit already exhausted...dataSync` 后写 AppLog/toast、关闭 `PreferKey.webService` 开关并 `stopSelf()`。
- `onTimeout(startId, fgsType)` 走同一停服路径；网络变化回调和 3 小时重建兜底在拒绝状态下直接返回，避免并发再次触发。
- `scripts/check-debug.sh` 通过。

**回归测试点**:
- Android 16+ 设备用 `adb shell device_config put activity_manager data_sync_fgs_timeout_duration <短时长>` 缩短限额后，WebService 后台超时应自动停服，不崩溃。
- 配额耗尽后从设置开关或磁贴重新启动 WebService，应提示被系统限制并关闭开关，不崩溃。
- 配额耗尽后网络变化回调不应再次崩溃。
- 正常配额内启动 WebService，网络 IP 变化仍应更新通知地址。

### 0.2 切后台后再打开阅读页偶发进度少一页

**记录日期**: 2026-06-29
**验证环境**: targetSdk=37, compileSdk=37, minSdk=26；日志设备 vivo V2458A / Android 15；翻页动画 Cover/Slide/Simulation
**文件**: `ReadBookActivity.kt`, `ReadBook.kt`

**现象**:
- 翻页动画进行中按 Home 切后台，进程被系统杀死后重建，从 DB/WebDAV 恢复时进度比实际少一页。
- 日志（2026-06-29 22:31）显示 ReadBookActivity 在后台被冻结 7.7 小时后 onDestroy→onCreate 重建，恢复位置为切后台前最后一次成功持久化的旧位置。

**根因**:
- 横向翻页（Cover/Slide/Simulation）的 `durChapterPos` 只在 `onAnimStop → fillPage → setPageIndex` 时更新（`TextPageFactory.kt`、`ReadBook.kt:436`）。动画进行中 `durChapterPos` 仍是翻页前位置。
- `onPause()` 直接调用 `ReadBook.saveRead()`（`ReadBookActivity.kt:363`），保存的是动画前旧位置。Activity 暂停后 `computeScroll()` 不再被驱动，动画悬空；进程被杀后 DB/WebDAV 都是旧位置，恢复后少一页。
- 次要：`saveRead()` 通过单线程 `globalExecutor` 异步执行 book 字段更新（`ReadBook.kt:918`），与同在 `onPause` 启动的 `syncProgress()` 异步竞争。`syncProgress` 网络返回后读取 `book.durChapterPos`，若 executor 排队未执行则读到旧值并上传 WebDAV。

**修复**:
- `onPause` 在 `saveRead()` 前调用 `binding.readView.pageDelegate?.abortAnim()`，强制完成进行中的翻页动画，确保 `durChapterPos` 已更新再保存。`abortAnim` 在无动画时无副作用（`HorizontalPageDelegate.kt:116-130`、`ScrollPageDelegate.kt:112-123`）。
- `saveRead()` 中 `book.durChapterIndex`/`book.durChapterPos` 更新移到 `executor.execute` 外同步执行，`executor` 内只保留 DB 查询标题和 `book.saveRead()` 写入，消除与 `syncProgress` 的竞争。
- `onResume` 非 `bookChanged` 分支开头防御性检查 `pageDelegate?.isStarted == true` 则 `abortAnim()`，覆盖进程未杀但动画悬空的边角情况。
- `./gradlew :app:compileAppDebugKotlin --no-daemon` 通过。

**回归测试点**:
- Cover/Slide/Simulation/NoAnim 翻页动画进行中按 Home，立即返回：应停在翻页后目标页。
- 翻页动画进行中按 Home，杀进程后重开：应恢复到翻页后目标页。
- 滚动翻页模式切后台返回：滚动位置不回退。
- 开启 WebDAV 进度同步：切后台后云端进度与本地一致，不回退。
- 无翻页操作时切后台返回：位置不变（abortAnim 无副作用）。

### 0.1 WebDAV 云端负数阅读进度确认同步后加载异常

**记录日期**: 2026-06-28
**验证环境**: targetSdk=37, compileSdk=37, minSdk=26；日志设备 Xiaomi MI PAD 4 / Android 8.1.0 / versionName=3.26.061822
**文件**: `BookProgress.kt`, `ReadBook.kt`, `ReadMangaViewModel.kt`, `BaseReadViewModel.kt`, `AppWebDav.kt`, `BookProgressTest.kt`

**现象**:
- 用户打开《诸神愚戏》后提示需要同步，选择同步后阅读页加载异常/退出。
- 日志显示 WebDAV 云端进度为 `chapter=291 pos=-3261`，进入阅读页后多次拉取同一进度。

**根因**:
- `durChapterPos < 0` 在本项目里是“章节最后一页”的持久化标记，`ReadBook.initData()` / `ReadMangaViewModel.initData()` 打开本地书籍状态时会转成正数位置。
- 但 WebDAV 弹窗确认同步走 `ReadBook.setProgress(progress)` / `ReadMangaViewModel.setProgress(progress)`，此前直接把云端 `progress.durChapterPos` 写入阅读态，导致分页用负数位置查找页面。
- 同步远近比较也直接比较原始 `durChapterPos`，远端 `-3261` 会被误判为小于本地正数位置。

**修复**:
- `BookProgress` 新增 `readChapterPos`（负数转正，`Int.MIN_VALUE` 兜底为 `Int.MAX_VALUE`）和 `compareReadPosition(book)`。
- 文本/漫画 `setProgress()` 进入阅读态前统一使用 `readChapterPos`。
- `ReadBook.syncProgress()`、`ReadMangaViewModel.syncProgress()`、`BaseReadViewModel.syncBookProgress()`、`AppWebDav.downloadAllBookProgress()` 统一用 `compareReadPosition()` 比较进度远近。
- 保留持久化负数语义：保存到 `Book.durChapterPos` / WebDAV JSON 时仍可用负数表示最后一页。
- 新增 `BookProgressTest` 覆盖 `pos=-3261` 转正和比较逻辑。

**回归点**:
- 云端进度 `chapter=291 pos=-3261` 时，确认同步应跳到章节 291 的有效位置 3261，不应卡加载或退出。
- 本地已经在同一有效位置 `3261` 时，云端 `-3261` 应视为同一进度，不重复弹同步。
- 启动全量同步遇到远端最后一页标记时，应按有效位置判断是否比本地更新。

### 0. compileSdk/targetSdk 抬到 37 (Android 17) 前置闸门验证

**记录日期**: 2026-06-26
**验证环境**: compileSdk=37, targetSdk=37, minSdk=26；AGP 9.2.1 + Gradle 9.4.1 + Kotlin 2.3.21 + KSP 2.3.7；JDK toolchain 17
**文件**: `build.gradle` (`compile_sdk_version`), `app/build.gradle` (`targetSdk`)

**现象 / 目的**:
- Android 17 (API 37) 适配前置闸门：确认现有工具链无需升级 AGP 即可识别 `compileSdk=37`。
- 若 AGP 不认 API 37，则 Kotlin/KSP/AGP/Gradle 需联动升级，适配工作面会显著扩大。

**结论**:
- AGP 9.2.1 **直接支持** `compileSdk=37`，**无需升级 AGP**。
- `./gradlew :app:compileAppDebugKotlin --no-daemon` 通过；唯一警告 `ViewExtensions.kt:72 SHOW_IMPLICIT deprecated` 为既有问题，与本次修改无关。
- `./gradlew :app:assembleAppDebug --no-daemon` 完整 APK 打包通过，无 lint/打包侧阻断。
- native 库 `libarchive-jni.so` 旧条提示 "Unable to strip ... packaging them as they are" 为既有无 strip 符号情况，本次升级未新增问题。
- 68 处 `Build.VERSION.SDK_INT` 分支目前无需新增 API 37 分支；具体行为变更适配在后续条目按需补录。

**16KB page size 验证（同批次，2026-06-26）**:
- `libarchive-jni.so`（arm64-v8a 等）: `readelf -lW` LOAD Align = `0x4000` (16384 = 16KB)；`zipalign -c -v -p 16 <apk>` 输出 `lib/.../libarchive-jni.so (OK)`。.so 内部 LOAD 段与 APK 内 zip 偏移均已满足 16KB 对齐，无需升级 `libarchive 1.1.6`。
- cronet: `app/build.gradle:140` `jniLibs.excludes.add('lib/*/libcronet*.so')` 将 cronet .so 排除在 APK 之外。运行时优先使用系统 HttpEngine（Android 14+ / API 34+ 系统 native）或 `play-services-cronet` 外部 Provider；兜底路径 (`CronetLoader.preDownload`) 仅在二者不可用时从阿里云 Maven 下载 `cronet-embedded-<version>.aar` 提取 `libcronet.<version>.so` 动态 `System.load()`。
- 16KB page size 设备均为新版 Android（15+/高端），系统 HttpEngine 一定可用，兜底下载路径不会在 16KB 设备触发，cronet 的 16KB 兼容由系统/Google Play 服务侧承担，不在应用 APK 内。
- **结论: 16KB page size 验证通过，当前无需升级 cronet/libarchive 或改任何代码**。

**前台服务运行限制验证（同批次，2026-06-26）**:
- 服务清单 9 个：`dataSync`=CheckSource/CacheBook/ExportBook/WebService/WebTileService/Download；`mediaPlayback`=TTSReadAloud/HttpReadAloud/AudioPlay。全部已在 `AndroidManifest.xml` 声明 `foregroundServiceType` 并用 `ServiceCompat.startForeground(type)` 启动。
- `BaseService.onTimeout(startId, fgsType)`（`base/BaseService.kt:74`，API 35+ 引入）已统一实现：dataSync 触发系统超时即记 `onTimeout startId fgsType` 日志并 `stopSelf()` 优雅停止，不会 ANR/崩溃。
- Android 16+ (API 36) 对 `dataSync` 引入每 24h 累计 6h 运行上限（应用前台不计入）；带 native 的 `mediaProcessing` 类型不在本项目使用。
- 风险评估:
  - `mediaPlayback` 三服务无累计限额，朗读/音频播放不受影响。
  - `dataSync` 六服务中：CheckSource/CacheBook/ExportBook/Download 为一次性任务，正常时长远小于 6h 不构成风险。
  - **`WebService` + `WebTileService` 是唯一长挂场景**：用户开启 Web 服务后挂在后台传输数据，长时间运行（如整夜）可能在 Android 16+ 触发 dataSync 累计上限被系统 stop；属体验影响（Web 服务断开需重启）非崩溃风险。
- 已有缓解：`AGENTS.md` 记录 `PreferKey.webServiceWakeLock` 默认 false（强开有耗电和部分 ROM 杀后台风险），不修改该默认值。
- **新增缓解 (2026-06-28)**: WebService 监听 `EventBus.SAVE_CONTENT`（新章节加载完成），运行超过 3 小时后调用 `upWebServer()` 重建 httpServer/webSocketServer 实例，成功后重置计时。解决 NanoHTTPD 长时运行连接泄漏/死锁、WiFi 切换旧 IP 绑定等应用层问题；不 stopSelf 整个 Service，保留前台服务状态。**同日补强**：加 `lifecycleScope` 兜底定时器（每 30 分钟轮询）覆盖长时间无新章节加载的静默场景；给 `upWebServer()` 加 `synchronized` 重入保护防并发重建。详见 `新功能踩坑记录.md` §二.3。
- **结论: 前台服务无需改任何代码**；后续若 WebService 投诉增多可考虑加用户提示或切长挂数据传输到 WorkManager/jobscheduler，但非 API 37 必改项。（2026-06-28 更新：已加应用层定期重启 server 实例缓解，系统级 dataSync 6h/24h 累计限制仍需用户手动重启服务）

**隐式 Intent 接收侧动态 receiver 限制验证（同批次，2026-06-26）**:
- Android 14+ (targetSdk 34+) 对动态注册 receiver 收紧：必须在 `registerReceiver` 显式指定 `ContextCompat.RECEIVER_EXPORTED` 或 `RECEIVER_NOT_EXPORTED`，否则抛 `SecurityException`。本项目 4 处裸 `registerReceiver` 风险点（全部接收系统级广播，统一走 EXPORTED）：
  - `help/media/BecomingNoisyReceiver.kt:28` ACTION_AUDIO_BECOMING_NOISY
  - `help/AppFreezeMonitor.kt:40` ACTION_SCREEN_ON/OFF
  - `utils/ContextExtensions.kt:324` ACTION_BATTERY_CHANGED (`registerReceiver(null,...)` sticky 查询)
  - `ui/book/read/ReadBookActivity.kt:332` ACTION_TIME_TICK / ACTION_BATTERY_CHANGED (timeBatteryReceiver)
- 已有规范范式：`service/DownloadService.kt:42`、`ui/widget/ReaderInfoBarView.kt:132` 已用 `ContextCompat.registerReceiver(..., RECEIVER_EXPORTED)`，本次把 4 处裸调用统一到此范式，新增 `import androidx.core.content.ContextCompat`（BecomingNoisyReceiver / AppFreezeMonitor / ContextExtensions / ReadBookActivity）。
- `AppFreezeMonitor` 原有 `@SuppressLint("UnspecifiedRegisterReceiverFlag")` 仅压 lint、不防 targetSdk 34+ 的运行时 SecurityException，本次整改后保留该注解（中文 IDE 报路径下 lint 误报，flag 已显式传入）。
- 发送侧: 全项目无 `sendBroadcast` 调用；`startService`/`startActivity` 隐式调用均为系统定义 action（打开文件/URL 等），不受动态 receiver 限制影响。
- `AndroidManifest.xml` 唯一静态 receiver `MediaButtonReceiver` 已 `exported="true"` + 明确 intent-filter，符合 Android 14+ 要求。
- `./gradlew :app:compileAppDebugKotlin --no-daemon` 通过。
- **结论: 隐式 Intent 收紧风险已修复 4 处，无其它发送侧或静态 receiver 风险**。

**MANAGE_EXTERNAL_STORAGE 收紧评估（同批次，2026-06-26）**:
- `AndroidManifest.xml`: 声明 `MANAGE_EXTERNAL_STORAGE` (`tools:ignore="ScopedStorage"`)、`READ_EXTERNAL_STORAGE`、`WRITE_EXTERNAL_STORAGE` 三项；后两项无 `android:maxSdkVersion` 限定，为兼容 minSdk=26 旧设备保留（API 29- 仍生效，API 30+ 系统忽略）。
- `lib/permission/Permissions.kt:87` `isManageExternalStorage()` 已做分支：`API >= R(30)` 申请 `MANAGE_EXTERNAL_STORAGE`，`API < 30` 申请 `READ/WRITE_EXTERNAL_STORAGE`。对应 Android 11+ 的迁移逻辑，正确。
- 代码侧 5 处直接用 `Environment.getExternalStorageDirectory()`/`/storage/emulated/0`：`help/book/BookHelp.kt:333`(StorageManager)、`utils/FileUtils.kt:114`、`utils/RealPathUtil.kt:38/76/99`、`ui/file/FilePickerViewModel.kt:14`。均依赖 `MANAGE_EXTERNAL_STORAGE` 已授权后才执行，minSdk=26 老路径用 READ/WRITE，分段正确。
- API 37 平台层未移除 `MANAGE_EXTERNAL_STORAGE`，未新增字段级语法要求；收紧主要在 Google Play 上架策略（应用需提报证明）和系统对未明确授权的ressed enforcement。本项目走自有分发渠道（F-Droid/GitHub/直装），上架策略风险不存在。
- AGENTS.md 已明确"未迁移到 Scoped Storage（风险过高）"，保持现状。
- **结论: 存储权限无需改任何代码**；后续若 Google Play 上库需提报 All-Files access 证明或回退到 SAF/MediaStore，但非 API 37 必改项。

**Edge-to-Edge 评估（同批次，2026-06-26）**:
- Android 15+ (targetSdk 35+) 强制启用 edge-to-edge：默认绘制到屏幕边缘并要求应用自行处理 system bar insets。本项目 targetSdk=37 后此强制行为生效。
- 主题 `styles.xml` (Base.AppTheme) 已配 `android:statusBarColor=@color/transparent`、`android:windowDrawsSystemBarBackgrounds=true`、`android:enableOnBackInvokedCallback=true`，基本符合 edge-to-edge 友好布局。
- `BaseActivity.onCreate` → `setupSystemBar()` → `fullScreen()`：API >= R(30) 显式调 `WindowCompat.setDecorFitsSystemWindows(window, true)`，手动避开系统栏（fits=true）即非edge-to-edge绘制行为，系统会尊重此显式选择，targetSdk 35+ 强制不影响此窗口。
- `fullScreen()` 仍用 `window.decorView.systemUiVisibility = SYSTEM_UI_FLAG_LAYOUT_FULLSCREEN or LAYOUT_STABLE`、`FLAG_TRANSLUCENT_STATUS/NAVIGATION`、`addFlags(FLAG_DRAWS_SYSTEM_BAR_BACKGROUNDS)`，这些 API 在 Android 11+ 已 deprecated，但仅 lint 警告（已加 `@Suppress("DEPRECATION")`），运行时不阻塞。
- 布局仅 2 处 `fitsSystemWindows="true"`：`activity_book_info.xml:41`、`view_empty.xml:6`。其余布局靠 BaseActivity 全局 `setDecorFitsSystemWindows(true)` 避开系统栏。
- 已有 `WindowInsetsCompat` 扩展 (`utils/WindowInsetsExtensions.kt` 处理 navigationBarHeight/imeHeight)，阅读页 `ReadView/PageView/ReaderInfoBarView` 已用 insets 适配。
- 阅读器主体 (`book/read/`) 在 `BaseReadBookActivity.kt` 自行处理 system bar 隐藏/显示逻辑，未受影响。
- `AGENTS.md` "Edge-to-Edge 尚未全局启用" 含义：未走 `enableEdgeToEdge()` 全统一范式；但显式 `setDecorFitsSystemWindows(true)` 已达到不启用效果，且 API 37 强制行为不破坏此窗口。
- `./gradlew :app:compileAppDebugKotlin --no-daemon` 通过（之前批次已验证）。
- **结论: targetSdk 37 后强制 edge-to-edge 不破坏现有窗口表现，无需改代码**；长期建议：未来若要做沉浸式全屏阅读体验可统一改用 `WindowCompat.setDecorFitsSystemWindows(false)` + WindowInsetsCompat 手动 padding 的范式替换 `systemUiVisibility` 旧 API，但非 API 37 必改项。

**回归点**:
- 后续 API 37 行为变更（隐式 Intent、存储权限等）以独立条目记录。
- 升级 androidx 全家桶以独立条目记录。
- 目标头已同步更新为 `targetSdk=37, compileSdk=37, minSdk=26`。
- WebService/WebTile 在 Android 16+ 长挂可能触发 dataSync 6h/24h 上限被系统停，重启即可恢复，非崩溃。

### 1. WebDAV 仅同步进度不得触发本地 SAF 可读性检查

**记录日期**: 2026-06-18  
**验证环境**: targetSdk=36, compileSdk=36, minSdk=26；跨设备恢复后旧 SAF `content://.../tree/...` URI 失效场景  
**文件**: `AppWebDav.kt`, `ReadBook.kt`, `BaseReadViewModel.kt`, `ReadMangaViewModel.kt`, `AbsCallBack.kt`

**现象**:
- WebDAV 进度拉取成功后，`AppWebDav.canApplyBookProgress()` 对 `book.isLocal` 调 `FileBook.checkBookReadable()`。
- 跨设备恢复后的旧 SAF `content://.../tree/...` URI 失效时，该检查抛 `SecurityException`，导致阅读页/同步流程弹出“选择书籍所在文件夹/目录权限”提示。
- 进度同步本身只需校验章节范围，不应检查本地文件可读。

**根因**:
- `checkBookProgress()` 无条件对本地书执行 `FileBook.checkBookReadable()`，把“仅同步进度”和“真正加载本地书内容”两种场景混在一起。
- 仅同步进度不需要打开本地文件，旧 URI 失效不应阻断进度写入，更不应触发 SAF 目录权限弹窗。

**修复**:
- `AppWebDav` 进度校验拆成两种模式 `ProgressCheckMode`：
  - `RangeOnly`：只校验 `simulatedTotalChapterNum()` 范围。
  - `ReadableRequired`：校验章节范围，并对本地书执行 `FileBook.checkBookReadable()`。
- `canApplyBookProgress()`/`checkBookProgress()` 新增 `mode` 参数，默认 `RangeOnly`。
- `downloadAllBookProgress()`、`restoreBookProgressOnly()`、`ReadBook.syncProgress()`、`BaseReadViewModel.syncBookProgress()`、`ReadMangaViewModel.syncProgress()` 显式改用 `RangeOnly`，仅同步进度时不再触发本地 SAF 可读性检查。
- 真正打开/加载本地书内容的路径仍通过 `BookHelp.getBookPFD()` → `contentResolver.openFileDescriptor()` 自然抛 `SecurityException`，提示用户重新选择目录，不掩盖真实打不开书的问题。
- `RangeOnly` 模式下保留 `outOfRange` 日志，不记录 `localBookUnreadable`；`ReadableRequired` 模式下保留两者。
- 本条修正了旧条目 1（EPUB/ZIP entries）与旧条目 16（WebDAV 恢复备份 SAF 路径不一致）中“进度同步写入前对本地书做 `checkBookReadable`”的结论：该结论仅适用于 `ReadableRequired` 场景，不适用于仅同步进度。
- 可选脱敏：`AbsCallBack.onResponseStarted` 调试日志改用 `redactUrl()`，只保留 `scheme://host[:port]/path`，去掉 userinfo 与 query，避免 WebDAV URL 泄露。

**回归点**:
- 跨设备旧 `content://` URI 失效时，WebDAV 全量/即时进度同步不再弹目录权限；进度按章节范围正常写入或跳过。
- 真正打开该本地书时仍提示“打开本地书籍出错，请选择书籍所在文件夹”并弹出目录选择器。
- `outOfRange` 日志保留；`RangeOnly` 模式下不出现 `localBookUnreadable` 日志。
- 不改动完整备份恢复逻辑，不自动替换本地 SAF 路径。
- `onResponseStarted` 日志不再包含完整 URL（含 query/userinfo）。

### 2. EPUB/ZIP entries 读取失败不要返回 null

**记录日期**: 2026-06-14
**验证环境**: targetSdk=36, compileSdk=36, minSdk=26；本地 EPUB/ZIP 目录读取异常路径
**文件**: `AndroidZipFile.java`, `ResourcesLoader.java`

**现象**:
- 阅读本地 EPUB 时阅读页显示 `LoadTocError: Attempt to invoke interface method 'boolean java.util.Enumeration.hasMoreElements()' on a null object reference`。
- 日志里 `AndroidZipFile.entries()` 内部 IOException 被吞掉并返回 null，`ResourcesLoader.loadResources()` 随后直接调用 `entries.hasMoreElements()` 触发空指针。

**修复**:
- `AndroidZipFile.entries()` 捕获 IOException 后改为抛出带文件名上下文的 `IllegalStateException`，不再返回 null。
- `ResourcesLoader.loadResources()` 对 `zipFileWrapper.entries()` 增加 null 防御，抛出明确的 `ZipException`。
- WebDAV 全量进度同步写入本地书进度前，使用 `FileBook.checkBookReadable()` 校验 EPUB/ZIP 目录可读；不再只校验文件流能打开。
- 普通本地 EPUB 直接 PFD 读取目录失败时，回退为复制到私有缓存后用标准 `ZipFile` 懒加载读取。
- 阅读页即时 WebDAV 进度同步拉取失败或本地书校验失败时，跳过云端进度并继续使用本地当前进度打开文件。

**回归点**:
- 损坏、权限失效或无法读取目录的 EPUB/ZIP 不应再显示 `hasMoreElements()` 空指针。
- WebDAV 云端进度同步遇到目录不可读的本地 EPUB/ZIP 时应跳过该书，不应写入进度后打开失败。
- 已授权目录中的普通本地 EPUB 如果 PFD ZIP 解析失败，应尝试缓存后读取。
- 阅读页打开过程中云端进度拉取失败时，不应阻断本地文件打开，也不应自动套用异常云端进度。
- 可正常读取的 EPUB 目录加载、封面读取、章节内容加载不受影响。

### 3. ReadTimeRecorder 协程泄漏（GlobalScope 不可取消）

**记录日期**: 2026-06-24
**验证环境**: targetSdk=36, compileSdk=36, minSdk=26；`scripts/check-debug.sh` 编译通过
**文件**: `model/ReadTimeRecorder.kt`

**现象**:
- `ReadTimeRecorder.end()` 用 `GlobalScope.launch(Dispatchers.Main)` 启动 5 分钟延迟结算协程。
- `GlobalScope` 协程无法外部取消，`@OptIn(DelicateCoroutinesApi::class)` 标记被忽略。
- `endJobs` 里的 Job 虽然在 `endImmediately`/`start`/`setBook` 路径会显式 cancel，但若进程长期存活且这些路径未覆盖到（例如 flushAll 不清理 endJobs），会话延迟协程仍可能残留为不可见的全局协程。

**修复**:
- 新增内部 `private val scope = CoroutineScope(SupervisorJob() + Dispatchers.Main)`，用 `SupervisorJob` 包裹，单个会话延迟 Job 失败不影响其他会话。
- `end()` 内 `GlobalScope.launch` 改为 `scope.launch`，移除 `@OptIn(DelicateCoroutinesApi::class)` 和 `GlobalScope`/`DelicateCoroutinesApi` import。
- 调用方（`ReadBookActivity`、`ReadMangaActivity`、`VideoPlayActivity`、`BaseReadAloudService`、`AudioPlayService` 等）接口不变，无需改动。

**回归点**:
- 阅读页/漫画/视频/朗读/音频 start-end 计时仍正常，章节切换 flushAll 不丢时长。
- 退出阅读后 5 分钟内 endJobs 延迟协程能被 `endImmediately`/新 `start` 正确 cancel，不残留。
- 同一本书多源（阅读页 + TTS）共用计时，不会双计。

### 4. AppWebDav init 阻塞主线程 + ContentDownloadState 抽取

**记录日期**: 2026-06-24
**验证环境**: targetSdk=36, compileSdk=36, minSdk=26；`scripts/check-debug.sh` 编译通过；`testAppDebugUnitTest --tests io.legado.app.model.ContentDownloadStateTest` 通过
**文件**: `help/AppWebDav.kt`, `App.kt`, `model/ReadBook.kt`, `ui/book/manga/ReadMangaViewModel.kt`, `model/ContentDownloadState.kt`(新增), `test/.../ContentDownloadStateTest.kt`(新增)

**现象与修复**:

1. **AppWebDav init runBlocking**:
   - `object AppWebDav` 的 `init { runBlocking { upConfig() } }` 在单例首次被任意线程访问时同步执行网络校验。若首次访问在主线程（如配置页、阅读页同步读 `isOk`/`authorization`），会阻塞 UI 直到 WebDAV 网络请求完成或超时。
   - 修复: 移除 `init` 块的 `runBlocking`，`App.onCreate` 的 `Coroutine.async {}`(IO 协程) 主动调 `runCatching { AppWebDav.upConfig() }` 异步预热。配置就绪前 `authorization` 为 null，各读取点用 `?.let`/`?: throw` 安全降级，不崩溃。

2. **ContentDownloadState 抽取**:
   - `ReadBook` 和 `ReadMangaViewModel` 各自重复定义了 `downloadedChapters`/`downloadFailChapters`/`downloadScope`/`preDownloadSemaphore`/`preDownloadTask` 五个字段及 clear/cancel 逻辑。
   - 修复: 新增 `ContentDownloadState` 纯状态类（参照 `ChapterLoadState` 风格），承载五个字段 + `clear()`/`cancelPreDownload()`/`markDownloaded()`/`markFailed()`/`isDownloaded()`/`isFailedTooMany()`。`ReadBook` 和 `ReadMangaViewModel` 通过属性委托复用，下载调度逻辑因文本/漫画差异大仍留在各自类。`CacheBook` 通过 getter 透明访问同一 `ConcurrentHashMap` 实例，无需改动。
   - 附 `ContentDownloadStateTest`（7 个用例）覆盖标记/清除/独立性/共享实例。

3. **ReadBook book!! 强解包**:
   - `loadContent`(line 550) 和 `downloadAwait`(line 649) 用 `book!!` 强解包，book 为 null 时 NPE。
   - 修复: 改为 `book ?: return`/`book ?: return "加载失败"`，安全降级。

**回归点**:
- WebDAV 配置页保存、阅读页进度同步、远程书籍列表在 App 冷启动后立即访问 AppWebDav 不阻塞 UI；配置未就绪时降级而非崩溃。
- 文本阅读预下载、漫画阅读预下载、切书 clear、cancelPreDownloadTask 行为不变。
- `CacheBook` 下载成功/失败的状态记录仍正常反映到 `ReadBook.downloadedChapters`/`downloadFailChapters`。
- book 为 null 时 loadContent/downloadAwait 不再 NPE，安全跳过。

### 5. 缺少 import

**文件**: `ContextExtensions.kt`

**错误**: 使用 `NetworkCapabilities` 但忘记 import `android.net.NetworkCapabilities`。

**修复**: 添加 import。修改后先用 IDE 或 `scripts/check-debug.sh` 检查 import/编译错误。

### 6. 平台 API 不存在

**文件**: `MediaPlaybackLock.kt`

**错误**: 想用 `WIFI_MODE_HIGH_PERF` (API 29)，但 platform-36 的 android.jar 里没有这个常量。

**修复**: 回退到 `WIFI_MODE_FULL_HIGH_PERF`，加 `@Suppress("DEPRECATION")`。

### 7. ProgressDialog init receiver 类型不匹配

**文件**: `AndroidDialogs.kt`

**错误**: `AlertDialog.Builder(this).apply { if (init != null) init() }` 中 `init` 的 receiver 是 `AlertDialog`，不是 `AlertDialog.Builder`。

**修复**: 先 `create()` 得到 `AlertDialog`，再执行 `dialog.init()`，最后 `dialog.show()`。

### 8. getParcelable 可空性处理

**文件**: `FileAssociationDialog.kt`

**错误**: `BundleCompat.getParcelable()` 返回 `Uri?`，但后续 `importBook()` 需要非空 `Uri`。

**修复**: 使用 `?: return@registerHandleFile` 提前返回，避免 nullable 传入非空参数。

### 9. StaticLayout 替换时括号没对齐

**文件**: `TextChapterLayout.kt`

**错误**: 替换为 `StaticLayout.Builder.obtain(...).build()` 后残留多余右括号。

**修复**: 删除多余 `)`；后续替换多行构造器时优先编译确认括号结构。

### 10. WebDAV 同步后空章节列表崩溃

**文件**: `ReadBook.kt`, `ChapterListFragment.kt`

**现象**: 设备 A 下载书籍，设备 B 阅读并同步到 WebDAV，设备 A 同步后打开书籍崩溃。

**错误**: `Index 0 out of bounds for length 0`

**根因**: `chapterList?.get(0)` 在章节列表为空时抛异常。

**修复**: `chapterList?.get(0)` 改为 `chapterList?.firstOrNull()`。

### 11. WebDAV 首次配置误报应用验证失败

**记录日期**: 2026-06-01  
**验证环境**: targetSdk=36, compileSdk=36, minSdk=26；反馈设备 Android 8.1 老平板、Android 16  
**文件**: `AppWebDav.kt`

**现象**:
- 首次安装后依次填写 WebDAV 服务器地址、账号、密码，保存密码时提示“webDav应用验证失败”。
- 已配置成功的版本先输入错误密码会正常提示验证失败；随后重新输入正确密码，仍持续提示“webDav应用验证失败”。

**根因**:
- 旧 `upConfig()` 入口立即清空 `authorization` / `defaultBookWebDav`，校验失败会破坏旧的可用运行时状态。
- 旧逻辑直接用 `服务器地址 + AppConfig.webDavDir` 校验，应用子目录未创建时部分服务返回 401，被误判为账号密码错误。

**修复**:
- 仅当账号或密码为空时清空运行时配置；有配置时先校验再替换。
- 拆分 `serverWebDavUrl` 和 `rootWebDavUrl`，优先校验用户填写的服务器根地址，再创建应用子目录。
- `authorization` 和 `defaultBookWebDav` 只在校验通过并创建目录成功后替换。
- `checkAuthorization()` 失败不再删除已输入密码。
- 使用 `AtomicInteger configVersion` 忽略过期校验结果。
- WebDAV 日志只记录脱敏 URL、HTTP 状态码、401 标记和异常信息。

**回归点**:
- 首次安装新配置可用，默认子目录不存在时能自动创建。
- 错误密码后再输入正确密码，不应持续失败。
- 旧版本已有可用配置时，失败校验不能破坏远程书籍入口。

### 12. AGP 9.2.1 迁移后 split APK 打包冲突

**记录日期**: 2026-06-05  
**验证环境**: AGP 9.2.1, Gradle 9.4.1, targetSdk=36, compileSdk=36, minSdk=26  
**文件**: `app/build.gradle`, `gradle/libs.versions.toml`, `gradle/wrapper/gradle-wrapper.properties`

**现象**:
- `compileAppDebugKotlin` 通过。
- `assembleDebug` 到 `packageAppDebug` 阶段报 ZIP 异常或卡住。
- Debug split APK 应输出 `legado_all.apk`, `legado_arm64.apk`, `legado_armv7.apk`, `legado_x64.apk`, `legado_x86.apk`。

**根因**:
- AGP 9 已移除 `applicationVariants`，需要迁移到 `androidComponents`。
- `androidComponents` 的 `output.filters` 中 `filterType` 是 enum。写成 `it.filterType == "ABI"` 会永远匹配失败。
- ABI filter 匹配失败后，所有 split 输出都回退成 `legado_all.apk`，导致打包冲突。

**修复**:

```groovy
androidComponents {
    onVariants(selector().all()) { variant ->
        variant.outputs.forEach { output ->
            def abiFilter = output.filters.find { it.filterType.name() == "ABI" }
            def abiName = abiFilter ? (abiShortMap[abiFilter.identifier] ?: abiFilter.identifier) : "all"
            output.outputFileName.set("legado_${abiName}.apk")
        }
    }
}
```

**迁移注意**:
- AGP 9 内置 Kotlin 支持时，不要继续给 Android 模块显式应用 `kotlin-android` 插件。
- `signingConfig`, `minifyEnabled`, `splits.abi.enable`, `universalApk` 统一使用 Groovy `=` 赋值。
- 清理 AGP 9 已移除的 `gradle.properties` 属性。

### 13. 阅读器切到下一章偶发卡“加载中”

**记录日期**: 2026-06-06  
**验证环境**: targetSdk=36, compileSdk=36, minSdk=26；阅读页本地缓存章节/已从 WebDAV 下载到本地的书籍  
**文件**: `ReadBook.kt`

**现象**:
- 已经下载到本地的书籍，阅读到章节末尾切下一章时偶发一直显示“加载中”。
- 关闭 App 后重新打开同一本书可以恢复。

**根因**:
- `ReadBook.loadContent(index)` 和 `loadContentAwait(index)` 在 `chapterList` 非空时错误使用 `durChapterIndex` 取章节，而不是传入的 `index`。
- 预加载下一章时把下一章 index 加入 loading 集合，但实际完成的是当前章，导致下一章 loading 状态残留。

**修复**:
- 新增 `getChapter(index)`，统一按目标章节 index 从内存 `chapterList` 或数据库取章，并使用 `getOrNull()` 防越界。
- `loadContent()`、`loadContentAwait()`、`downloadIndex()` 统一走 `getChapter(index)`。
- `loadContentAwait()` 先取到章节再加入 loading 状态，避免无章节/越界时污染状态。

**回归点**:
- 已缓存在线书连续切章不应卡“加载中”。
- WebDAV 下载到本地的书籍连续翻章节不应需要重启。
- 首章上一章、末章下一章越界请求不能崩溃，也不能污染 loading 状态。
