# Android 14+/17 适配踩坑记录

> 项目: 阅读 (legado) Android 电子书阅读器
> 日期: 2026-05-31；最近更新: 2026-06-01
> 目标: targetSdk=36, compileSdk=36, minSdk=26

---

## 一、编译环境问题

### 1. Gradle Wrapper 本地路径反复被恢复
**现象**: 每次 commit 前我手动把 `gradle-wrapper.properties` 改为本地文件路径，但用户 git 操作时会被恢复为 HTTPS URL，导致下次编译时又去下载 Gradle。

**解决**: 每次编译前检查并改回本地路径，编译完再恢复为 HTTPS URL。

```properties
# 编译用（本地）
distributionUrl=file:///home/liu/Documents/legado-master/.gradle/gradle-8.13-bin.zip
# 提交用（恢复原样）
distributionUrl=https\://services.gradle.org/distributions/gradle-8.13-bin.zip
```

**建议**: 可以在项目根目录放一个 `local-build.sh` 脚本来自动切换。

---

### 2. 代理设置不生效
**现象**: `http_proxy` 环境变量设置了，但 Gradle Wrapper 下载时仍报 `Connection timed out`。

**原因**: Gradle Wrapper 用 Java 自己的网络栈，不一定读取 shell 环境变量。

**解决**: 将 Gradle zip 包放到本地 `.gradle/` 目录，使用 `file:///` URL 引用，彻底绕过下载。

---

## 二、Kotlin 编译错误

### 3. Lambda 中 this 解析错误
**文件**: `AudioPlayService.kt`, `BaseReadAloudService.kt`

**代码**:
```kotlin
ServiceCompat.startForeground(this, id, notification, type)
```
**错误**: `this` 在 lambda 中被解析为 `CoroutineScope` 而非 `Service`。

**解决**: 显式指定 `this@ServiceName`：
```kotlin
ServiceCompat.startForeground(this@AudioPlayService, id, notification, type)
```

---

### 4. 缺少 import
**文件**: `ContextExtensions.kt`

**错误**: 使用 `NetworkCapabilities` 但忘记 import `android.net.NetworkCapabilities`。

**解决**: 添加 import。建议修改后先用 IDE 或 `./gradlew ktlint` 检查 import。

---

### 5. 平台 API 不存在
**文件**: `MediaPlaybackLock.kt`

**错误**: 想用 `WIFI_MODE_HIGH_PERF` (API 29)，但 platform-36 的 android.jar 里没有这个常量。

**解决**: 回退到 `WIFI_MODE_FULL_HIGH_PERF`，加 `@Suppress("DEPRECATION")`。

---

### 6. ProgressDialog init receiver 类型不匹配
**文件**: `AndroidDialogs.kt`

**代码**:
```kotlin
AlertDialog.Builder(this).apply {
    if (init != null) init()  // init 是 (AlertDialog.() -> Unit)?
}
```
**错误**: `init()` 调用时 `this` 是 `AlertDialog.Builder`，但 `init` 接收者类型是 `AlertDialog`。

**解决**: 先 `create()` 得到 `AlertDialog`，再执行 `init`：
```kotlin
val dialog = AlertDialog.Builder(this).apply { ... }.create()
if (init != null) dialog.init()
dialog.show()
```

---

### 7. getParcelable 可空性处理
**文件**: `FileAssociationDialog.kt`

**代码**:
```kotlin
val uri = BundleCompat.getParcelable(arguments ?: return@registerHandleFile, "uri", Uri::class.java)
result.uri?.let { treeUri ->
    importBook(treeUri, uri)  // uri 是 Uri?，importBook 需要 Uri
}
```
**错误**: `getParcelable` 返回 `Uri?`，但 `importBook` 参数要求非空 `Uri`。

**解决**: 用 `?: return@registerHandleFile` 处理可空性：
```kotlin
val uri = BundleCompat.getParcelable(..., Uri::class.java)
    ?: return@registerHandleFile
```

---

### 8. StaticLayout 替换时括号没对齐
**文件**: `TextChapterLayout.kt`

**原始**:
```kotlin
else StaticLayout(
    text, textPaint, visibleWidth,
    Layout.Alignment.ALIGN_NORMAL, 0f, 0f, true
)
```
**错误替换**:
```kotlin
else StaticLayout.Builder.obtain(...)
    .build()
)
```
**错误**: 多出一个 `)`。

**解决**: 删除多余的 `)`。

---

## 三、运行时 Bug

### 9. WebDAV 同步后空章节列表崩溃
**文件**: `ReadBook.kt`(104), `ChapterListFragment.kt`(125)

**现象**: 设备A下载书籍 → 设备B阅读并同步到WebDAV → 设备A同步后打开书籍 → 崩溃
**错误**: `Index 0 out of bounds for length 0`

**根因**: `chapterList?.get(0)` 在章节列表为空时抛异常。

**修复**: `chapterList?.get(0)` → `chapterList?.firstOrNull()`

### 10. WebDAV 首次配置误报应用验证失败
**记录日期**: 2026-06-01  
**验证环境**: targetSdk=36, compileSdk=36, minSdk=26；反馈设备 Android 8.1 老平板、Android 16
**文件**: `AppWebDav.kt`

**现象**:
- 首次安装后依次填写 WebDAV 服务器地址、账号、密码，保存密码时提示"webDav应用验证失败"。
- 已配置成功的版本先输入错误密码会正常提示验证失败；随后重新输入正确密码，仍持续提示"webDav应用验证失败"。

**根因**:
- 旧 `upConfig()` 在函数入口立即执行 `authorization = null` / `defaultBookWebDav = null`，随后才做网络校验。如果校验失败，旧的可用运行时状态已被清空，导致后续"远程书籍"提示"WebDAV没有配置"。
- 旧逻辑使用 `服务器地址 + AppConfig.webDavDir` 作为认证检查地址，但这个应用子目录可能还未创建。部分 WebDAV 服务对不存在的子目录 `PROPFIND` 返回 401，导致应用把"目录不存在/未创建"误判为账号密码错误。

**修复**:
- `upConfig()` 改为先读账号密码。仅当账号或密码为空时才清空运行时配置（用户主动清空）；有配置时，先完成校验再决定是否替换。
- `authorization` 和 `defaultBookWebDav` 只在**全部校验通过+子目录创建成功**后才一次性替换，校验失败不破坏旧的可用运行时状态。
- 拆分 `serverWebDavUrl` 和 `rootWebDavUrl`。认证优先检查用户填写的服务器根地址，通过后再创建应用子目录和业务子目录。
- 对 WebDAV URL、账号、密码做首尾空白清理。
- `checkAuthorization()` 失败不再 `removePref(PreferKey.webDavPassword)`，避免删除用户已输入的密码。
- 使用 `AtomicInteger configVersion` 忽略过期校验结果，只有最后一次配置更新能写入状态或弹出失败提示。
- `WebDav.check()` 增加日志：记录检查的 URL、HTTP 状态码、是否 401、网络异常信息（不含明文密码）。

**回归点**:
- 旧版本已有可用 WebDAV 配置，启动后远程书籍可用。
- 进入 WebDAV 设置，输入错误密码，确认提示验证失败。
- 再输入正确密码，确认不再提示验证失败。
- 打开 WebDAV 远程书籍，不应提示"WebDAV没有配置"。
- 重启 App 后 WebDAV 仍可用。
- 首次安装新配置也应可用。
- 新设备首次配置 WebDAV，默认子目录 `legado` 不存在时应能自动创建。
- 连续快速输入错误密码、正确密码时，旧的失败请求不能覆盖成功状态。

---

## 四、API 适配要点总结

### 前台服务 (Android 14+)
- 必须使用 `ServiceCompat.startForeground(this@Service, id, notification, ServiceInfo.FOREGROUND_SERVICE_TYPE_*)`
- 同时需要在 Manifest 中声明 `android:foregroundServiceType`

### PendingIntent (Android 12+)
- `FLAG_MUTABLE` → `FLAG_IMMUTABLE`

### 系统 UI 标志 (Android 15+)
- `SYSTEM_UI_FLAG_HIDE_NAVIGATION` 等新 API 上彻底废弃
- 必须用 `WindowInsetsController` + `WindowInsetsCompat`
- 旧 API (26-29) 仍需保留 `systemUiVisibility` 分支

### getParcelable / getSerializable
- Bundle: `BundleCompat.getParcelable(bundle, key, Class::class.java)`
- Intent: `IntentCompat.getParcelableExtra(intent, key, Class::class.java)`
- 注意返回类型是 `T?`，需要处理 null

### Class.newInstance()
- `T::class.java.newInstance()` → `T::class.java.getDeclaredConstructor().newInstance()`

### overridePendingTransition (API 34+)
- `overrideActivityTransition(OVERRIDE_TRANSITION_OPEN/CLOSE, enterAnim, exitAnim)`
- 需要在 `startActivity`/`finish` **之前**调用

### ProgressDialog (API 26+ deprecated)
- 替换为 `AlertDialog` + `ProgressBar`
- 水平进度条: `ProgressBar(context, null, android.R.attr.progressBarStyleHorizontal)`

### FragmentPagerAdapter → FragmentStateAdapter
- 需要 `ViewPager2` + `TabLayoutMediator`
- `getCount()` → `getItemCount()`
- `getItem()` → `createFragment()`
- `getPageTitle()` → `TabLayoutMediator` 回调

### StaticLayout 构造器 (API 28+ deprecated)
- `StaticLayout.Builder.obtain(source, start, end, paint, width)`
- `.setAlignment()`, `.setLineSpacing(extra, mult)`, `.setIncludePad(bool)`, `.build()`

### WindowMetrics (API 30+)
- 替代 `windowManager.defaultDisplay.getRealMetrics()`
- `windowManager.currentWindowMetrics.bounds`

### 预测性返回手势 (API 33+)
- 主题添加 `<item name="android:enableOnBackInvokedCallback">true</item>`
- 确保所有 Activity 已使用 `OnBackPressedDispatcher`

---

## 五、文件清单

### 修改文件（共 29 个）

**第一轮 (17 文件)**:
- `AndroidManifest.xml` - screenOrientation, foregroundServiceType
- `AudioPlayService.kt`, `BaseReadAloudService.kt`, `CacheBookService.kt`, `CheckSourceService.kt`, `DownloadService.kt`, `ExportBookService.kt`, `WebService.kt`
- `NetworkUtils.kt`, `ContextExtensions.kt`, `ActivityExtensions.kt`
- `BaseReadBookActivity.kt`, `ClickActionConfigDialog.kt`
- `BaseDialogFragment.kt`
- `MediaPlaybackLock.kt`, `WebService.kt`
- `WebSettingsExtensions.kt`

**第二轮 (12 文件)**:
- `ActivityResultContracts.kt`
- `AndroidDialogs.kt`
- `TocActivity.kt`, `activity_chapter_list.xml`
- `backup_rules.xml`, `data_extraction_rules.xml`
- `ActivityExtensions.kt`, `FragmentExtensions.kt`, `ConfigActivity.kt`
- `MediaButtonReceiver.kt`
- `PermissionActivity.kt`

**第三轮 (9 文件)**:
- `HandleFileContract.kt`, `GroupEditDialog.kt`, `BookmarkDialog.kt`, `FileAssociationDialog.kt`
- `WelcomeConfigFragment.kt`
- `ViewExtensions.kt`
- `TextChapterLayout.kt`, `TextPage.kt`
- `styles.xml`

**Bug Fix (2 文件)**:
- `ReadBook.kt`
- `ChapterListFragment.kt`

---

## 六、编译命令

```bash
# Debug 编译（仅验证 Kotlin 代码，不生成 APK）
ANDROID_HOME=/home/liu/Documents/legado-master/.sdk \
./gradlew :app:compileAppDebugKotlin --no-daemon

# Debug APK（无签名，测试用）
ANDROID_HOME=/home/liu/Documents/legado-master/.sdk \
./gradlew :app:assembleAppDebug --no-daemon

# Release APK（需要签名配置）
ANDROID_HOME=/home/liu/Documents/legado-master/.sdk \
./gradlew :app:assembleAppRelease --no-daemon
```

---

## 七、构建 Release APK 注意事项

### 1. Gradle Wrapper 本地路径切换
**每次构建前必须做这件事，否则会卡住**

这个项目没有全局 Gradle，依赖 `.gradle/gradle-8.13-bin.zip`：

```properties
# 构建前（临时改为本地路径）
distributionUrl=file:///home/liu/Documents/legado-master/.gradle/gradle-8.13-bin.zip

# 构建完（恢复原样，避免误提交）
distributionUrl=https\://services.gradle.org/distributions/gradle-8.13-bin.zip
```

**坑**：`gradle-wrapper.properties` 被 restore/checkout 后会恢复成 https，编译时会重新下载，国内网络大概率超时。

**建议**：写个 `build.sh` 脚本自动切换，或者把本地 zip 路径一直保留（反正 `.gitignore` 已忽略 wrapper 目录）。

---

### 2. Release 签名配置
**签名信息存储在 `app/gradle.properties`，不会提交到 git**（已加进 `.gitignore`）

```properties
# app/gradle.properties
RELEASE_STORE_FILE=signing/legado-self-release.jks
RELEASE_STORE_PASSWORD=********
RELEASE_KEY_ALIAS=legado-self
RELEASE_KEY_PASSWORD=********
```

**Keystore 位置**: `app/signing/legado-self-release.jks`

> ⚠️ **重要**：`app/signing/` 和 `app/gradle.properties` 已加入 `.gitignore`，不会误提交。但 keystore 文件和密码必须本地备份，丢了就无法覆盖更新现有 release 版本！

---

### 3. 构建产物位置

```
app/build/outputs/apk/app/release/
├── legado_arm64.apk   (10MB, ⭐ 推荐安装 - 绝大多数手机)
├── legado_all.apk     (16MB, 包含所有架构)
├── legado_x64.apk     (11MB, 模拟器/平板)
├── legado_x86.apk     (11MB, 旧模拟器)
└── legado_armv7.apk   (9.6MB, 老旧设备)
```

---

### 4. Release 构建耗时
- 首次构建：约 15-20 分钟（需要下载依赖、R8 混淆、Lint 检查）
- 后续构建：约 3-5 分钟（大量 task 会被缓存）
- 关键阶段：`:app:minifyAppReleaseWithR8`（R8 混淆，最耗时）

---

### 5. 构建成功但一堆 deprecation 警告
正常现象！这些警告来自 API 26-29 兼容分支，不影响功能：
- `systemUiVisibility` deprecated
- `FLAG_TRANSLUCENT_STATUS` deprecated
- `navigationBarColor` deprecated

---

## 八、前台服务启动入口统一（2026-06-01）

### 背景
Android 8+、12+、14+ 对后台启动前台服务限制越来越严。项目里朗读、音频播放、缓存、导出、Web 服务都依赖 Service，如果入口还走普通 `startService()`，容易出现偶发启动失败或被系统拒绝。

### 改法
1. **工具函数修正** (`ContextExtensions.kt`)：
   - `startForegroundServiceCompat()` 改为直接调用 `ContextCompat.startForegroundService()`，不再先尝试 `startService()` 再 fallback
   - 新增 `safeStartForegroundService()` 捕获异常并写 AppLog + toastOnUi，用于需要错误提示的场景

2. **启动类入口改用前台启动**：
   - `ReadAloud.play()` → `safeStartForegroundService()`
   - `AudioPlay.play()` / `loadOrUpPlayUrl()` / `addTimer()` / `skipTo()` / `prev()` / `next()` → `startForegroundServiceCompat()`
   - `CacheBook.start()` → `startForegroundServiceCompat()`
   - `CheckSource.start()` → `startForegroundServiceCompat()`
   - `Download.start()` → `startForegroundServiceCompat()`
   - `ExportBookService` 所有导出启动入口（CacheActivity、BookshelfManageActivity）→ `startForegroundServiceCompat()`
   - `WebService.start()` → `startForegroundServiceCompat()`

3. **运行中 action 保留普通 startService**：
   - `ReadAloud.pause()` / `resume()` / `stop()` 等 → 先判断 `BaseReadAloudService.isRun`，再 `startService()`
   - `AudioPlay.pause()` / `resume()` / `stop()` 等 → 先判断 `AudioPlayService.isRun`，再 `startService()`
   - `CacheBook.stop()` / `remove()` → 先判断 `CacheBookService.isRun`，再 `startService()`
   - `CheckSource.stop()` / `resume()` → 先判断 `CheckSourceService.isRun`，再 `startService()`
   - `WebService.serve()` → 先判断 `WebService.isRun`，再 `startService()`

4. **新增 isRun 标志**：
   - `CheckSourceService` 原本没有 companion object，新增了 `isRun` 标志，在 `onStartCommand` 设为 true，`onDestroy` 设为 false

### 核心原则
- 启动前台服务 → `ContextCompat.startForegroundService()`
- 给已运行服务发命令 → 普通 `startService()`，并先判断 `isRun`
- 不要机械替换所有 `startService()`，否则会为发 stop/pause 反而启动一个新服务

---

## 九、未处理项（风险过高或需等待）

1. **Edge-to-Edge**: 需全局调整所有 Activity 的布局，影响面过大，建议等 Android 17 最终版
2. **存储迁移**: `MANAGE_EXTERNAL_STORAGE` 迁移到 Scoped Storage 对阅读类 App 风险过高
3. **PhoneStateListener → TelephonyCallback**: API 31+ 废弃，但重构量大且影响朗读功能
4. **READ_PHONE_STATE 权限**: API 33+ 需更细分权限，当前代码仍需兼容旧设备

---

*记录结束*
