# Android API 26-37 适配踩坑记录

> 最新修订: 2026-09-06 CST
> 项目: 阅读 (legado) Android 电子书阅读器
> 最新问题记录: 2026-09-05；文档复核: 2026-09-06
> 当前 SDK 边界: compileSdk=37；`app` minSdk=26/targetSdk=37，`modules/book` minSdk=21/targetSdk=36，`modules/rhino` minSdk=26/targetSdk=36

---

## 文档维护规则

- 本文件最多保留 15 条适配/构建/运行时问题的详细记录。
- 当达到 15 条后需要新增下一条时，把当前整个文档按归档日期改名并移入 `文档归档/`，然后新建 `适配踩坑记录.md`。不再来回挪动单条条目。
- 主文件保留长期有效规则和归档索引，避免后续 AI 读取时被过期细节干扰。
- 时间敏感结论必须写明记录日期、验证日期、适用 SDK/依赖/系统版本；超过 30 天、target/compile SDK 改变、核心依赖升级或系统规则变化时重新验证。

## 归档索引

- `文档归档/适配踩坑记录-2026-05_06.md`: 早期 Gradle 下载/代理问题、Release 构建附录、前台服务启动入口统一、提交前拦截本地文件、本地 Debug 编译验证脚本等；2026-06-29 前归档。
- `文档归档/适配踩坑记录-2026-07.md`: 2026-06 至 2026-07-12 的 Web 进度同步、导入弹窗、验证码、文件关联、TTS、WebDAV、阅读器越界、日志模块重构等记录；2026-07-12 归档。
- `文档归档/适配踩坑记录-2026-07-15.md`: 2026-07-12 至 2026-07-15 的日志启动、异步 UI、协程和系统栏、依赖升级、Kotlin/Gradle/AGP/Web 构建迁移等 15 条记录；2026-07-15 归档。
- `文档归档/适配踩坑记录-2026-09-05.md`: 2026-07-15 至 2026-08-21 的阅读页点击、本地书导入、WebService、WebDAV、Android 17 权限和 Release/R8 等 15 条记录；2026-09-05 归档。

## 长期有效规则

- Gradle/AGP: AGP 9 使用 `androidComponents`，split APK 取 ABI 时比较 `it.filterType.name() == "ABI"`；不要恢复旧 `applicationVariants`。
- Release/R8 反射: `-keepattributes Signature` 在 R8 full mode 下不能单独保证业务 DTO 的泛型签名存在；Gson 反射模型必须显式保留，或改用不依赖反射泛型的 JSON 树编解码。Debug 成功不能替代 Release DEX 与真机验证。
- UI 主题: `Base.AppTheme` 继承 `Theme.AppCompat.DayNight.NoActionBar`；普通按钮使用 AppCompat/项目控件，不得直接引入会强制校验 `Theme.MaterialComponents` 的 `MaterialButton`。现有 `TabLayout`/`FloatingActionButton` 不按本条泛化替换。
- GitHub Actions: `.github/workflows/test.yml` 的发布 job 使用 job 级 `contents: write`；否则 APK 虽能编译并上传 Artifact，创建或更新 beta Release 时仍会收到 403。
- 编译验证: Kotlin/代码改动后优先跑 `scripts/check-debug.sh`；生成 Debug APK 跑 `./gradlew :app:assembleDebug --no-daemon`。最终可安装的个人签名 Release 必须在提交且工作树干净后运行 `scripts/build-signed-release.sh`。
- minSdk: `app`/`modules/rhino` 为 26，`modules/book` 为 21。调用高于所在模块 minSdk 的 API 必须按 API level 分支，旧 API 只在旧版本分支局部 `@Suppress("DEPRECATION")`。
- Content Provider: `ReaderProvider` 保持导出以支持外部集成，但读写必须由随 `${applicationId}` 变化的 `signature` 权限保护；不要恢复无权限导出。
- 阅读页尺寸: `ContentTextView` 宽度不变、仅高度变化的 300ms 延迟只能保留最新请求；任何新尺寸（尤其横竖屏宽度变化）都必须撤销旧延迟任务，并用代次检查禁止过期任务回写分页尺寸。
- 阅读页分页: 实际尺寸应用后直接清除三章旧分页，不得以 Activity 初始化状态跳过失效；尺寸快照在 Main 构造，async/await 回写和同书缓存复用均校验分页尺寸代次，取消必须继续传播，详见 0.4。
- 前台服务: 启动入口用 `ContextCompat.startForegroundService()`；服务内必须用 `ServiceCompat.startForeground(..., type)` 并声明 `foregroundServiceType`。销毁时显式移除前台通知，并设置停机门闩短路所有异步刷新。
- WebService 熄屏访问: 服务启动和每次 HTTP/WebSocket 入站访问都续期 90 秒 CPU/Wi-Fi 活跃租约；只有 `webServiceWakeLock=true` 时保持到服务停止。
- Android 17 局域网: targetSdk 37 的 WebService 必须在 Manifest 声明并在用户启用时运行时申请精确的 `ACCESS_LOCAL_NETWORK`；拒绝、取消或撤销后回退关闭。
- WebDAV: 日志不得包含账号、密码、Authorization header、URL query/fragment；进度下载写入前校验章节范围和 v2 身份，仅同步进度使用 `RangeOnly`。HTTP 404 统一表示远端文件缺失；只将 `UnknownHostException`/`ERR_NAME_NOT_RESOLVED` 归类为网络或 DNS 问题。
- SAF 本地书: 跨设备恢复后的 `content://.../tree/...` 可能失效；不要自动猜路径替换，必须提示用户重新选择书籍目录。
- 本地书身份: 书名/作者/文件名只用于展示和检索；数据库使用 `localFileKey = SHA1(文件 URI + 内容 SHA1)` 区分设备内文件，WebDAV 使用不含路径的 `bookProgressKey`。
- Web 前端: 唯一生产入口是 `modules/web/index.html` → `src/main.ts`。修改运行时源码后必须同步 `app/src/main/assets/web/`；旧 `/uploadBook/index.html` 只做 `/#/uploadBook` 兼容跳转。
- Web 阅读时长: 浏览器只提交经过时长，手机端限制单次 5 秒至 24 小时并用自身时间确定自然日；任何超过 10 分钟的已观察空闲间隔都必须让当前章节保持作废。
- 日志: 文件写入使用独立单线程，单条记录只写一次；导出前必须 flush 并创建一致性快照，初始化失败必须持久化错误并反馈到导出状态。
- 系统栏: API 30+ 使用 `WindowInsetsControllerCompat`；API 26-29 的 `systemUiVisibility` 只能留在明确旧版分支中。
- 多语言资源: 默认资源新增可翻译字符串时必须同步全部受支持语言目录并保持格式占位符一致；完整 lint 有历史错误时按资源名和错误增量判断本轮回归。

---

## 最近记录

### 0.1 熄屏期间横竖屏切换后阅读页只显示左半文字

**记录日期**: 2026-09-05
**验证日期**: 2026-09-05（3 项尺寸时序定向测试、183 项全量 JVM 测试、Debug Kotlin 编译、`scripts/check-debug.sh` 及 Debug APK 组装通过；小米平板真机待安装验收）
**验证环境**: AGP 9.3.0、Gradle 9.6.1、Kotlin 2.4.10，app compileSdk/targetSdk=37；当前无 ADB 连接设备
**文件**: `ChapterProvider.kt`, `ViewSizeUpdateCoordinator.kt`, `ViewSizeUpdateCoordinatorTest.kt`

**问题与根因**:
- 阅读页在 Manifest 中自行处理 `orientation|screenSize`，`ContentTextView.onSizeChanged()` 把最新宽高交给 `ChapterProvider`，后者重算可见区并触发正文重分页。
- 为避免系统栏显隐造成频繁重排，旧逻辑把“宽度不变、只改高度”延迟 300ms，但收到新尺寸时不会统一撤销旧任务。
- 熄屏旋转时可先收到“竖屏旧宽度 + 横屏新高度”的中间尺寸，再收到正确横屏尺寸。正确尺寸虽然先应用，旧延迟任务仍会在 300ms 后回写竖屏宽度，因此横屏画布只在左侧按竖屏宽度排版，右侧留空。

**修复与约束**:
- 用 `ViewSizeUpdateCoordinator` 收口尺寸更新；每个新的有效尺寸都先取消上一个延迟任务，宽度变化仍立即重排，仅高度变化仍保留 300ms 防抖。
- 延迟回调再校验请求代次、当前宽度和当前尺寸；即使底层取消与回调竞态，过期任务也不能覆盖更新的横屏尺寸。
- 不改动双页阅读设置、分页算法、进度位置或 Activity 方向策略。

**回归点**:
- 竖屏阅读后熄屏，在熄屏期间转为横向再唤醒，正文必须按横屏最终宽高重分页，不得只占左半屏。
- 横屏熄屏后转竖屏、亮屏直接往返旋转、分屏/悬浮窗连续调整高度时，最终尺寸均为 latest-wins，并保留仅高度变化的防抖。
- Cover、Slide、Simulation、NoAnim 及 Scroll 模式分别检查当前页定位、双页判定、页眉页脚和翻页截图；自动化结果不能替代小米平板锁屏旋转真机验收。

### 0.2 平板列表滚动后部分条目贴左、部分条目居中

**记录日期**: 2026-09-05
**验证日期**: 2026-09-05（代码审查、`scripts/check-debug.sh` Debug Kotlin 编译、3 项宽度计算 JVM 测试、含 2 项新增布局测试的 AndroidTest Kotlin 编译通过；Android 布局测试运行及小米平板真机待验证；按用户要求不生成 APK）
**适用环境**: app compileSdk/targetSdk=37，RecyclerView 1.4.0；现场日志版本 3.26.090101 (17309)，横屏窗口 3200×2136、440dpi
**文件**: `AdaptiveContentWidth.kt`, `AdaptiveContentLayoutTest.kt`

**证据与原因**:
- 14:18:49 从阅读页返回主界面，14:18:57 的“我的”截图显示上方条目贴左、后续条目居中。窗口尺寸正常，截图时间未发现崩溃/ANR；14:19:53 的 ConfigActivity 恢复异常晚于截图，不能解释此错位。
- 2026-09-01 的限宽实现于 `onLayoutChange` 中直接修改水平 padding。Android `View.layout` 在监听器返回后清除 `PFLAG_FORCE_LAYOUT`，可能使 padding 产生的 `requestLayout` 失效。已附着条目保留旧布局，而滚动中新复用条目使用新 padding，与截图的混排吻合；日志未记录逐条布局坐标，完整触发时序仍需真机验收。
- 此问题位于主界面 RecyclerView，和 0.1 阅读页正文分页的延迟尺寸任务是两条独立路径。

**修复与约束**:
- `onLayoutChange` 只安排 `post` 任务，待当前布局返回后才更新 padding，以保留新一轮重新布局请求。
- 连续尺寸变化合并为一个任务，执行时读取当前 View 宽度、density 和列表/网格模式，不保存中间尺寸；上下 padding 保留执行时的值。
- detach 时取消任务，attach 时重新安排；保持现有 840dp、发现页 12dp 最小边距和网格全宽策略。
- 不要求替换 RecyclerView、重建 Activity、刷新数据集或改变正文分页算法。

**回归点**:
- Android 布局测试验证宽窄切换后所有可见行左右坐标一致，继续滚动复用后仍一致；并检查连续尺寸/模式更新和上下 inset 保留。
- 真机横屏启动，打开小说后返回“我的”，上下滚动至 TXT 目录规则、规则订阅和阅读记录均对齐；再检查旋转、全屏/分屏往返、切后台返回。
- 两套书架列表与网格切换、发现页和各设置页检查点击区域、滚动位置及底部导航；编译和 JVM 结果不能替代实际布局测试运行及用户真机验收。

## 0.3 稳定依赖分批升级（2026-09-05）

- 记录日期: 2026-09-05；版本来源为上游发布说明、Google Maven / Maven Central 和 npm registry。
- 适用环境: Gradle 9.6.1 / AGP 9.3.0 / Kotlin 2.4.10 / JDK 17；SDK 边界保持不变。
- 第一批: KSP 2.3.10 → 2.3.11、jsoup 1.22.2 → 1.23.2、ConstraintLayout 2.2.1 → 2.2.2。
- KSP 继续生成 Room / Glide 代码；标准检查用 `scripts/check-debug.sh`，不能因上游任务跳过修复而绕过编译生成。
- jsoup 必须通过实际 `AnalyzeByJSoup` / `AnalyzeByXPath` 适配器检查旧规则、CSS 回退、负索引、正文过滤、XML CDATA 和表格片段，不能只测试 `Jsoup.parse()` 能否调用。
- 新增 `HtmlRuleCompatibilityTest`；2026-09-05 第一批 `scripts/check-debug.sh`、全量 188 项 JVM 测试（含 5 项解析回归）和 AndroidTest Kotlin 编译通过。实际书源联网解析、布局和真机验收待验证。
- 第二批: AppCompat 1.7.1 → 1.8.0、Fragment 1.8.9 → 1.9.0、WebKit 1.16.0 → 1.17.0；2026-09-05 标准 Debug Kotlin 编译、188 项 JVM 测试和 AndroidTest Kotlin 编译通过，无需业务代码适配。
- AppCompat 1.8 最低 API 23，当前仅 `app` 引用且 minSdk 26，`modules/book` 的 API 21 边界不受影响；保持 `Theme.AppCompat`，不用版本升级替代主题迁移。
- WebKit 是兼容 API 库，其版本不代表设备 WebView 引擎版本；1.17 新 API 仍需 WebView feature 检查。本轮未启用新导航、预连接或缓存 API，现有暗黑模式和系统版本分支保留。
- 第二批真机回归: 主题/字体/弹窗、阅读页与设置页旋转/分屏、Fragment 返回与状态恢复、内置浏览器和 RSS 页面；JVM 的主题布局约束检查只证明静态布局规则。
- 第三批: OkHttp 5.4.0 → 5.5.0、Media3 1.10.1 → 1.11.0、Protobuf Javalite 4.35.1 → 4.36.1；2026-09-05 标准 Debug Kotlin 编译、196 项 JVM 测试和 AndroidTest Kotlin 编译通过。
- 实际编译发现 OkHttp 5.5 移除 `HttpMethod.redirectsWithBody`，`redirectsToGet` 新增状态码参数；`AbsCallBack.buildRedirectRequest` 改用新入口，转 GET 时清除请求体头，否则保留原方法和 body。`CronetRedirectTest` 覆盖 POST 301/302/303、307/308 保持方法、PROPFIND 全部状态、QUERY 301/302 与 303 差异及 GET/HEAD；不改现有重定向许可和其他请求头策略。
- OkHttp 升级检查 `AbsCallBack`、`DecompressInterceptor`、`OkHttpUtils` 和 `NetworkUtils` 的内部 API 调用；保留现有 DNS、代理、Cookie、TLS 配置与 Cronet 回退顺序，不宣称升级即可启用 ECH 或改变证书验证策略。
- `DecompressInterceptorTest` 在实际 OkHttp interceptor 链中检查 gzip/raw deflate 解压与响应头清理，以及 Range 请求保留压缩标记/原始字节；该测试不访问外网，不能证明 TLS、DNS 和 Cronet 原生引擎兼容。
- Media3 所有播放/HLS/UI/OkHttp 数据源共享一个版本，重点复核 `ExoPlayerHelper`、`InputStreamDataSource` 与朗读/音频/视频服务；真机 HTTP TTS、缓存、拖动进度和音频 offload 待验证。
- Cronet 保持 143.7445.0：Google Maven 的 500.0.2 `cronet-api` / `cronet-embedded` 已是迁移空包，后者 AAR 不含 `.so`，必须先迁移依赖坐标与 `CronetLoader` 的下载路径，不能直接改共用版本号。
- 第四批: Web 按现有 semver 范围更新；Vue 3.5.42、Router 5.3.1、VueUse core/shared 14.4.0、hotkeys-js 4.0.7、Vite 8.2.2，ESLint 10.10.0、eslint-plugin-vue 10.10.0、Sass Embedded 1.104.0、vue-tsc 3.3.11、Prettier 3.9.6、unplugin-auto-import 21.1.0、@tsconfig/node20 20.1.10。其他 manifest 下限按既有锁定版本对齐，不误算成新的实际版本升级。
- Web 验证环境 Node 22.22.1 / pnpm 9.15.9；Pinia 3.0.4、TypeScript 5.9.3、npm-run-all2 8.0.4 保留，升级主版本需另行评估，未修改本机工具链或 CI。
- 2026-09-05 Web frozen 安装、生成声明后的类型检查、15 项阅读状态测试、ESLint（前后均 0 error / 0 warning）与 149 模块生产构建通过；`auto-imports.d.ts` 的新生成注释同步保存。
- 桌面 1280×900 / 窄屏 390×844 无头 Chrome 使用本地固定 API 响应检查欢迎页、书架、阅读页、刷新、右键切章、离开提交进度、传书/书源/替换规则路由，均无 pageerror；这不替代真实手机 WebService / WebDAV / 上传写入验证。
- 2026-09-06 收尾复核：Debug / Release-R8 打包成功（202 项任务），5 个 Debug 和 5 个 Release APK 中的 `index.html` / `favicon.ico` 均与本次 dist 和仓库 assets 字节一致。该构建用于验证；正式个人签名 Release 交付仍须在最终提交后按 `scripts/build-signed-release.sh` 流程生成。
- 本轮仅做依赖与兼容适配，检查后不新增用户更新日志；不把版本清单作为用户可见功能。

### 0.4 横屏转竖屏后右侧文字截断、下方留空

**记录/验证日期**: 2026-09-06（`scripts/check-debug.sh`、198 项全量 JVM 测试含 5 项尺寸时序测试、AndroidTest Kotlin 编译通过；新增 2 项真实 Android 分页测试未运行，真机待验证；按用户要求不生成 APK）
**适用环境**: 原生阅读页，app compileSdk/targetSdk=37、minSdk=26；用户明确复现为横屏转竖屏
**文件**: `ChapterProvider.kt`、`ReadBook.kt`、`TextChapter.kt`、`ViewSizeUpdateCoordinatorTest.kt`、`ReaderPageSizeTest.kt`

**证据与缺口**:
- 2026-09-05 18:25 截图为竖屏，长行右侧被裁切、下方大面积空白，符合继续绘制较宽、较矮分页的表现。已有 `/home/liu/localsend/logs/logcat.txt` 仅至 14:21，不能用这份日志证明 18:25 的完整时序或唯一根因。
- 首轮修复仅保护 `upViewSize()` 延迟任务。实际尺寸变化后仍通过一次性 `UP_CONFIG(5)` 请求重载，而 Activity 在初始化未完成时会忽略；同书初始化又可保留已完成的章节缓存，未检查其尺寸。
- `TextChapterLayout` 原先在 IO 构造时逐项读取主线程可变尺寸，可能读到跨两次更新的几何数据；两条正文加载路径也没有分页尺寸版本的回写门槛。

**修复与约束**:
- 尺寸应用后推进 `ChapterProvider.sizeGeneration`，直接取消并清除前/中/后三章分页；只有重载动作受 `isInitFinish` 控制，初始化前失效不能被忽略。
- `TextChapter` 标记构造时尺寸版本；`processContent()` 只把尺寸快照构造切到 Main，内容处理和实际排版仍在 IO，排版任务保留外层加载作用域。
- async/await 在 Main 挂载结果前、分页消费时检查尺寸版本；await 必须先校验再取消旧章节，过期结果按取消传播，不得吞掉取消后回调成功。同书缓存复用也检查尺寸版本。
- 清除缓存不改章节内字符位置；不改分页算法、单双页默认策略、Activity 配置变化处理或系统栏分支。新增尺寸日志只记录宽高、可见区、单双页和代次，不记录正文。

**验证边界与回归**:
- JVM 新增横转竖延迟回写、快速往返最终尺寸两项；AndroidTest 新增已完成三章缓存失效（无 Activity 观察者）、旧横屏结果拒绝与竖屏文本右边界检查。后者只编译，不能等同实际排版或截图验收。
- 用户生成 APK 后检查：横屏 → 竖屏、横屏熄屏 → 转竖屏唤醒、快速往返、退出后另一方向重开同书；确认右侧完整换行、页面高度、原文位置及继续翻页。
- 再覆盖自动双页、强制单页和各翻页动画/Scroll、分屏拖动。小米平板真实窗口与视觉效果均待验证，不沿用 0.1 的构建结果宣称本次真机问题已消失。

*Last updated: 2026-09-06*
