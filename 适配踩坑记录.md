# Android API 26-37 适配踩坑记录

> 最新修订: 2026-07-15 01:42 CST
> 项目: 阅读 (legado) Android 电子书阅读器
> 最近更新: 2026-07-15
> 当前 SDK 边界: compileSdk=37；`app` minSdk=26/targetSdk=37，`modules/book` minSdk=21/targetSdk=36，`modules/rhino` minSdk=26/targetSdk=36

---

## 文档维护规则

- 本文件最多保留 15 条适配/构建/运行时问题的详细记录。
- 当达到 15 条后需要新增下一条时，把当前整个文档改名为 `适配踩坑记录-YYYY_MM.md`（按归档时的年月）移入 `文档归档/`，然后新建空的 `适配踩坑记录.md` 重新开始记录。不再来回挪动单条条目。
- 主文件保留长期有效规则和归档索引，避免后续 AI 读取时被过期细节干扰。
- 时间敏感结论必须写明记录日期、验证日期、适用 SDK/依赖/系统版本；超过 30 天、target/compile SDK 改变、核心依赖升级、系统规则变化时重新验证。

## 归档索引

- `文档归档/适配踩坑记录-2026-05_06.md`: 早期 Gradle 下载/代理问题、Release 构建附录、前台服务启动入口统一、提交前拦截本地文件、本地 Debug 编译验证脚本等；2026-06-29 前归档。
- `文档归档/适配踩坑记录-2026-07.md`: 2026-06 至 2026-07-12 的 Web 进度同步、导入弹窗、验证码、文件关联、TTS、WebDAV、阅读器越界、日志模块重构等记录；2026-07-12 归档。
- 当前主文件即 2026-07-12 起的记录。

## 长期有效规则

- Gradle/AGP: AGP 9 使用 `androidComponents`，split APK 取 ABI 时比较 `it.filterType.name() == "ABI"`；不要恢复旧 `applicationVariants`。
- 编译验证: Kotlin/代码改动后优先跑 `scripts/check-debug.sh`；生成 Debug APK 跑 `./gradlew :app:assembleDebug --no-daemon`。最终可安装的个人签名 Release 必须在提交且工作树干净后运行 `scripts/build-signed-release.sh`；本地签名属性与 keystore 必须仅所有者可访问，脚本接受权限 `400` 或 `600`（通常使用 `600`）。脚本固定校验五个 APK、个人证书指纹、包名、版本与 `.idsig`/metadata 同批一致，再原子同步到 `app/app/release/`。不得只复制单个 APK，也不得把 Gradle 缺少签名属性时的 Debug 签名回退当成 Release 交付。
- minSdk: `app`/`modules/rhino` 为 26，`modules/book` 为 21。兼容分支不要删除；调用高于所在模块 minSdk 的 API 必须按 API level 分支，旧 API 只在旧版本分支局部 `@Suppress("DEPRECATION")`。
- 前台服务: 启动入口用 `ContextCompat.startForegroundService()`；服务内必须用 `ServiceCompat.startForeground(this@Service, id, notification, type)` 并声明 `foregroundServiceType`。Android 16+ `dataSync` 24 小时累计限额耗尽后，再次前台化会抛 `ForegroundServiceStartNotAllowedException`；WebService 这类长挂服务必须捕获并停服。
- WebDAV: 日志不得包含账号、密码、Authorization header、URL query/fragment；进度下载写入前必须校验章节范围。本地书可读性校验只在真正加载内容时做（`ReadableRequired`），仅同步进度（`downloadAllBookProgress`/`restoreBookProgressOnly`/`syncProgress`/`syncBookProgress`）用 `RangeOnly`，不得触发 SAF 目录权限检查。`durChapterPos < 0` 是章节最后一页持久化标记，进入阅读态或比较远近时必须使用有效正数位置。
- SAF 本地书: 备份恢复后不同设备的 `content://.../tree/...` 可能失效；不要自动猜路径替换，必须提示用户重新选择书籍目录。
- Web 前端: 修改 `modules/web/src` 后必须同步 `app/src/main/assets/web/`，否则 APK 不生效。`modules/web/pnpm-lock.yaml` 必须跟踪，安装使用 `pnpm install --frozen-lockfile`；依赖/类型工具配置重建后与 APK asset 字节一致时，不制造无意义的 assets diff，运行时源码变化仍必须暂存 assets。
- 日志: 文件 handler 使用独立单线程 `Legado-LogWriter`，不得与阅读/图片回收共用 `globalExecutor`；单条记录只经 `logger.log(record)` 写入一次；关闭开关时不得保留预初始化缓存；导出前必须 flush 并创建一致性快照；初始化失败必须持久化错误并反馈到导出状态。
- 系统栏: API 30+ 使用 `WindowInsetsControllerCompat`；API 26-29 的 `systemUiVisibility` 只能留在明确版本分支中。阅读页切换新实现时必须回归状态栏/导航栏分别隐藏、菜单弹出、配置弹窗、多窗口和明暗图标。

---

## 最近 15 条记录

### 1.5 Android Gradle Plugin 9.3 稳定版迁移

**记录日期**: 2026-07-14
**验证日期**: 2026-07-14（Debug Kotlin、74 项 JVM 单测、Release/R8 与 5 个 ABI APK 均通过）
**验证环境**: AGP 9.3.0, Gradle 9.6.1, Kotlin 2.4.10, JDK 17.0.19, compileSdk/targetSdk=37
**文件**: `gradle/libs.versions.toml`

**迁移内容与约束**:
- AGP `9.2.1 -> 9.3.0`；官方兼容下限为 Gradle 9.5、JDK 17，最高支持 API 37，本项目 Gradle 9.6.1/JDK 17/compileSdk 37 均在支持范围内。
- 继续使用 AGP 9 内置 Kotlin、稳定 Variant API 与 `androidComponents` 输出命名；ABI filter 仍以 `filterType.name() == "ABI"` 判断，all/arm64/armv7/x64/x86 五个产物无同名冲突。
- AGP 9.3 修复了内部以 Project 对象作为依赖记法的问题；升级后 Debug/单测/Release 不再出现此前来自 AGP 9.2.1 的 Gradle 10 弃用提示，项目脚本无需增加规避代码。
- 不跟进 AGP 9.4 alpha；后续稳定升级仍必须完整覆盖 KSP/Room、Lint Vital、R8/L8、资源压缩和 split APK，不能只跑 Kotlin 编译。

**回归点**:
- `scripts/check-debug.sh`、完整 JVM 单测和 `assembleRelease` 必须全部成功，Release 产物必须恰好包含五个预期 APK。
- `libarchive-jni.so` 无法 strip 与 512 MiB Metaspace 临近上限仍是既存告警；若后续构建失败或明显变慢，应独立测量并调整构建内存，不能归因于业务代码。
- 修改 APK 输出逻辑时继续禁止恢复已移除的 `applicationVariants`，并保留 enum filter 的正确比较方式。

### 1.4 Kotlin 2.4.10 补丁升级

**记录日期**: 2026-07-14
**验证日期**: 2026-07-14（Debug Kotlin、74 项 JVM 单测、Release/R8 与 5 个 ABI APK 均通过）
**验证环境**: Kotlin 2.4.10, AGP 9.2.1, Gradle 9.6.1, JDK 17.0.19, compileSdk/targetSdk=37
**文件**: `gradle/libs.versions.toml`

**升级内容与约束**:
- Kotlin `2.4.0 -> 2.4.10`；这是 2.4.0 语言版本线的稳定 bugfix release，不引入 2.4.20 Beta 的工具链预览改动。
- 项目继续使用 AGP 9 内置 Kotlin 与独立 Parcelize 插件，没有恢复已删除的 `kotlin-android` 插件；KSP 2.3.10、Room 生成代码和 Rhino Kotlin/Java 混编均可正常编译。
- Release 必须单独验证，不能用 Debug 成功替代：本批 R8、L8 desugar、资源优化与五个 split APK 均成功生成。
- Release 仍有既存的 `libarchive-jni.so` 无法 strip 提示和 512 MiB Metaspace 临近上限提示；本批未新增构建失败，也不混入内存参数调整。

**回归点**:
- `scripts/check-debug.sh`、完整 JVM 单测和 `assembleRelease` 都必须通过，并核对 all/arm64/armv7/x64/x86 五个 APK。
- 关注 const annotation、reified 类型推断及 Gradle 增量编译相关回归；2.4.10 官方修复集中在这些编译器/工具边界。
- Kotlin 2.4.20 Beta 仍按预发布策略暂缓，待稳定版后重新生成依赖报告再评估。

### 1.3 npm-run-all2 兼容升级与 9.x 暂缓

**记录日期**: 2026-07-14
**验证日期**: 2026-07-14（frozen 安装、`pnpm run build`、Web assets 同步检查均通过）
**验证环境**: Node 22.22.1, pnpm 9.15.9, npm-run-all2 8.0.4
**文件**: `modules/web/package.json`, `modules/web/pnpm-lock.yaml`

**升级内容与约束**:
- npm-run-all2 `6.2.6 -> 8.0.4`，保持既有 `run-p type-check "build-only {@}" -- && node ./scripts/sync.js` 脚本不变；并行类型检查、Vite 构建及随后执行 sync.js 的顺序均正常。
- npm-run-all2 9.0.x 要求 Node `^22.22.2 || ^24.15.0 || >=26.0.0`，与项目公开的 `^20.19.0 || ^22.13.0 || >=24` 引擎范围冲突，当前验证机 Node 22.22.1 也不在其支持范围内。
- 本项目没有使用 9.x 新增的 `--node-run`，也不依赖“glob 无匹配时报错”的旧行为；没有功能需求值得为了任务编排器抬高整个 Web 项目的 Node 最低版本。
- `pnpm run build -- <额外参数>` 会因脚本末尾的 `&& node ./scripts/sync.js` 把参数交给 sync.js，而不是 build-only；这是现有脚本边界，当前 CI/本地标准命令不传额外参数，本批不借工具升级改写脚本语义。
- 后续只有在项目统一放弃 Node 20、构建机升级到 npm-run-all2 支持的 Node 版本时，才重新评估 9.x；`@types/node` 继续保持 20。

**回归点**:
- `pnpm run build` 必须同时运行 type-check 和 build-only，并在两者成功后执行 sync.js；任一并行任务失败时不得继续同步产物。
- 标准 `pnpm run build` 不传额外参数；如未来需要切换 Vite mode，应先独立调整参数转发脚本并增加失败用例。
- 任务编排器本身不改变 bundle；重建的 `dist/index.html` 应与 APK asset 字节一致。

### 1.2 TypeScript 7 暂缓迁移

**记录日期**: 2026-07-14
**验证日期**: 2026-07-14（查阅 TypeScript 7.0 官方发布说明，并用独立 dlx 环境复测 vue-tsc 组合）
**验证环境**: Node 22.22.1, vue-tsc 3.3.7, TypeScript 7.0.2
**文件**: `modules/web/package.json`（评估后保持 TypeScript 5.9.3，不改依赖）

**暂缓原因与约束**:
- TypeScript 7.0 是 Go 原生重写版本，当前没有稳定的编程 API；官方明确指出 Vue/Volar 等嵌入 TypeScript 的工作流暂时不能使用 7.0，Vue 项目需要继续使用旧编译器线。
- 独立执行 `vue-tsc 3.3.7 + TypeScript 7.0.2` 时在启动阶段即抛出 `ERR_PACKAGE_PATH_NOT_EXPORTED`，因为 `typescript/lib/tsc` 不再导出；这不是项目类型错误，无法用 tsconfig 调整解决。
- 本项目生产构建依赖 `vue-tsc --build --force` 检查 Vue template 和生成声明，不能只让原生 `tsc` 通过就视为迁移成功，也不引入双 TypeScript 编译器造成编辑器与 CI 结论分裂。
- 当前继续锁定已验证的 TypeScript 5.9.3。只有 TypeScript 提供稳定 API 且 Vue Language Tools/vue-tsc 官方声明支持后，才重新建立独立迁移批次。

**复测门槛**:
- `vue-tsc --version` 与 `vue-tsc --build --force` 在 TypeScript 7 环境必须能启动并完成，不得依赖未导出的 `typescript/lib/*` 内部路径。
- 重新核对 Vue template、auto-import/global components 声明、ESLint TypeScript parser、Vite 生产构建与 IDE 语言服务，再决定是否替换 5.9.3。

### 1.1 Vite 8、Rolldown 与 unplugin 迁移

**记录日期**: 2026-07-14
**验证日期**: 2026-07-14（frozen 安装、`pnpm run type-check`、`pnpm run build-only`、Web assets 同步检查均通过）
**验证环境**: Node 22.22.1, pnpm 9.15.9, Vite 8.1.4, @vitejs/plugin-vue 6.0.8, unplugin-auto-import 21.0.0, unplugin-vue-components 32.1.0
**文件**: `modules/web/package.json`, `modules/web/pnpm-lock.yaml`, `modules/web/vite.config.ts`, `modules/web/src/auto-imports.d.ts`, `modules/web/src/components.d.ts`, `app/src/main/assets/web/index.html`

**迁移内容与约束**:
- Vite `5.4.21 -> 8.1.4`、Vue 插件 `5.2.4 -> 6.0.8`、auto-import `0.18.6 -> 21.0.0`、components `0.27.5 -> 32.1.0`；新版 unplugin 为 ESM-only，项目配置原本就是 ESM，无需 CommonJS 兼容层。
- Vite 8 使用 Rolldown/Oxc 构建；生产环境删除 `console`/`debugger` 的配置从 `esbuild.drop` 迁移到 `build.rolldownOptions.output.minify.compress`，避免继续依赖已废弃的构建期 esbuild 选项。
- Vite 7 起 Sass 只支持 modern API，删除已不再接受的 `css.preprocessorOptions.scss.api`；`sass-embedded` 继续作为 SCSS 编译器。
- Vite 新版默认浏览器基线已提高。为避免工具升级隐式缩小 APK WebView/桌面浏览器兼容范围，显式保留 Vite 5 的 `es2020/edge88/firefox78/chrome87/safari14` 构建 target；提高兼容基线必须另批评估。
- unplugin 会重写 auto-import 与全局组件声明，生成文件必须随版本迁移提交，并由 vue-tsc 验证；生产 bundle 变化后必须同步 APK asset。

**回归点**:
- `vite-plugin-singlefile` 必须在 Rolldown 构建中继续内联 JS/CSS，最终只产生可直接加载的 `dist/index.html` 与 favicon。
- 生产产物不得残留项目中的 `console.*`/`debugger` 调试语句；开发模式仍应保留调试行为。
- Web 书架、阅读页、设置页和路由仍需在后续浏览器/真机回归中检查；本批自动验证覆盖类型检查、137 个模块转换、单文件生产构建和 APK assets 字节同步。

### 1.0 Vue TSC 3 与 Vue TSConfig 0.9 迁移

**记录日期**: 2026-07-14
**验证日期**: 2026-07-14（frozen 安装、`pnpm run type-check`、`pnpm run build-only`、Web assets 同步检查均通过）
**验证环境**: Node 22.22.1, pnpm 9.15.9, TypeScript 5.9.3, vue-tsc 3.3.7, @vue/tsconfig 0.9.1
**文件**: `modules/web/package.json`, `modules/web/pnpm-lock.yaml`

**迁移内容与约束**:
- vue-tsc `2.2.12 -> 3.3.7`、`@vue/tsconfig 0.5.1 -> 0.9.1`；Vue TSC 3 的 hybrid mode 默认始终开启，本项目只使用 CLI build 模式，无编辑器扩展配置需要迁移。
- `@vue/tsconfig` 0.9 要求 TypeScript `>=5.8`，因此先把 TypeScript 5.x 从 5.5.4 提升到最新稳定 5.9.3；TypeScript 7 仍保留为后续独立主版本迁移，不能与本批混合判断回归。
- 0.9 基础配置将目标更新为 ESNext/ES2022 语义并启用 `verbatimModuleSyntax`、`moduleDetection=force` 等现代 bundler 配置；项目 `tsconfig.app.json` 已能直接继承，无需覆盖或关闭严格选项。
- Vite 最终输出兼容范围仍由 `vite.config.ts` 的 build target 决定，TypeScript 的检查 target 不代表 APK Web 产物浏览器基线。

**回归点**:
- `vue-tsc --build --force` 必须完整通过，不得新增导入类型、Vue template、全局组件或 auto-import 声明错误。
- Vite 5 生产构建产物应保持字节一致，说明本批只改变开发期类型检查，不改变 Web 运行时代码。
- 后续 Vite/unplugin 新版声明使用较新的 TypeScript 语法，必须在本批之后迁移；TypeScript 7 仍需单独验证新的类型推断和 lib.d.ts 行为。

### 0.9 ESLint 10 与 Vue lint 插件迁移

**记录日期**: 2026-07-14
**验证日期**: 2026-07-14（frozen 安装、ESLint 基线比对、`pnpm run type-check`、`pnpm run build-only`、Web assets 同步检查通过）
**验证环境**: Node 22.22.1, pnpm 9.15.9, ESLint 10.7.0, eslint-plugin-vue 10.9.2
**文件**: `modules/web/package.json`, `modules/web/pnpm-lock.yaml`, `scripts/check-web-assets-sync.sh`

**迁移内容与约束**:
- ESLint `9.39.4 -> 10.7.0`、eslint-plugin-vue `9.33.0 -> 10.9.2`；项目已使用 `eslint.config.mjs` flat config，没有旧 `.eslintrc`、v10 feature flag 或自定义 rule API 需要迁移。
- ESLint 10 不支持 Node 20.19 以下及 Node 21/23；`engines.node` 收紧为 `^20.19.0 || ^22.13.0 || >=24`。`@types/node` 继续保持 20，不随工具运行环境主版本上调。
- 当前 `@vue/eslint-config-typescript` 14.9.0 已声明兼容 ESLint 10 与 eslint-plugin-vue 10，Prettier 配置也兼容，无需为了主版本迁移改写 flat config。
- 升级前后 `pnpm exec eslint .` 都是同一组 40 个既存错误，没有新增规则错误；历史 `any`/未使用声明/单词组件名问题留作独立 lint 清理，不混入工具迁移。
- 纯开发工具升级不会改变生产 bundle；同步钩子以重建的 `dist/index.html` 与 APK asset 字节比较为准，产物相同时不制造空洞的 assets 改动。

**回归点**:
- Node 20.19、22.13 或 24+ 环境可加载 flat config；更低 Node 版本应在安装阶段收到 engines 不兼容提示。
- ESLint 必须能完整扫描 TypeScript/Vue 文件且不出现配置、parser 或插件加载错误；既存 lint 错误数量不得因迁移增加。
- 类型检查、生产构建和单文件内联产物保持正常，`dist/index.html` 与 APK asset 一致。

### 0.8 Web 构建依赖锁定与无差异 assets 校验

**记录日期**: 2026-07-14
**验证日期**: 2026-07-14（`pnpm install --frozen-lockfile`、`pnpm run type-check`、`pnpm run build-only`、Web assets 字节同步检查均通过）
**验证环境**: Node 22.22.1, pnpm 9.15.9, lockfileVersion 9.0
**文件**: `.gitignore`, `modules/web/pnpm-lock.yaml`, `scripts/check-web-assets-sync.sh`

**问题与约束**:
- `modules/web/package.json` 使用范围版本，只提交清单会让不同时间安装解析到不同补丁/小版本；必须跟踪模块内 lockfile 并在 CI/本地验证中使用 frozen 安装。
- 根目录仍忽略误生成的 `pnpm-lock.yaml`，`.gitignore` 只用 `!modules/web/pnpm-lock.yaml` 放行 Web 模块快照。
- 依赖或类型工具配置变化后，重建的 `dist/index.html` 可能与已提交 APK asset 完全一致；同步检查在两者字节一致时放行，不要求制造无意义的 assets diff。
- 运行时源码/模板/Vite/浏览器目标配置变化仍必须同步并暂存 `app/src/main/assets/web/`；依赖构建产物字节变化时同样必须同步，不能用不同步的 `dist` 绕过 APK 资源边界。

**回归点**:
- 从干净依赖目录执行 frozen 安装不得改写 lockfile 或报告清单不匹配。
- 类型检查与生产构建必须通过，`dist/index.html`、`dist/favicon.ico` 分别与 APK assets 一致。
- 依赖/类型工具变更在已构建且产物相同时通过同步检查；运行时输入或产物变化而未暂存 assets 时仍应被拦截。

### 0.7 Web 基础依赖补丁/小版本升级

**记录日期**: 2026-07-14
**验证日期**: 2026-07-14（`pnpm run type-check`、`pnpm run build-only`、Web assets 同步检查均通过）
**验证环境**: Node 22.22.1, pnpm 9.15.9, Vue 3.5.39, Vite 5.4.21
**文件**: `modules/web/package.json`, `app/src/main/assets/web/index.html`

**升级内容**:
- Vue 声明版本 `^3.5.12 -> ^3.5.39`，生产构建由 3.5.38 更新到 3.5.39；保持 Vue 3.5 稳定线，不引入框架迁移。
- Prettier 声明版本 `^3.3.3 -> ^3.9.5`，保持 Prettier 3 稳定线，仅影响开发期格式化工具。
- `@vueuse` 14、Pinia 3、Vue Router 5、Vite 8、TypeScript 7、ESLint 10、Vue TSC 3 及 unplugin 新主版本继续拆分为独立迁移，不与补丁批次混合。

**回归点**:
- Web 阅读页、书架、替换规则和设置页必须通过 Vue 类型检查与生产构建。
- 构建后的 `dist/index.html` 必须同步到 `app/src/main/assets/web/index.html`，并通过 `scripts/check-web-assets-sync.sh`。
- 后续主版本迁移按“构建工具 -> 类型工具 -> 运行时状态/路由库”分批处理，每批重新检查单文件打包与 APK assets。

### 0.6 Android 补丁依赖升级与废弃 API 收敛

**记录日期**: 2026-07-14
**验证日期**: 2026-07-14（Debug Kotlin、73 项 JVM 单测、Release/R8 与 5 个 ABI split APK 输出均通过）
**验证环境**: AGP 9.2.1, Kotlin 2.4.0, Gradle 9.6.1, compileSdk/targetSdk=37, minSdk=26
**文件**: `gradle/libs.versions.toml`, `WelcomeConfigFragment.kt`, `ViewExtensions.kt`

**升级与兼容内容**:
- KSP `2.3.8 -> 2.3.10`、Glide `5.0.7 -> 5.0.9`、简繁转换库 `0.2.16 -> 0.2.17`；均为当前稳定线内的补丁升级。
- 欢迎图片在 API 30+ 明确使用 `WEBP_LOSSY`，API 26-29 保留局部抑制的旧 `WEBP` 分支，并检查压缩返回值，避免编码失败后静默保存空文件。
- 软键盘显示统一使用 `WindowCompat.getInsetsController(...).show(Type.ime())`，无 Activity Window 时才以 flags=0 回退 `InputMethodManager`，不再使用已废弃的 `SHOW_IMPLICIT`/`SHOW_FORCED`。
- 依赖报告中的 AppCompat/Fragment/Media3/WebKit/AGP 后续版本均为 alpha/beta，Protobuf 为 RC；Hutool 继续按项目兼容约束锁定，本轮不混入预发布或高风险迁移。

**回归点**:
- Glide KSP 生成代码、Room KSP、Debug/Release 混淆和五种 APK split 必须正常。
- API 26-29 与 API 30+ 分别选择欢迎图片，生成的 WebP 文件必须可读，失败时应提示而不是写入空文件。
- 普通输入框、弹窗输入框和阅读页配置输入框请求焦点后软键盘应正常显示，返回/切后台后不应残留强制键盘。

### 0.5 Kotlin 2.4 与 Gradle 9.6 构建迁移

**记录日期**: 2026-07-14
**验证日期**: 2026-07-14（Debug Kotlin、73 项单测、Release/R8 与 5 个 ABI split APK 输出均通过）
**验证环境**: AGP 9.2.1, Kotlin 2.4.0, Gradle 9.6.1, compileSdk/targetSdk=37, minSdk=26
**文件**: `gradle/libs.versions.toml`, `gradle/wrapper/gradle-wrapper.properties`, 根目录及三个 Android 模块的 `build.gradle`

**迁移内容**:
- Kotlin `2.3.21 -> 2.4.0`，Gradle wrapper `9.4.1 -> 9.6.1`；AGP 暂留当前稳定版 9.2.1。
- 删除从未应用到 Android 模块的 `kotlin-android` 插件别名，继续使用 AGP 9 内置 Kotlin，仅保留 Parcelize 插件。
- 删除根项目 `ext.compile_sdk_version`；compileSdk 放入 Version Catalog，三个模块显式读取，避免 Gradle 9.6 对父项目隐式属性查找的弃用路径。
- 模块依赖从 `project(path: ':modules:...')` 改为 DependencyHandler 的 `project(':modules:...')`，消除 Gradle 10 将移除的 Project 对象依赖记法。
- Kotlin 2.4 会报告用裸单例表达式触发初始化的旧写法；`RhinoScriptEngine` 新增显式 `ensureInitialized()`，保留全局 `ContextFactory` 初始化副作用且消除歧义。
- `--warning-mode all` 仍报告一条 Project 对象依赖记法弃用；带追踪堆栈定位在 AGP 9.2.1 内部 `VariantDependenciesBuilder` 创建测试组件，项目脚本已使用 `DependencyHandler.project(String)`，需等待 AGP 上游修正，当前不影响 Debug/Release。

**回归点**:
- 检查 Kotlin/Java 混编、Parcelize、KSP/Room 生成代码和 Rhino 模块编译。
- Release/R8 必须通过，并核对 APK split 输出未因 Gradle 升级恢复同名冲突。
- Gradle 配置阶段不应出现父项目隐式属性查找或重复应用 Kotlin Android 插件警告。

### 0.4 Android 基础依赖小批次升级

**记录日期**: 2026-07-14
**验证日期**: 2026-07-14（Debug Kotlin、73 项单测、Release/R8 均通过）
**验证环境**: AGP 9.2.1, Gradle 9.4.1, Kotlin 2.3.21, compileSdk/targetSdk=37, minSdk=26
**文件**: `gradle/libs.versions.toml`

**升级范围**:
- KSP `2.3.7 -> 2.3.8`
- AndroidX Core `1.18.0 -> 1.19.0`
- Lifecycle `2.10.0 -> 2.11.0`
- OkHttp `5.3.2 -> 5.4.0`
- Protobuf Javalite `4.35.0 -> 4.35.1`

**回归点**:
- KSP/Room 必须同时覆盖 Debug 与 Release/R8，确认生成 DAO、数据库实现和混淆边界正常。
- OkHttp 升级后验证普通书源、图片请求、WebDAV、Glide OkHttp 集成及 Cronet 关闭时的网络路径。
- Lifecycle/Core 升级后验证 Activity/Fragment 生命周期回调、Flow 收集、返回键和 Activity Result API。
- Protobuf Javalite 升级后确认 Cronet 网络请求初始化及 Release 打包无重复类或裁剪异常。

### 0.3 链式协程回调竞态与系统栏兼容 API 迁移

**记录日期**: 2026-07-14
**验证日期**: 2026-07-14（Debug Kotlin 编译、完整单测、Release/R8 通过；Lint 的两个 NewApi 错误已消除，仅剩既有 49 个 MissingTranslation 错误）
**验证环境**: Gradle 9.4.1 + AGP 9.2.1, compileSdk/targetSdk=37, minSdk=26
**文件**: `help/coroutine/Coroutine.kt`, `utils/ActivityExtensions.kt`, `ui/book/read/BaseReadBookActivity.kt`, `ui/book/read/config/ClickActionConfigDialog.kt`, `base/BaseReadActivity.kt`, `res/values/styles.xml`, `gradle/libs.versions.toml`

**问题与修复**:
- 链式协程原先在构造时立即启动，快速任务可能早于 `.onSuccess/.onError` 注册完成；现在内部任务统一 LAZY 创建，非 LAZY 调用在当前同步配置链结束后启动，并增加单测覆盖快速成功回调。
- `CancellationException` 原先会进入普通错误处理且执行 `onFinally`，违背结构化并发语义；现在取消异常直接重抛，取消只进入 `onCancel`，信号量仍在内部 finally 可靠释放。
- 默认 `MainScope()` 改为带 `SupervisorJob` 的显式进程 scope，并明确 UI 代码仍应传生命周期 scope，避免把进程任务与界面任务混为一谈。
- API 30+ 状态栏/导航栏显隐及图标明暗迁移到 `WindowCompat`/`WindowInsetsControllerCompat`；API 26-29 保留局部抑制的旧 flags，阅读页原有分别隐藏和滑动临时显示语义保留。
- 移除主题中错误放置且触发 NewApi Lint 的 `enableOnBackInvokedCallback`；项目继续使用 `OnBackPressedDispatcher`。`activity-ktx` 空壳依赖改为 `activity`。
- Android 16+ 且 `smallestScreenWidthDp >= 600` 时不再尝试强制阅读页方向，避免依赖系统已经忽略的方向锁定。

**回归点**:
- 快速返回的链式协程必须执行成功回调；取消不能进入错误或普通 finally 回调。
- API 26、29、30、35+ 分别检查普通页面状态栏底色/图标，阅读页菜单显示与隐藏、导航栏和状态栏独立开关、配置弹窗及多窗口。
- 手机阅读方向设置保持原行为；Android 16+ 大屏保持自适应，不应因强制方向触发尺寸重建或布局错位。
- `activity` 依赖调整后 `viewModels`、`OnBackPressedDispatcher`、Activity Result API 必须正常编译运行。

### 0.2 RecyclerView 异步刷新点击 NPE 与超长数字输入 NumberFormatException

**记录日期**: 2026-07-14
**验证日期**: 2026-07-14（`./gradlew :app:compileAppDebugKotlin --no-daemon` 通过）
**验证环境**: Gradle 9.4.1 + AGP 9.2.1, compileSdk/targetSdk=37, minSdk=26
**文件**: `ui/book/import/remote/ServersDialog.kt`, `ui/book/read/config/SpeakEngineDialog.kt`, `ui/config/CheckSourceConfig.kt`, `ui/replace/edit/ReplaceEditActivity.kt`, `ui/book/read/BaseReadBookActivity.kt`, `lib/cronet/CronetCoroutineInterceptor.kt`(已删除)

**证据与根因**:
- 列表数据由 Room Flow/协程异步刷新，刷新瞬间 `holder.layoutPosition` 可能变成 `RecyclerView.NO_POSITION`，`getItemByLayoutPosition()` 返回 null，原代码用 `!!` 取 `id` 直接触发主线程 NPE。
- 数字输入框只限制了输入类型为数字，未限制长度，输入超过 `Long`/`Int` 范围后 `toLong()`/`toInt()` 抛 `NumberFormatException` 导致崩溃。
- 模拟阅读弹窗起始日期来自 `book.getStartDate()`，未开始模拟阅读时为 null，`startDate` 文本为空，点击日期选择器调用 `LocalDate.parse(startDate.text)` 会抛 `DateTimeParseException`（okButton 处已兜底，点击处漏兜底）。
- `CronetCoroutineInterceptor` 为未接入死代码（实际使用的是 `CronetInterceptor`，见 `help/http/Cronet.kt`），其回调 `waitForDone` 仍含 `TODO("Not yet implemented")`，一旦误用会抛 `NotImplementedError`。

**修复**:
- 列表回调改用更及时的 `holder.bindingAdapterPosition`，并用 `getItemByLayoutPosition(...)?.let { }` 安全取值，避免 `!!`。
- 数字解析统一改用 `toLongOrNull()`/`toIntOrNull()`，`CheckSourceConfig` 校验落空一并提示“不能为空”；`ReplaceEditActivity`、`BaseReadBookActivity.intOr` 解析失败回退默认值。
- 模拟阅读起始日期点击处与 okButton 统一：文本为空时回退 `LocalDate.now()`，并统一用 `dateFormatter` 解析。
- 删除 `CronetCoroutineInterceptor.kt` 死代码，消除 `TODO()` 崩溃点。

**回归点**:
- 服务器配置单选/编辑/删除、朗读引擎选择/编辑/删除在快速增删与 Flow 刷新过程中点击不应崩溃。
- 书源校验超时输入超长值应提示而非崩溃；替换规则超时、下载/模拟阅读章节序号输入超长值应回退默认值。
- Cronet 路径行为应与旧 `CronetInterceptor` 一致，无新增异常。

### 0.1 开启日志后首次启动可能停在灰色预览窗口

**记录日期**: 2026-07-12
**验证日期**: 2026-07-12（33 项日志测试、`scripts/check-debug.sh`、`:app:assembleRelease` 通过；修正后真机首次冷启动待复测）
**验证环境**: vivo V2458A, Android 16；Release 3.26.071202 首次进程日志与第二次正常启动 logcat 对比
**文件**: `App.kt`, `LogUtils.kt`

**证据与根因**:
- 第一次启动的 appLog 只写到“日志模块初始化完成”，没有 `App onCreate`、设备信息或 Activity 生命周期；约 10 秒后第二次进程正常进入。
- 下一条同步操作是后台调用 `WebSettings.getDefaultUserAgent()`，它会冷启动系统 WebView，并与正在创建 Activity 的主线程争用 WebView 全局初始化；首次安装/更新后风险最高。
- `LogUtils.init()` 新增了一次 `logDeviceInfo()`，而 App 初始化流程随后又调用一次，形成重复 WebView 初始化请求。

**修复**:
- `LogUtils.init()` 不再自行重复记录设备信息；由 App/手动开启日志的调用方在初始化完成后记录一次。
- 启动设备信息改记无副作用的系统 HTTP User-Agent，不再调用 `WebSettings.getDefaultUserAgent()` 强制初始化 WebView。
- 日志开关关闭时 App 启动不再构建设备信息字符串。

**回归点**:
- 开启“记录日志”后首次安装、覆盖安装及强制停止冷启动都应立即显示启动图并进入主界面。
- appLog 应各只有一份初始化完成和设备信息，不再出现启动阶段 WebView 加载日志。
- Web 页面、书源 WebView 和崩溃报告按自身路径获取 WebView 信息，不受本项修改影响。

*Last updated: 2026-07-15*
