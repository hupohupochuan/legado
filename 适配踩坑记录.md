# Android API 26-37 适配踩坑记录

> 最新修订: 2026-08-21 CST
> 项目: 阅读 (legado) Android 电子书阅读器
> 最新问题记录: 2026-08-21；文档复核: 2026-08-21
> 当前 SDK 边界: compileSdk=37；`app` minSdk=26/targetSdk=37，`modules/book` minSdk=21/targetSdk=36，`modules/rhino` minSdk=26/targetSdk=36

---

## 文档维护规则

- 本文件最多保留 15 条适配/构建/运行时问题的详细记录。
- 当达到 15 条后需要新增下一条时，把当前整个文档按归档日期改名并移入 `文档归档/`，然后新建 `适配踩坑记录.md` 重新开始记录。不再来回挪动单条条目。
- 主文件保留长期有效规则和归档索引，避免后续 AI 读取时被过期细节干扰。
- 时间敏感结论必须写明记录日期、验证日期、适用 SDK/依赖/系统版本；超过 30 天、target/compile SDK 改变、核心依赖升级、系统规则变化时重新验证。
- 当前主文件条目从 `0.1` 起按新增时间递增，编号越大越新；当前 `0.14` 为最新记录。

## 归档索引

- `文档归档/适配踩坑记录-2026-05_06.md`: 早期 Gradle 下载/代理问题、Release 构建附录、前台服务启动入口统一、提交前拦截本地文件、本地 Debug 编译验证脚本等；2026-06-29 前归档。
- `文档归档/适配踩坑记录-2026-07.md`: 2026-06 至 2026-07-12 的 Web 进度同步、导入弹窗、验证码、文件关联、TTS、WebDAV、阅读器越界、日志模块重构等记录；2026-07-12 归档。
- `文档归档/适配踩坑记录-2026-07-15.md`: 2026-07-12 至 2026-07-15 的日志启动、异步 UI、协程和系统栏、依赖升级、Kotlin/Gradle/AGP/Web 构建迁移等 15 条记录；2026-07-15 归档。
- 当前主文件自 2026-07-15 归档后延续，最新记录为 2026-08-21。

## 长期有效规则

- Gradle/AGP: AGP 9 使用 `androidComponents`，split APK 取 ABI 时比较 `it.filterType.name() == "ABI"`；不要恢复旧 `applicationVariants`。
- Release/R8 反射: `-keepattributes Signature` 在 R8 full mode 下不能单独保证业务 DTO 的泛型签名存在；Gson 反射模型必须显式保留，或改用不依赖反射泛型的 JSON 树编解码。Debug 成功不能替代 Release DEX 与真机验证。
- UI 主题: `Base.AppTheme` 继承 `Theme.AppCompat.DayNight.NoActionBar`；普通按钮使用 AppCompat/项目控件，不得直接引入会强制校验 `Theme.MaterialComponents` 的 `MaterialButton`，否则资源编译可通过但真机 inflate 会崩溃。现有 `TabLayout`/`FloatingActionButton` 不按本条泛化替换。
- GitHub Actions: `.github/workflows/test.yml` 的发布 job 使用 job 级 `contents: write`；否则 APK 虽能编译并上传 Artifact，创建或更新 beta Release 时仍会收到 403 并中断后续共存版构建。
- 编译验证: Kotlin/代码改动后优先跑 `scripts/check-debug.sh`；生成 Debug APK 跑 `./gradlew :app:assembleDebug --no-daemon`。最终可安装的个人签名 Release 必须在提交且工作树干净后运行 `scripts/build-signed-release.sh`。
- minSdk: `app`/`modules/rhino` 为 26，`modules/book` 为 21。调用高于所在模块 minSdk 的 API 必须按 API level 分支，旧 API 只在旧版本分支局部 `@Suppress("DEPRECATION")`。
- Content Provider: `ReaderProvider` 保持导出以支持外部集成，但读写必须由随 `${applicationId}` 变化的 `signature` 权限保护；不要恢复无权限导出，也不要改用会在不同变体间冲突的固定全局权限名。
- 前台服务: 启动入口用 `ContextCompat.startForegroundService()`；服务内必须用 `ServiceCompat.startForeground(this@Service, id, notification, type)` 并声明 `foregroundServiceType`。销毁时必须用 `ServiceCompat.stopForeground(...STOP_FOREGROUND_REMOVE)` 兜底移除通知，并设置停机门闩（`isStopping`）短路所有网络回调、重启检查和通知刷新；不得依赖系统或厂商自动清理前台通知。
- WebService 熄屏访问: 服务启动和每次 HTTP/WebSocket 入站访问都续期 90 秒 CPU/Wi-Fi 活跃租约；默认关闭永久保活时，以最后一次访问为起点空闲 90 秒自动释放，`webServiceWakeLock=true` 时继续保持到服务停止。定时释放必须与统一停机共同清理，不能用默认强开永久锁或网页心跳代替。
- Android 17 局域网: targetSdk 37 的 WebService 必须在 Manifest 声明并在用户启用时运行时申请精确的 `ACCESS_LOCAL_NETWORK`。所有 UI 启动入口和服务内部重建都要复核；拒绝、取消或撤销后回退关闭。绕过 UI 直接拉起 FGS 而缺权时，先完成最小 foreground promotion 再统一停机，不能在 `onCreate()` 里直接停止。
- WebDAV: 日志不得包含账号、密码、Authorization header、URL query/fragment；进度下载写入前必须校验章节范围和 v2 身份，仅同步进度使用 `RangeOnly`，真正加载本地内容才使用 `ReadableRequired`。HTTP 404 不依赖响应短语/XML 错误体，统一表示远端文件缺失；keyless 唯一本地旧书仅在 v2 缺失、无效、身份错或越界的首次迁移窗口兼容旧书名作者文件，v2 定型后不得回退，参与同步的设备必须一并升级。
- SAF 本地书: 跨设备恢复后的 `content://.../tree/...` 可能失效；不要自动猜路径替换，必须提示用户重新选择书籍目录。
- 本地书身份: 书名/作者/文件名只用于展示和检索；数据库使用 `localFileKey = SHA1(文件 URI + 内容 SHA1)` 区分设备内文件，书架 Diff 用 `bookUrl` 区分条目；WebDAV 另用不含路径的内容 SHA-1 `bookProgressKey`。摘要只用于身份，不承担安全校验。
- Web 前端: 唯一生产入口是 `modules/web/index.html` → `src/main.ts`。修改运行时源码后必须同步 `app/src/main/assets/web/`；依赖/声明清理若重建产物字节一致，不制造无意义的 asset diff。旧 `/uploadBook/index.html` 只做 `/#/uploadBook` 兼容跳转，上传业务失败也可能返回 HTTP 200，必须继续检查 `ReturnData.isSuccess`。
- Web 阅读时长: 浏览器只提交经过时长，手机端限制单次 5 秒至 24 小时并用自身时间确定自然日；任何超过 10 分钟的已观察空闲间隔都必须让当前章节保持作废。
- 日志: 文件写入使用独立单线程，单条记录只写一次；导出前必须 flush 并创建一致性快照，初始化失败必须持久化错误并反馈到导出状态。
- 系统栏: API 30+ 使用 `WindowInsetsControllerCompat`；API 26-29 的 `systemUiVisibility` 只能留在明确版本分支中。
- 多语言资源: 默认资源新增可翻译字符串时，必须同步全部受支持语言目录并保持格式占位符一致；完整 lint 有历史错误时按资源名和错误增量判断本轮回归。

---

## 最近 15 条记录

### 0.14 WebService 熄屏后首次打开长时间无数据

**记录日期**: 2026-08-21
**验证日期**: 2026-08-21（日志时序复核、源码链路检查、全量 173 项 JVM 测试及 `scripts/check-debug.sh` 通过；当前无 ADB 连接设备，熄屏真机回归待验证）
**验证环境**: 用户导出的 2026-08-16 至 2026-08-20 应用日志；AGP 9.3.0、Gradle 9.6.1、Kotlin 2.4.10，app compileSdk/targetSdk=37
**文件**: `WebService.kt`, `HttpServer.kt`, `AssetsWeb.kt`

**现象与证据**:
- 最近一次日志中，手机在 00:33:23 进入 `SCREEN_OFF`，浏览器 00:33:25 已请求根页面，但首个 `/getReadConfig` 到 00:33:49 才出现，根页面到初始化接口间隔约 23.781 秒。
- 随后的 `/getBookshelf` 约 9 ms 即完成，封面请求也立即跟上，没有证据表明 Room 返回空书架或 WebService 已被杀。00:46 同一次熄屏期间再次刷新时，根页面到配置接口仅约 0.389 秒。
- 历史日志还存在同型离群：服务启动并熄屏后，根页面请求已到达，但配置接口约 573.226 秒后才出现；其间没有对应的 WebService 停机记录。已统计样本的根页面到配置接口中位数约 1.160 秒。

**根因判断**:
- 根请求已经进入 NanoHTTPD，后续书架接口又能快速返回，故本次不是前台服务超时、服务进程退出或数据库无数据。
- 首屏 HTML 由 `AssetsWeb` 从 APK asset 以流式响应交给 NanoHTTPD；异常窗口集中在“根请求已到达、浏览器尚未执行首个初始化接口”之间。结合熄屏状态，最符合的是 CPU/Wi-Fi 省电使首次响应传输或浏览器接收偶发停滞。
- 该判断基于应用日志时序，未取得设备内核、电源管理或抓包证据，不能进一步归因到某个 ROM 或 Wi-Fi 驱动。

**修复与边界**:
- WebService `onCreate()` 后立即取得非引用计数的 CPU `PARTIAL_WAKE_LOCK` 与 `WIFI_MODE_FULL_HIGH_PERF` 锁；默认模式下保持 90 秒，覆盖服务刚启动后的首次页面和初始化接口。
- `HttpServer` 每次收到请求都会调用现有 `WebService.serve()`，对应的 `serve` action 把释放时间重置为“最后一次访问后 90 秒”；WebSocket 握手沿用同一入口，也会续期。连续访问不叠加引用计数，只取消并重排一个释放任务。
- 原有 `webServiceWakeLock=true` 仍表示服务全程保活，不安排 90 秒释放；默认值和设置语义不变。统一停机路径取消释放任务，并仅在锁处于持有状态时释放。
- 不修改前台服务类型、`onTaskRemoved()`、Web 前端、请求协议或心跳策略，也不把永久唤醒锁默认改为开启。

**回归点**:
- 永久唤醒锁关闭时，启动 WebService 后立即熄屏，90 秒内首次打开根页面、书架和阅读页应连续返回，不再长时间停在无数据。
- 持续请求间隔小于 90 秒时，每次请求都应把释放时间向后续期；最后一次请求后空闲超过 90 秒，CPU/Wi-Fi 锁应自动释放。
- 空闲超过 90 秒后再次访问，收到请求即重新取得锁并重新计时；若设备在请求到达前已断开 Wi-Fi，本机制不承诺主动恢复网络。
- 永久唤醒锁开启时，超过 90 秒不得自动释放；关闭或重启服务时，无论永久配置如何都不得残留锁或释放任务。
- 仍需在问题手机上用“熄屏后首次打开”和“熄屏空闲超过 90 秒再打开”两条路径真机验收；编译通过不能替代省电策略验证。

### 0.13 同名异书共用 WebDAV 阅读进度

**记录日期**: 2026-08-15；2026-08-16 补充首个 v2 的 404 适配
**验证日期**: 2026-08-16（全量 173 项 JVM 测试、AndroidTest Kotlin 编译、`scripts/check-debug.sh` 和 Debug APK 打包通过；新增覆盖无响应短语的 HTTP 404 缺失文件分类；当前无连接设备，修复包的真实双设备 WebDAV 仍待验收）
**验证环境**: AGP 9.3.0、Gradle 9.6.1、Kotlin 2.4.10、Room 2.8.4，app compileSdk/targetSdk=37
**文件**: `Book.kt`, `BookProgress.kt`, `BookProgressIdentity.kt`, `LocalBookIdentity.kt`, `FileBook.kt`, `AppWebDav.kt`, `WebBookProgressUploadScheduler.kt`, `BookController.kt`, `BookDao.kt`, `DatabaseMigrations.kt`, `MigrationTest.kt`, `WebDav.kt`, `WebDavException.kt`

**问题与根因**:
- Room 84 已允许同名同作者的不同本地文件并存，但 `AppWebDav.getProgressFileName()` 仍固定生成 `<name>_<author>.json`。两册书上传时反复覆盖同一个远端文件，下载、启动全量同步和“仅恢复阅读进度”也把同一份 JSON 应用到每个同名条目。
- Web 阅读的 60 秒上传调度器和互斥协调器同样用 `name + author` 作内存 key；即使只改远端文件名，窗口内后一册仍会覆盖前一册的 pending 进度。
- `localFileKey` 含本机 SAF URI/路径，虽然能区分设备内条目，却不能跨设备稳定匹配，直接复用会让同一本书在两台设备上得到不同云端文件。
- 2026-08-16 两组真机日志显示：新版 `3.26.081505 (17304)` 读取未建立的 v2 文件时服务器返回 HTTP 404，但响应既无 `Not Found` 短语也无 WebDAV XML 异常，底层误包装为“未知错误 code:404”。单书同步因此中止，不再进入“远端无进度则上传本地进度”的首建 v2 分支。
- 同时另一台仍是 `3.26.080917 (17300)`，只会读写旧 `<name>_<author>.json`；新版已持有 `bookProgressKey` 后只读 v2，故两版不能双向同步。新版不回读无身份 legacy，否则会重新引入同名异书串进度。

**修复与兼容约束**:
- Room 升至 85，`books` 新增可空 `bookProgressKey`。新导入本地书在原有单次流式读取中同时生成 `localFileKey` 与 `content-sha1:<原始内容 SHA-1>`，不为进度身份再扫描一次文件；不同内容的同名书因此得到不同 key，同一文件复制到另一设备后 key 不含路径。
- 新进度文件名为 `v2-<bookProgressKey 的 SHA-256>.json`；`BookProgress` JSON 保存 key，但运行时定位用的 `bookUrl` 为 transient，不把本地 SAF URI 上传 WebDAV。读取 v2 文件时要求 JSON key 精确匹配目标。
- 84→85 SQL 迁移不读取 SAF，旧 key 保持 null。可读的 keyless 唯一本地书在首次上传、单书拉取、启动全量同步或“仅恢复”时，从精确 `bookUrl` 惰性计算内容身份；在线书仍只在同名冲突时用 `bookUrl` SHA-256。惰性读取失败时跳过该册。
- keyless 唯一本地书的迁移窗口候选顺序为 v2、旧 `<name>_<author>.json`：只接受精确 key 且范围有效的 v2，v2 一旦有效即为身份权威；仅 v2 缺失、无效、身份错或范围无效时才在该窗口尝试 legacy。首次写 v2 只比较可信远端与精确本地位置，且仅在 v2 写入当前有效位置后持久化 key；已有 key、同名歧义书均不回退 legacy。
- Web `/saveBookProgress` 继续要求并优先使用 `bookUrl` 定位 Room；缺少 `bookUrl` 的旧请求只有 name+author 唯一时才兼容。60 秒上传状态和互斥使用 `bookProgressKey`，尚无 key 时回退唯一 `bookUrl`，不再合并同名书。
- `WebDav.checkResult()` 现在先按 HTTP 404 抛出 `ObjectNotFoundException`，不再依赖服务器的 response message 或 XML body。`AppWebDav` 因此会把缺失 v2 当成正常空远端，由现有比较/上传路径建立首个 v2。

**回归点与边界**:
- 同时导入两本同名同作者但内容不同的 TXT/EPUB，分别阅读并等待/flush 上传，WebDAV 应出现两个 v2 文件；重启 App、Web 阅读和“仅恢复阅读进度”都只更新对应册。
- 设备 A 新导入、设备 B 从 84 升级且各自只有同一内容一册时，B 首次同步后必须读写同一 v2；无有效 v2 时才以 legacy 与 B 的精确本地位置迁移，有效 v2 出现后不得再让无身份 legacy 参与比较。两册同名异书时旧文件不能应用到任一册，必须成功补齐各自 key 后才读写 v2。旧书 SAF 失权时保持本地进度并记录跳过，不弹成 WebDAV 配置失败。
- 内容字节完全相同的两个副本具有同一个跨设备 key，会共享云端进度；设备内仍按路径保留两条。若未来要求相同字节副本也跨设备独立，需要显式且可迁移的用户身份，不能把设备路径塞回云端 key。
- 自动化结果不能替代两台真实设备使用不同 SAF URI 的往返同步；还需检查首次为旧同名大文件计算摘要的耗时，以及 84→85 真机升级不丢书。
- 返回空 body、HTML body 或无 response message 的 404 都必须视为缺失；401/500 继续走原错误处理。双设备验收前必须确认两台均安装含本修复的 v2 版本，不得用一台 pre-v2 客户端与一台 v2 客户端作为同步成功验收。

### 0.12 Android 17 局域网权限阻断 WebService 入站连接

**记录日期**: 2026-08-12
**验证日期**: 2026-08-12（3 项权限边界 JVM 测试、2 项 API 37.1 仪器测试、`scripts/check-debug.sh`、AndroidTest Kotlin 编译和 Debug APK 组装通过；lint 保持历史基线 49 errors/934 warnings/1 hint，本轮权限文案无新增错误；API 37.1 Google APIs Play Store x86_64 模拟器完成拒权、授权、运行中撤销、进程重启兜底、快捷磁贴允许/拒绝、emulator NAT 入站 HTTP/WebSocket 黑盒验证；真实手机与另一台 Wi-Fi 设备互访仍待验收）
**验证环境**: Android 17/API 37.1 AVD，app Debug targetSdk/compileSdk 37，HTTP 1122、WebSocket 1123
**官方依据**: [Local network permission](https://developer.android.com/privacy-and-security/local-network-permission)
**文件**: `AndroidManifest.xml`, `Permissions.kt`, `WebServiceLocalNetworkAccess.kt`, `WebServicePermissionActivity.kt`, `WebService.kt`, `WebTileService.kt`, `OtherConfigFragment.kt`, 多语言 `strings.xml`, `WebServiceLocalNetworkAccessTest.kt`, `WebServiceLocalNetworkPermissionTest.kt`

**问题与根因**:
- Android 17 对 targetSdk 37 应用默认阻断局域网出入站连接；原 Manifest 未声明 `ACCESS_LOCAL_NETWORK`，WebService 仍可在进程内监听 `*:1122`/`*:1123`，但同一局域网设备发起的 TCP 连接会超时。通知、开关和监听 socket 因而会形成“显示运行但网页不可达”的假成功。
- `adb forward tcp:HOST tcp:DEVICE` 和设备内访问 `127.0.0.1`/本机局域网 IP 都能绕过该入站限制，拒权时仍可能返回 HTTP 200，不能用来证明适配成功；模拟器必须使用 emulator console `redir` 让连接经过 NAT 入站路径。
- 项目通用 `PermissionsCompat` 在普通权限拒绝后直接跳应用设置，并依赖进程静态回调；快捷磁贴、配置变化和进程重建下不能构成自洽的关键启动链。
- 服务端只在 UI 前检查仍不够：同 UID 可以绕过入口直接拉起非导出的 FGS，权限也可能在运行期间撤销。若服务在 `onCreate()` 发现缺权后直接 `stopSelf()`，系统会因尚未完成前台提升抛出 `ForegroundServiceDidNotStartInTimeException`。

**修复与约束**:
- Manifest 声明精确权限；`WebServiceLocalNetworkAccess` 仅在 `SDK >= 37 && targetSdk >= 37` 时要求授权，API 26-36 不弹该权限。不得用 `NEARBY_WIFI_DEVICES` 或同组其他授权代替精确检查。
- 新增非导出的透明 `WebServicePermissionActivity`，自己持有 Permission/Settings 的 Activity Result；每次允许或从设置返回都重新检查精确权限。拒绝或取消会把 Web 开关写回关闭，并允许用户选择进入应用设置，不依赖进程全局静态回调。
- 设置开关统一经 `WebService.start()` 门禁；Android 17 快捷磁贴缺权时用 `startActivityAndCollapse(PendingIntent)` 打开授权页；端口变更走保留启用意图的 `restart()`，避免 stop 写回开关后触发监听重入。
- `onCreate()`、`onStartCommand()`、网络变化、定时/章节触发重建、`upWebServer()`、通知刷新和 keep-alive 全部复核权限。缺权走原有 `requestStop()/tearDown()`，清开关、Tile、两个 server 和 ID 105 通知。
- 绕过 UI 的直接 FGS 缺权时先发布瞬时最小前台通知完成系统握手，再立即统一停机并移除；正常设置/磁贴门禁不会创建这个兜底通知。
- `isRun=true` 与 Tile 激活延后到 HTTP、WebSocket 两个 server 都启动成功后的 `restartGuard` 提交段，启动失败不能提前显示运行。

**黑盒结果与回归点**:
- API 37.1 拒权/AppOp ignore 时，直接 FGS 启动无崩溃、无 1122/1123 监听、无 WebService/ID 105 残留；授权后 emulator NAT redir 的 HTTP 返回 200，WebSocket 原始握手返回 101。
- 运行中 `pm revoke` 会终止原进程并立即清除服务、端口和通知；约 11 秒后的系统重启只进入缺权最小前台兜底并立即销毁，15 秒观察内没有端口、通知或循环崩溃。
- Quick Settings Tile 必须进入专用用途说明页，再显示 Android 17 的局域网/附近设备系统权限框；取消和拒绝保持 Tile/设置开关关闭，允许后才监听端口。
- 每次模拟器自动化同时核对 `dumpsys package` 的 runtime `granted=` 与 `cmd appops get --uid ... ACCESS_LOCAL_NETWORK`；`pm grant` 返回码不能单独作为授权证据。
- 仍需用实体 Android 17 手机与另一台同 Wi-Fi 设备访问手机局域网 IP，分别回归根页面、网页传书、Web 阅读和 WebSocket 管理；该实体网络验收不能被 ADB 或模拟器结果替代。

### 0.11 网页传书把手机端业务失败误报为上传成功

**记录日期**: 2026-08-03
**验证日期**: 2026-08-03（Vue 类型检查、ESLint、Prettier、既有阅读时长 6 项测试、Vite 生产构建、桌面/390px Headless Chrome 页面渲染、旧 URL 跳转、HTTP 200 业务失败与重试成功的浏览器级模拟接口回归、APK Web asset 字节同步、`scripts/check-debug.sh`、Debug APK 组装及包内两个 Web 文件反查通过；连接真实手机上传待验证）
**验证环境**: Vue 3.5.39、Vite 8.1.4、Google Chrome Headless、Node 22.22.1、pnpm 9.15.9
**文件**: `UploadBook.vue`, `localBookUpload.ts`, `api.ts`, `bookRouter.ts`, `Welcome.vue`, `BookShelf.vue`, `assets/web/index.html`, `assets/web/uploadBook/index.html`

**问题与根因**:
- `HttpServer.buildResponse()` 对 `ReturnData.isSuccess=true/false` 都构造 HTTP 200；旧 `uploadBook/index.html` 只检查 `XMLHttpRequest.status === 200`，保存目录失效、文件解析异常等业务失败仍显示绿色成功，且不展示 `errorMsg`。
- 当前 Web 已收敛为 Vue Hash Router 单入口，但欢迎页仍跳到独立维护的旧传书 HTML；该页不经过类型检查/生产构建，支持格式也漏掉手机端已有的 CBZ、ZIP、RAR、7Z。
- 书架页另有单文件上传实现，形成两套入口和两套状态反馈，旧直达 URL 与当前 Web 架构持续漂移。

**修复与约束**:
- 新增统一 `/#/uploadBook` 路由；欢迎页和书架都进入该路由。历史 `/uploadBook/index.html` 缩减为兼容跳转，不再保留第二套上传代码。
- 多文件选择/拖放后串行调用 `addLocalBook`，每项展示上传进度；响应先校验 `isSuccess`/`errorMsg` 结构，只有 `isSuccess=true` 才显示已导入，失败保留手机端原因并允许重试。
- 上传继续使用浏览器原生 `XMLHttpRequest` + `FormData`，不手写 `Content-Type`，由浏览器生成 multipart boundary；不得改回会注入 `application/json` 的共享 `ajax`。
- 可选扩展名与手机端 `bookFileRegex`/`archiveFileRegex` 对齐：TXT、EPUB、UMD、PDF、MOBI、AZW3、AZW、CBZ、ZIP、RAR、7Z；压缩包仍由手机端检查是否包含受支持书籍。
- 模拟接口先返回“HTTP 200 + `isSuccess=false`”，页面显示具体失败且没有成功文案；点击重试后返回 `isSuccess=true`，状态才变为已导入，覆盖本次根因而非只验证页面能打开。

**回归点**:
- 使用无效/失权书籍目录上传，网页必须显示手机端错误且可重试，不能因 HTTP 200 画成功；重设目录后重试同一项应成功。
- 连续选择或拖入多本不同格式书籍，确认按队列逐个上传，单项失败不阻塞后续项目；同名重复和空文件在浏览器端明确拒绝。
- 欢迎页、书架入口、`/#/uploadBook` 直达与旧 `/uploadBook/index.html` 四条路径都进入同一 Vue 页面；浏览器前进/后退不能落回旧实现。
- 真实手机验证时检查上传后书架新增条目、文件内容与本地 `localFileKey`，不能用 Headless Chrome 模拟结果替代手机存储/Room 导入验证。

### 0.10 同名同作者的不同本地文件被 REPLACE 覆盖

**记录日期**: 2026-08-03
**验证日期**: 2026-08-03（4 项本地书身份/书架条目定向 JVM 测试、全量 144 项 JVM 测试、AndroidTest Kotlin 编译、`scripts/check-debug.sh` 及 Debug APK 打包通过；Room 83→84 仪器迁移测试已编译，当前无连接设备未执行；Vivo 原复现流程待安装回归）
**验证环境**: AGP 9.3.0、Gradle 9.6.1、Kotlin 2.4.10、Room 2.8.4；原日志 `/home/liu/localsend/logs-2/logcat.txt`
**文件**: `Book.kt`, `BookDao.kt`, `AppDatabase.kt`, `DatabaseMigrations.kt`, `FileBook.kt`, `LocalBookIdentity.kt`, `ImportBook.kt`, `ImportBookActivity.kt`, 两套书架 `BaseBooksAdapter.kt`, `MigrationTest.kt`, `LocalBookIdentityTest.kt`

**问题与根因**:
- 原日志在 02:53 和 02:57 多次进入 `AssociationActivity -> ReadBookActivity`，两个本地文件被解析为相同《国士无双》/骁骑校；该路径不是用户手动编辑书籍信息导致的偶发覆盖。
- `books` 原有 `Index(name, author, unique = true)`，`BookDao.insert()` 又使用 `OnConflictStrategy.REPLACE`。第二个不同 `bookUrl` 只要解析出相同书名与作者，SQLite 就会删除旧行并插入新行。
- 两套书架适配器的 `areItemsTheSame()` 同样使用书名+作者；即使数据库已保留两行，RecyclerView 仍会把它们当成同一项。
- 本地导入页原先按 `originName` 判断已在书架，点击时还会查同文件名记录并改写其 `bookUrl`，不同目录下的同名文件仍可能串书。

**修复与边界**:
- Room 版本升至 84；`migration_83_84` 将书名+作者改为普通索引，增加可空且唯一的 `localFileKey`。SQLite 允许多个 `NULL`，因此旧数据和在线书不会在迁移时冲突。
- `LocalBookIdentity` 先流式计算内容 SHA-1，再对“完整文件 URI + NUL 分隔 + 内容摘要”计算最终 SHA-1，并以 `sha1:` 前缀存储。该摘要是本地去重/身份键，不是防篡改或密码学凭据。
- 导入时先按 `localFileKey`、再按精确 `bookUrl` 查找：只要路径不同就保留新书，即使内容相同也不合并；同一路径下的文件被原地替换时，因磁盘上只有一个物理位置，按 `bookUrl` 回退更新原记录及新摘要。
- 83 及更旧记录在 SQL 迁移中不读 SAF/本地文件，`localFileKey` 保留为空；用户下次重新导入该文件时再补齐。
- 本地文件列表改按精确 URI 判断和打开，不再按同文件名改路径；书架 style1/style2 均以 `bookUrl` 作为 Diff 条目身份。
- 持久化书籍的文件地址变更改走 `BookDao.relocate()`，在一个 Room 事务内写入目标主键并删除旧主键；不再依赖书名+作者唯一冲突帮忙清理旧行。无法在当前 UI 路径安全重算摘要时先清空 `localFileKey`，下次导入再补齐，不保留与旧路径绑定的过期键。
- 在线书的同名换源/书架导入改为显式查找在线书并迁移进度，避免解除全局唯一索引后遗留旧书行，也不会因同名本地书存在而删除本地记录。
- 本轮修复的身份边界是 `books` 书架条目、章节和行内阅读位置；历史上按书名/作者存储的书签、阅读记录汇总和 WebDAV 进度文件未在本轮扩展 schema，不得误宣称已按 `localFileKey` 隔离。

**回归点**:
- 导入同书名同作者、但路径不同的 TXT/EPUB，style1/style2 列表与网格都必须同时显示两本，分别打开后内容、章节和 `books` 行内进度不串书。
- 重复导入同一 URI 且内容未变的文件不新增书；原地覆盖同一 URI 后重新导入，只更新该记录的内容身份。
- 从 83 版升级后旧书保持可打开；重新导入旧文件后补齐 `localFileKey`，再导入另一本同名书不覆盖。
- 内容摘要增加一次顺序读取；用超大 TXT/EPUB 验证导入期间不卡 UI、进度反馈正常，以及无读权限时明确失败。

### 0.9 Release/R8 擦除 TTS 选择泛型导致系统朗读引擎无法选中

**记录日期**: 2026-08-02
**验证日期**: 2026-08-02（5 项定向测试、全量 140 项 JVM 测试、`scripts/check-debug.sh`、预提交 Release/R8 构建及成品 DEX 反查通过；修复包已安装到 Vivo Android 16 真机，并验证 Google、Sherpa、HTTP TTS 与系统默认逐项单选及新版作用域文案）
**验证环境**: Vivo V2458A，Android 16/API 36，原包 `shutiao.reader.release` 3.26.080217 (17294)；AGP 9.3.0、Gson 2.14.0、Release `minifyEnabled=true`
**文件**: `TtsEngineSelection.kt`, `SpeakEngineDialog.kt`, `ReadAloudConfigDialog.kt`, `TTSReadAloudService.kt`, `TtsEngineSelectionTest.kt`

**问题与证据**:
- 真机已安装并启用 vivo AIService、Google 和 Sherpa 三个 Android TTS 服务；系统默认是 Google，说明列表数据和手机引擎安装状态正常。点击 Sherpa 后四个系统单选框全部变为未选中，复现了“除系统默认外无法选择”。
- Debug DEX 保留 `TypeToken<SelectItem<String>>`、`SelectItem<T>` 及字段 `TT;` 签名；Release DEX 只剩 `TypeToken<SelectItem>`，且 `value` 的实际反射类型退化为 `java.io.Serializable`。Gson 按该类型反序列化字符串时抛出 `JsonIOException: Interfaces can't be instantiated`。
- `SpeakEngineDialog.upTts()` 用 `runCatching().getOrNull()` 静默吞掉解析异常，随后用 `null` 与每个 RadioButton tag 比较，因此点击后全部取消勾选。初始“系统默认”只因 `ttsEngine == null` 的快捷分支显示为选中，并不代表 JSON 解析成功。
- `TTSReadAloudService.initEngine()` 使用同一泛型反射路径；即使把某个系统引擎 JSON 保存下来，服务仍取不到包名并回退系统默认。
- HTTP TTS 行刷新还把 `adapter.itemCount`（header + 数据总数）误当作从 `headerCount` 开始的区间长度，通知范围越过真实 HTTP 数据行。
- 同机选择 Google 后，服务日志确认实际构造参数为 `com.google.android.tts`，排除了“保存后仍回退默认引擎”；无声的另一条链路是 Google 中文离线包停在“正在下载…”，引擎回退 `cmn-cn-x-ssa-server` 联网语音后连续超时并返回 `-7`。当时 Google TTS UID 未被 Clash VPN 接管，属于设备侧语音数据/代理范围问题，用户调整后确认解决，不在 App 内伪装成引擎选择修复。

**修复与约束**:
- 新增纯 Kotlin `TtsEngineSelection`，继续读写历史兼容字段 `title`/`value`，但使用 Gson `JsonObject`/`JsonParser` 树 API 显式取字符串，不再依赖 `SelectItem<T : Serializable>` 的反射泛型签名。
- 引擎弹窗、设置摘要和朗读服务统一使用同一编解码入口；系统默认的空包名、已安装引擎包名及历史漂亮格式 JSON 均保持兼容，数字 HTTP TTS ID 不会被误判为系统引擎配置。
- 系统引擎点击时只解析一次选中值；HTTP 行刷新数量改用 `getActualItemCount()`，不再把 header 重复计入区间长度。
- 弹窗底部作用域按钮由含糊的“书 / 全局”改为“仅用于本书 / 设为全局”，中间仍为“取消”；只澄清现有保存语义，不改变书籍覆盖全局配置的优先级。
- 新增编解码与源码契约测试，禁止 TTS 三个运行时入口重新使用 `fromJsonObject<SelectItem...>`，并锁定真实 HTTP 行刷新范围。Release 验证还必须反查成品 DEX 或做真机选择，不能只跑 Debug JVM 测试。

**回归点**:
- 分别选择系统默认、vivo、Google、Sherpa，并用“仅用于本书”和“设为全局”保存；重新打开弹窗后勾选、设置摘要和实际发声引擎必须一致。
- 选择两个 HTTP TTS 规则时只能勾选当前规则，切回任一系统引擎后 HTTP 勾选必须清除；删除或刷新规则列表不得越界或保留旧选中状态。
- 升级前已保存的多行 JSON 系统引擎配置必须继续生效；空值和损坏 JSON 在服务侧安全回退系统默认，不得崩溃。

### 0.8 阅读记录提示卡误用 MaterialButton 导致真机布局崩溃

**记录日期**: 2026-08-02
**验证日期**: 2026-08-02（Vivo Android 16 原包日志已确认根因；定向布局兼容测试和 `scripts/check-debug.sh` 已通过；修复包真机复测待执行）
**验证环境**: Vivo V2458A，Android 16/API 36，`shutiao.reader.release` 3.26.080203 (17290)
**文件**: `view_read_record_header.xml`, `AppThemeLayoutCompatibilityTest.kt`, `.github/workflows/test.yml`

**问题与证据**:
- 关闭阅读记录后打开“我的 → 阅读记录”，RecyclerView 创建 header 时在 `view_read_record_header.xml:244` 实例化 `MaterialButton` 失败，页面通过全局异常提示暴露 `InflateException`。
- 最深层异常明确为 `The style on this component requires your app theme to be Theme.MaterialComponents`；项目 `Base.AppTheme` 实际继承 `Theme.AppCompat.DayNight.NoActionBar`。
- Android 资源编译只校验类和属性可用，不会执行 Material Components 的运行时主题检查，因此之前的 Debug/Release 编译均未发现该问题。

**修复与约束**:
- 初次止血将提示卡按钮换成 `AppCompatButton`；随后为保留开启和关闭两条路径，当前控制卡改为主页面常驻的 `SwitchCompat`，关闭时仅隐藏热力图，右上角不再保留重复入口。两种控件都不要求迁移全局主题。
- 新增 JVM 源码契约测试，扫描 App layout XML 中直接实例化的 `MaterialButton`，并校验阅读记录双向开关常驻主页面且不回流右上角菜单；只要 App 全局主题仍是 AppCompat，错误控件就必须在构建前失败，而不是留到真机 inflate。现有已运行的 `TabLayout`/`FloatingActionButton` 不在本次无证据扩大范围内。
- `Test Build` 在 Release 编译前运行该定向测试；未来若正式迁移全局主题，必须同步调整主题、全部界面回归和该门禁，不能只删除测试绕过。

**回归点**:
- 关闭阅读记录后进入页面，主页面双向开关保持可见、热力图隐藏且无 `InflateException`；开启后热力图出现，再次关闭后热力图重新隐藏。
- 开启阅读记录时直接进入页面，双向开关正确显示开启状态，热力图和统计卡行为保持不变；右上角菜单不再显示重复开关。
- 深色、浅色主题以及 API 26/36 至少各验证一次按钮文字、强调色、点击反馈和页面重建。

### 0.7 GitHub Actions 编译成功后发布 beta 返回 403

**记录日期**: 2026-08-02
**验证日期**: 2026-08-02（GitHub 远端运行记录与注解已核对；工作流语法和本地差异检查通过；修复后的远端 push/Release 创建待验证）
**验证环境**: GitHub Actions `ubuntu-latest`，`Test Build` run `30568503272`，远端 master `8e69d7986ec9ade5889ae37b9f18abd5681aba18`
**文件**: `.github/workflows/test.yml`

**问题与证据**:
- `Build Release (原包名)` 和 Artifact 上传均成功，说明 Gradle、JDK、签名和 APK 产出不是本次失败点。
- `Publish Pre-Release (原包名)` 调用 Release API 时返回 `403 Resource not accessible by integration`；原工作流未声明 `permissions`，仓库默认只读的 `GITHUB_TOKEN` 无权创建或更新 Release。
- 发布步骤失败后，共存版构建及其上传步骤全部跳过，最终表现为工作流标红且自动发布不完整。
- 同一批 Web 改动会先触发 `Build Web`，其完成后再以 `workflow_run` 触发 `Test Build`；并发策略取消较早的 push run 属于现有替换机制，不是 Gradle 构建失败。

**修复与约束**:
- 只给 `Test Build` 的 `build` job 增加 `contents: write`，覆盖删除旧 beta Release、创建预发布以及向同一 Release 追加共存版 APK；不扩大 `prepare` 等其它 job 的权限。
- Pull Request 仍由现有 `github.event_name != 'pull_request'` 条件跳过发布步骤；来自 fork 的 PR token 也会由 GitHub 降权，不把写权限用于 PR 发布。
- 不修改仓库 Secret、签名材料、Gradle 任务、Release 标签名或 Web 构建触发链。

**回归点**:
- 推送到远端 `master` 后，确认 `Test Build` 的原包名编译、Artifact 上传、beta Release 创建以及共存版编译均依次成功。
- 同时含 Web 与 Android 改动时，允许早先的 push run 被后续 `workflow_run` 替换，但最终运行必须成功且关联相同远端提交。
- 若仍出现 403，核对仓库 Actions 的 Workflow permissions 是否允许工作流申请写权限，以及组织策略是否覆盖仓库设置。

### 0.6 阅读记录跨天落盘导致热力图起始日缺失

**记录日期**: 2026-08-01
**验证日期**: 2026-08-01（`scripts/check-debug.sh` 编译通过；跨天阅读记录、新旧数据库迁移、真机回归待执行）
**验证环境**: AGP 9.3.0, Gradle 9.6.1, app Debug, compileSdk/targetSdk=37, minSdk=26
**文件**: `app/src/main/java/io/legado/app/model/ReadTimeRecorder.kt`

**问题与证据**:
- 旧 `flushSession()` 用 `ReadRecord.dayKey()` 读取当前系统日期作为整条 session 的 `day`，当日开始、次日结束的阅读时长全部落在结束日。
- 阅读记录页 `refreshHeatmap()` 按 `day` 汇总，开始日没有对应记录，显示为 0 阅读量；结束日可能显示超过当日 24 小时的可疑时长。
- 主键 `(bookName, day, startSec)` 把 session 开始秒作为主键之一，跨天后同一天同一本书可能出现多个不重叠时间段，旧写法只保留一条。

**修复与约束**:
- `flushSession()` 改为调用 `flushSessions()`，把 `[startSec, endSec)` 按本地自然日边界拆成多条记录写入。
- 每条记录独立使用 `ReadRecord.dayKey(curStart)` 计算归属日期，`curEnd` 取 `min(endSec, nextDayStart)`，保证次日 00:00 之前和之后的时长分别归属到正确日期。
- 不足 5 秒的小段丢弃，与旧规则一致；新写入或 `REPLACE` 冲突仍覆盖旧记录。
- 数据库迁移 `migration_82_83` 已把旧 `readTime` 累计时长按天拆分，修复逻辑与迁移口径一致；Room 数据库版本保持 83，无需新增迁移。

**回归点**:
- 连续阅读跨越本地 00:00，两日后的热力图分别显示对应分钟数，且都不超过 24 小时。
- 非跨天正常阅读记录保持原有行为，单条记录写入。
- 从旧版本升级且已有 `readTime` 累计数据的设备，升级后热力图不丢失、不重复。

### 0.5 WebService 关闭后前台通知残留

**记录日期**: 2026-07-31
**验证日期**: 2026-07-31（`scripts/check-debug.sh`、Kotlin 编译通过；Vivo Android 16 真机快速开关复测待执行）
**验证环境**: AGP 9.3.0, Gradle 9.6.1, app Debug, compileSdk/targetSdk=37, minSdk=26
**文件**: `app/src/main/java/io/legado/app/service/WebService.kt`
**日志证据**: `/home/liu/localsend/logs/logs/appLog-26-07-31_10-24-33.736.txt:84`（11:58:44.961 onCreate → 11:58:45.840 onDestroy，导出窗口内无再次启动，通知却残留）

**问题与证据**:
- `onDestroy()` 只复位 `isRun=false`，未调用 `ServiceCompat.stopForeground(...STOP_FOREGROUND_REMOVE)` 或 `notificationManager.cancel(NotificationId.WebService)`。
- `isRun` 设置较晚，网络回调 `onNetworkChanged`、后台 30 分钟重启检查器、`HttpServer`/`WebSocketServer` 的 `serve()` 回调在停止窗口内仍可 `notificationManager.notify()` 刷新通知。
- 通知刷新若晚于服务销毁到达，会留下脱离服务生命周期的孤儿通知（ID 105）。
- Android 官方明确提供 `STOP_FOREGROUND_REMOVE` 立即移除关联通知，不应依赖系统或厂商自动清理（参考 `ServiceCompat` 与停止前台服务文档）。

**修复与约束**:
- 新增 `@Volatile isStopping` 停机门闩。`tearDown()` 与 `upWebServer()` 的"提交成功"段（`isRun=true`、`startTimeMs`、`postEvent`、`startForegroundNotification`）共用同一 `restartGuard` 临界区，保证 isStopping/isRun 的写入与 isStopping 检查互斥，消除"后台线程在 tearDown 之后再把 isRun 写回 true 并发布有效地址事件"的窗口。
- 提交段在临界区内发现 `isStopping=true`（tearDown 已抢先）时，立即停掉刚启动的 `httpServer`/`webSocketServer` 并返回，避免"界面显示开启但端口已关闭"和端口占用。
- `tearDown()` 状态写入（`isStopping=true`/`isRun=false`/`isRestarting=false`）在临界区内；锁外执行网络注销、server 关闭、锁释放等可能阻塞或抛异常的清理，避免长时间持锁。
- `tearDown()` 清理步骤显式 `try { ... } catch (e: Exception)` 捕获并通过 `LogUtils.e` 记录，非 `runCatching`（后者捕获 Throwable 会吞掉 OOM、`ThreadDeath` 等致命错误）；`stopForeground` + `notificationManager.cancel(NotificationId.WebService)` 放入 `finally`，保证即使其它清理步骤抛异常，ID 105 通知也一定被移除。`stopServerSafely` 封装 `isAlive` 检查与 `stop()` 异常记录。
- 新增 `requestStop()` = `tearDown()` + `stopSelf()`；从 `upWebServer` 的 IOException/无网络分支调用 `requestStop` 进入 `tearDown` 时会再次 `synchronized(restartGuard)`，同线程对 JVM monitor 可重入，不会自死锁。`onStartCommand` 的 `IntentAction.stop`、`upWebServer` 的 IOException / 无网络分支、`startForegroundNotification` 的普通通知异常、`stopForForegroundRestriction` 全部改走 `requestStop()`，不再直接 `stopSelf()`。
- `onDestroy()` 改为先 `tearDown()` 再 `super.onDestroy()`。
- 网络回调、`tryRestartOnChapterLoaded`、`startForegroundNotification`、`upWebServer`（入口与提交段）、`onStartCommand` 的 `serve`/默认分支全部前置 `isStopping` 短路。
- companion 的 `isRun`/`hostAddress` 加 `@Volatile`，因为 `serve()` 会从 HTTP/WebSocket 线程读取 `isRun`。
- companion `stop()` 调用 `notificationManager.cancel(NotificationId.WebService)` 兜底清除"服务已不在但 105 通知残留"的历史孤儿通知。
- 只清理 `NotificationId.WebService`（105），不用 `cancelAll()`，避免影响缓存、下载、朗读通知；不改 WakeLock 默认值、3 小时重启逻辑、Web 前端。
- 保留"首次 `startForeground`、后续 `notify`"设计，未恢复反复 `startForeground`，避免重新引入 Android 16 `dataSync` 配额崩溃。

**回归点**:
- Vivo Android 16 真机快速开关 Web 服务（通知栏取消、设置开关、快捷磁贴）多次，确认 ID 105 通知立即消失且无残留。
- 关闭瞬间断网/联网切换，确认网络回调不刷新通知。
- 运行满 30 分钟触发后台重启检查时关闭服务，确认无新 server 残留、无通知复发。
- 启动失败（前台启动被拒）和 Android 16 `dataSync` 超时路径，确认走 `requestStop()` 且通知移除。

### 0.4 文件关联导入同文件自覆盖截断与空文件误判

**记录日期**: 2026-07-30
**验证日期**: 2026-07-30（`scripts/check-debug.sh`、Debug APK、10 项 `InputStreamExtensionsTest`、AndroidTest Kotlin 编译通过；`lintAppDebug` 仍为 49 项历史 `MissingTranslation` 且本轮资源无新增错误；因无设备，`LocalBookFileImportHelperTest` 与 Vivo Android 16 原复现步骤仍待真机执行）
**验证环境**: AGP 9.3.0, Gradle 9.6.1, app Debug, compileSdk/targetSdk=37, minSdk=26
**文件**: `app/src/main/java/io/legado/app/utils/LocalBookFileImportHelper.kt`, `app/src/main/java/io/legado/app/ui/association/FileAssociationDialog.kt`, `app/src/main/java/io/legado/app/model/fileBook/FileBook.kt`, `app/src/main/java/io/legado/app/utils/UriExtensions.kt`, `app/src/main/java/io/legado/app/utils/InputStreamExtensions.kt`, `app/src/main/java/io/legado/app/ui/association/FileAssociationViewModel.kt`, `app/src/main/res/layout/dialog_progressbar_view.xml`

**问题与证据**:
- 文件管理器通过 `content://media/external/file/xxx` 分发已位于 App 授权目录的书籍时，`FileAssociationDialog.isSameDocument` 仅比较 authority + documentId，直接判定为不同文件。
- 后续以 `"wt"` 打开授权目录中的同名目标并复制源流；`t` 标志会在打开时截断目标到 0 字节，而源、目标实为同一物理文件，导致源输入流也被清空，最终导入报 `EmptyFileException`。
- `FileBook.importLocalFile` 把 `DocumentFile.length() == 0` 直接视为空文件，但 DocumentFile 返回 0 也可能表示长度未知；`hasReadableContent()` 使用 `runCatching().getOrDefault(false)` 会把权限拒绝、Provider 异常和真正 EOF 都变成 `EmptyFileException`。
- `InputStream.isJson()` 使用 `available() - 128` 做跳转，`available()` 只是估计值，负数 skip 产生 `Invalid argument` 错误；后续改为顺序读取后，尾部缓冲算法在 `total > maxLen` 时存在确定性越界，且完整扫描大型书籍。
- `UriExtensions.readUri` 捕获业务回调异常后吞掉，外层 `runCatching` 既收不到成功也收不到失败，进度对话框永不结束；`dialog_progressbar_view.xml` 的“取消”没有绑定点击监听。
- 初版安全复制中备份失败仍继续覆盖、回滚异常被吞、写后未重新校验、新目标失败后遗留空文件；取消操作只在“源→临时文件”阶段生效，备份/写入/回滚使用阻塞式 `copyTo()`。

**修复与约束**:
- `LocalBookFileImportHelper` 在全局互斥事务内查找或创建 SAF 目标，调用端不再提前丢失“本轮新建”状态；临时文件使用 `File.createTempFile()`，已有目标的备份与回滚结果都重新校验大小/SHA-256，失败保留备份路径，新目标失败时删除。
- 所有读写阶段在读前、写前和破坏性打开前检查取消；取消后的回滚与清理放入 `NonCancellable`，回滚校验完成前不关闭文件关联界面或继续导入书籍。进度回调按阶段和固定字节间隔节流，避免大文件每 8 KiB 向主线程投递消息。
- `isSameFile` 契约明确：URI 相同但底层文件不存在或不是普通文件时返回 false；`fileIdentity` 校验 `st_mode` 为普通文件且 `st_ino != 0`。
- `FileBook.importLocalFile` 在 `size == 0` 时调用 `hasReadableContent()`，只有成功读取到 EOF 才判定为空；权限和 I/O 异常原样向上传播。
- `InputStream.isJson()` 仅使用范围为 `0..128` 的有效尾部长度移动数据，并补充末块 1 字节及反复小块读取测试；文件关联优先分流二进制书籍和大于 1 MiB 的 TXT，小型 TXT 继续兼容历史 JSON 内容识别。
- `UriExtensions.readUri` 把 `error` 改为必传回调并更新全部调用点；content、文件权限和业务回调异常都进入同一错误终态，不再从异步权限回调直接抛到主线程。
- `FileAssociationDialog` 进度条区分暂存、备份、写入、校验、回滚阶段；大小未知时保持不确定进度；百分比限制在 0..100；取消按钮绑定协程取消和对话框关闭。

**回归点**:
- 必须通过两种不同 authority 指向同一底层文件的仪器测试，断言导入前后 SHA-256、大小完全不变；当前测试仅覆盖 `file://` + `FileProvider`，真实跨 authority 场景需补充。
- 覆盖真实空文件、大小未知但内容可读、同名不同内容、写入完成点取消/失败回滚、SAF 新目标取消删除、并发事务、临时文件清理及权限失效场景。
- 在 Android 16 真机按原步骤复测 TXT/EPUB/PDF/CBZ；同时补 API 27（Android 8.1）兼容测试。
- 任何修改本地书导入流程的后续改动都必须沿用“先临时文件、再校验、后替换”的安全复制路径，禁止恢复直接 `openOutputStream(..., "wt")` 后复制源流的写法。

### 0.3 新增缓存导出字符串未同步全部语言导致 lint 错误增加

**记录日期**: 2026-07-29
**验证日期**: 2026-07-29（`lintAppDebug` 从 62 项错误降回父提交已知的 49 项历史错误，13 个本功能资源名不再出现 `MissingTranslation`；Debug Kotlin 编译及 `scripts/check-debug.sh` 通过）
**验证环境**: AGP 9.3.0, Gradle 9.6.1, app Debug, compileSdk/targetSdk=37
**文件**: `values/strings.xml`, `values-vi/strings.xml`, `values-pt-rBR/strings.xml`, `values-ja-rJP/strings.xml`, `values-es-rES/strings.xml`

**问题与证据**:
- 缓存进度和安全导出新增 13 个默认语言字符串时只同步了三个中文目录，没有同步 vi/pt/ja/es；Android lint 按缺失的资源名报错，因此错误总数由父提交的 49 项增加到 62 项。
- `lintAppDebug` 当前仍因 49 项历史 `MissingTranslation` 返回失败，不能把整个任务的失败直接归因于本轮，也不能因为存在历史错误而忽略新增资源名。

**修复与约束**:
- 为 vi、pt-rBR、ja-rJP、es-rES 补齐同名字符串，保留 `%1$d` 至 `%6$d` 等位置占位符；资源编译通过后逐项确认 13 个资源名不再出现在 `MissingTranslation` 报告中。
- 面向用户的缓存/导出文案不能标记为 `translatable=false` 或用 lint 忽略规避；新增默认字符串时同步检查三个中文目录和四个其他语言目录。
- 完整 lint 继续以错误清单和父提交基线作增量核对；本轮不顺带修改剩余 49 项历史翻译问题。

**回归点**:
- 修改任一带参数文案后，所有语言必须保留相同数量、类型和编号的格式占位符；先跑资源编译，再检查 lint 是否出现 `StringFormatMatches` 或 `MissingTranslation`。
- 支持语言目录发生增删时，重新核对默认资源的可翻译集合，不能继续硬编码当前目录清单而不验证实际 `res/values-*`。

### 0.2 阅读页点击阈值死区在 Android 8.1 老平板表现为点击失效

**记录日期**: 2026-07-16
**验证日期**: 2026-07-16（定向 JVM 测试、Debug Kotlin 编译及 `scripts/check-debug.sh` 通过；MI PAD 4 需安装新包复测）
**验证环境**: Xiaomi MI PAD 4 日志为 Android 8.1/API 27；app minSdk=26、compileSdk/targetSdk=37
**文件**: `ReadView.kt`, `ReadTouchDecider.kt`, `MoreConfigDialog.kt`, `ReadTouchDeciderTest.kt`

**问题与证据**:
- 设备日志没有崩溃或 ANR，但复现时多次经过锁屏、恢复和阅读页重建；旧日志未记录 `MotionEvent` 坐标，不能仅凭日志判断具体一次点击被哪一层消费。
- `ReadView` 原先用普通 `scaledTouchSlop` 标记“已移动”，抬手又用该标记否决单击；翻页委托却按更大的自定义 `pageTouchSlop` 判断拖动，两个阈值之间会形成既不点击也不翻页的死区。
- 翻页动画被按下动作中止后，旧逻辑按物理中心无条件吞掉动作；即使中心配置为菜单，也不会进入 `showActionMenu()`。

**修复与约束**:
- 普通触摸阈值只负责取消长按、启动文本扩选；整页拖动使用系统 `scaledPagingTouchSlop`，用户显式设置的正数仍优先，设置页摘要同步显示同一默认值。
- 整页拖动状态一旦越界即保持到抬手；只有未越过整页阈值且委托也未移动时才按点击处理，避免页尾无可翻内容的真实滑动反而触发九宫格动作。
- 动画中止状态按本次按下动作保存并在抬手/取消后清理；菜单动作立即响应，翻页、目录等非菜单动作仍消费一次，避免中止旧动画后同一次触摸又执行第二个动作。
- 不强制覆盖 `AppConfig.clickActionMC`（持久化键 `clickActionMiddleCenter`）；用户把中心区改成其他动作时继续遵循九宫格配置。

**回归点**:
- 位移超过普通触摸阈值但未超过整页阈值时仍触发一次已配置点击；超过整页阈值、到页尾无内容可翻、滑回起点时都不得退化为点击。
- 文本长按、选区拖动、图片点击、多点触控和 `ACTION_CANCEL` 不得误触菜单；显式设置小于或大于系统值的 `pageTouchSlop` 都应继续生效。
- Cover、Slide、Simulation、Scroll、NoAnim 五种文本翻页模式均需检查点击与拖动；动画进行中点击菜单应只中止动画并显示菜单，非菜单动作只中止当前动画。
- Android 8.1 MI PAD 4 安装新包后重点复测锁屏恢复、中心轻点和连续翻页；当前编译/JVM 结果不能替代这项真机验证。

### 0.1 ReaderProvider 增加按变体隔离的签名权限

**记录日期**: 2026-07-15
**验证日期**: 2026-07-15（Debug/Release Manifest 合并、AndroidTest 编译及 Debug APK 组装通过，并核对最终权限名）
**验证环境**: AGP 9.3.0, Gradle 9.6.1, app Debug/Release, compileSdk/targetSdk=37
**文件**: `app/src/main/AndroidManifest.xml`, `app/src/androidTest/java/io/legado/app/ReaderProviderPermissionTest.kt`, `api.md`, `app/src/main/assets/privacyPolicy.md`

**问题与安全边界**:
- `ReaderProvider` 需要保留 `exported=true` 供外部工具集成，但旧配置没有声明读写权限，任意已安装应用都可以尝试调用书源、书籍和正文接口。
- 固定使用旧 `io.legado.READ_WRITE` 或不带变体的权限名会与当前 `shutiao.reader.*` applicationId 不一致；Debug/Release 若使用不同证书并存，还可能争用同一个系统级自定义权限名。

**修复与约束**:
- Manifest 顶层声明 `${applicationId}.permission.READ_WRITE`，`protectionLevel="signature"`；`ReaderProvider` 通过同一权限统一保护读写入口。
- 合并后的 Release 权限为 `shutiao.reader.release.permission.READ_WRITE`，Debug 为 `shutiao.reader.debug.permission.READ_WRITE`，authority 同样按各自 applicationId 隔离。
- 外部调用方必须在自身 Manifest 声明目标变体的权限，并与目标 App 使用同一签名证书；仅声明权限但证书不同仍会被系统拒绝。
- 这是有意的外部兼容性变更：所有依赖无权限访问或使用不同证书的第三方客户端都会失去访问能力，不得为恢复兼容而降回普通权限或无权限导出。

**回归点**:
- 每次变更 applicationId/buildType/flavor 后都要检查合并 Manifest，确认权限名与 Provider authority 使用同一个最终 applicationId。
- Debug 和 Release 必须分别保留 `signature` 保护，不得在变体 Manifest 中覆盖为 `normal`、移除权限或只保护部分读写操作。
- Android 仪器测试通过最终包名解析 Provider，并断言系统解析出的读写权限、导出状态及 `PROTECTION_SIGNATURE` 基础保护级别；不要恢复硬编码旧 authority 的失效样例。
- 同签名集成测试时，客户端仍需显式声明对应权限；异签名客户端应收到系统权限拒绝，不能进入 `ReaderProvider` 控制器。

*Last updated: 2026-08-21*
