# Android 14+/17 适配踩坑记录

> 最新修订: 2026-06-18 00:00 CST
> 最近三次修订:
> - 2026-06-18 00:00 CST: WebDAV 仅同步进度改为 RangeOnly，不再对本地书做 checkBookReadable，避免误触发 SAF 目录权限提示。
> - 2026-06-16 00:00 CST: 补充 WebDAV 仅恢复阅读进度模式，避免跨设备完整恢复写入失效 SAF URI。
> - 2026-06-14 14:15 CST: 记录 EPUB/ZIP entries 读取失败返回 null 导致 `hasMoreElements()` 空指针的问题。
>
> 项目: 阅读 (legado) Android 电子书阅读器  
> 最近更新: 2026-06-18  
> 目标: targetSdk=36, compileSdk=36, minSdk=26

---

## 文档维护规则

- 本文件只保留最近 15 条适配/构建/运行时问题的详细记录，旧记录迁移到 `文档归档/`。
- 归档后主文件保留索引和仍有效的长期规则，避免后续 AI 读取时被过期细节干扰。
- 新增第 16 条时，把最旧 1 条移动到归档，再把主文件条目重新整理为 15 条。
- 时间敏感结论必须写明记录日期、验证日期、适用 SDK/依赖/系统版本；超过 30 天、target/compile SDK 改变、核心依赖升级、系统规则变化时重新验证。

## 归档索引

- `文档归档/适配踩坑记录-2026-05_06.md`: 早期 Gradle 下载/代理问题、Release 构建附录、前台服务启动入口统一、旧文件清单和未处理项。

## 长期有效规则

- Gradle/AGP: AGP 9 使用 `androidComponents`，split APK 取 ABI 时比较 `it.filterType.name() == "ABI"`；不要恢复旧 `applicationVariants`。
- 编译验证: Kotlin/代码改动后优先跑 `scripts/check-debug.sh`；生成 Debug APK 跑 `./gradlew :app:assembleDebug --no-daemon`。
- minSdk=26: 兼容分支不要删除；旧 API 调用需要按 API level 分支并局部 `@Suppress("DEPRECATION")`。
- 前台服务: 启动入口用 `ContextCompat.startForegroundService()`；服务内必须用 `ServiceCompat.startForeground(this@Service, id, notification, type)` 并声明 `foregroundServiceType`。
- WebDAV: 日志不得包含账号、密码、Authorization header、URL query/fragment；进度下载写入前必须校验章节范围。本地书可读性校验只在真正加载内容时做（`ReadableRequired`），仅同步进度（`downloadAllBookProgress`/`restoreBookProgressOnly`/`syncProgress`/`syncBookProgress`）用 `RangeOnly`，不得触发 SAF 目录权限检查。
- SAF 本地书: 备份恢复后不同设备的 `content://.../tree/...` 可能失效；不要自动猜路径替换，必须提示用户重新选择书籍目录。
- Web 前端: 修改 `modules/web/src` 后必须同步 `app/src/main/assets/web/`，否则 APK 不生效。

---

## 最近 15 条记录

### 0. WebDAV 仅同步进度不得触发本地 SAF 可读性检查

**记录日期**: 2026-06-18  
**验证环境**: targetSdk=36, compileSdk=36, minSdk=26；跨设备恢复后旧 SAF `content://.../tree/...` URI 失效场景  
**文件**: `AppWebDav.kt`, `ReadBook.kt`, `BaseReadViewModel.kt`, `ReadMangaViewModel.kt`, `AbsCallBack.kt`

**现象**:
- WebDAV 进度拉取成功后，`AppWebDav.canApplyBookProgress()` 对 `book.isLocal` 调 `FileBook.checkBookReadable()`。
- 跨设备恢复后的旧 SAF `content://.../tree/...` URI 失效时，该检查抛 `SecurityException`，导致阅读页/同步流程弹出“选择书籍所在文件夹/目录权限”提示。
- 进度同步本身只需校验章节范围，不应检查本地文件可读。

**根因**:
- `checkBookProgress()` 无条件对本地书执行 `FileBook.checkBookReadable()`，把“仅同步进度”和“真正加载本地书内容”两种场景混在一起。
- 仅同步进度不需要打开本地文件，旧 URI 失效不应阻断进度写入，更不应触发 SAF 目录权限弹窗。

**修复**:
- `AppWebDav` 进度校验拆成两种模式 `ProgressCheckMode`：
  - `RangeOnly`：只校验 `simulatedTotalChapterNum()` 范围。
  - `ReadableRequired`：校验章节范围，并对本地书执行 `FileBook.checkBookReadable()`。
- `canApplyBookProgress()`/`checkBookProgress()` 新增 `mode` 参数，默认 `RangeOnly`。
- `downloadAllBookProgress()`、`restoreBookProgressOnly()`、`ReadBook.syncProgress()`、`BaseReadViewModel.syncBookProgress()`、`ReadMangaViewModel.syncProgress()` 显式改用 `RangeOnly`，仅同步进度时不再触发本地 SAF 可读性检查。
- 真正打开/加载本地书内容的路径仍通过 `BookHelp.getBookPFD()` → `contentResolver.openFileDescriptor()` 自然抛 `SecurityException`，提示用户重新选择目录，不掩盖真实打不开书的问题。
- `RangeOnly` 模式下保留 `outOfRange` 日志，不记录 `localBookUnreadable`；`ReadableRequired` 模式下保留两者。
- 本条修正了旧条目 1（EPUB/ZIP entries）与旧条目 16（WebDAV 恢复备份 SAF 路径不一致）中“进度同步写入前对本地书做 `checkBookReadable`”的结论：该结论仅适用于 `ReadableRequired` 场景，不适用于仅同步进度。
- 可选脱敏：`AbsCallBack.onResponseStarted` 调试日志改用 `redactUrl()`，只保留 `scheme://host[:port]/path`，去掉 userinfo 与 query，避免 WebDAV URL 泄露。

**回归点**:
- 跨设备旧 `content://` URI 失效时，WebDAV 全量/即时进度同步不再弹目录权限；进度按章节范围正常写入或跳过。
- 真正打开该本地书时仍提示“打开本地书籍出错，请选择书籍所在文件夹”并弹出目录选择器。
- `outOfRange` 日志保留；`RangeOnly` 模式下不出现 `localBookUnreadable` 日志。
- 不改动完整备份恢复逻辑，不自动替换本地 SAF 路径。
- `onResponseStarted` 日志不再包含完整 URL（含 query/userinfo）。

### 1. EPUB/ZIP entries 读取失败不要返回 null

**记录日期**: 2026-06-14
**验证环境**: targetSdk=36, compileSdk=36, minSdk=26；本地 EPUB/ZIP 目录读取异常路径
**文件**: `AndroidZipFile.java`, `ResourcesLoader.java`

**现象**:
- 阅读本地 EPUB 时阅读页显示 `LoadTocError: Attempt to invoke interface method 'boolean java.util.Enumeration.hasMoreElements()' on a null object reference`。
- 日志里 `AndroidZipFile.entries()` 内部 IOException 被吞掉并返回 null，`ResourcesLoader.loadResources()` 随后直接调用 `entries.hasMoreElements()` 触发空指针。

**修复**:
- `AndroidZipFile.entries()` 捕获 IOException 后改为抛出带文件名上下文的 `IllegalStateException`，不再返回 null。
- `ResourcesLoader.loadResources()` 对 `zipFileWrapper.entries()` 增加 null 防御，抛出明确的 `ZipException`。
- WebDAV 全量进度同步写入本地书进度前，使用 `FileBook.checkBookReadable()` 校验 EPUB/ZIP 目录可读；不再只校验文件流能打开。
- 普通本地 EPUB 直接 PFD 读取目录失败时，回退为复制到私有缓存后用标准 `ZipFile` 懒加载读取。
- 阅读页即时 WebDAV 进度同步拉取失败或本地书校验失败时，跳过云端进度并继续使用本地当前进度打开文件。

**回归点**:
- 损坏、权限失效或无法读取目录的 EPUB/ZIP 不应再显示 `hasMoreElements()` 空指针。
- WebDAV 云端进度同步遇到目录不可读的本地 EPUB/ZIP 时应跳过该书，不应写入进度后打开失败。
- 已授权目录中的普通本地 EPUB 如果 PFD ZIP 解析失败，应尝试缓存后读取。
- 阅读页打开过程中云端进度拉取失败时，不应阻断本地文件打开，也不应自动套用异常云端进度。
- 可正常读取的 EPUB 目录加载、封面读取、章节内容加载不受影响。

### 2. Lambda 中 this 解析错误

**文件**: `AudioPlayService.kt`, `BaseReadAloudService.kt`

**错误**: `ServiceCompat.startForeground(this, id, notification, type)` 在 lambda 中把 `this` 解析为 `CoroutineScope` 而非 `Service`。

**修复**: 显式指定 Service 接收者：

```kotlin
ServiceCompat.startForeground(this@AudioPlayService, id, notification, type)
```

### 3. 缺少 import

**文件**: `ContextExtensions.kt`

**错误**: 使用 `NetworkCapabilities` 但忘记 import `android.net.NetworkCapabilities`。

**修复**: 添加 import。修改后先用 IDE 或 `scripts/check-debug.sh` 检查 import/编译错误。

### 4. 平台 API 不存在

**文件**: `MediaPlaybackLock.kt`

**错误**: 想用 `WIFI_MODE_HIGH_PERF` (API 29)，但 platform-36 的 android.jar 里没有这个常量。

**修复**: 回退到 `WIFI_MODE_FULL_HIGH_PERF`，加 `@Suppress("DEPRECATION")`。

### 5. ProgressDialog init receiver 类型不匹配

**文件**: `AndroidDialogs.kt`

**错误**: `AlertDialog.Builder(this).apply { if (init != null) init() }` 中 `init` 的 receiver 是 `AlertDialog`，不是 `AlertDialog.Builder`。

**修复**: 先 `create()` 得到 `AlertDialog`，再执行 `dialog.init()`，最后 `dialog.show()`。

### 6. getParcelable 可空性处理

**文件**: `FileAssociationDialog.kt`

**错误**: `BundleCompat.getParcelable()` 返回 `Uri?`，但后续 `importBook()` 需要非空 `Uri`。

**修复**: 使用 `?: return@registerHandleFile` 提前返回，避免 nullable 传入非空参数。

### 7. StaticLayout 替换时括号没对齐

**文件**: `TextChapterLayout.kt`

**错误**: 替换为 `StaticLayout.Builder.obtain(...).build()` 后残留多余右括号。

**修复**: 删除多余 `)`；后续替换多行构造器时优先编译确认括号结构。

### 8. WebDAV 同步后空章节列表崩溃

**文件**: `ReadBook.kt`, `ChapterListFragment.kt`

**现象**: 设备 A 下载书籍，设备 B 阅读并同步到 WebDAV，设备 A 同步后打开书籍崩溃。

**错误**: `Index 0 out of bounds for length 0`

**根因**: `chapterList?.get(0)` 在章节列表为空时抛异常。

**修复**: `chapterList?.get(0)` 改为 `chapterList?.firstOrNull()`。

### 9. WebDAV 首次配置误报应用验证失败

**记录日期**: 2026-06-01  
**验证环境**: targetSdk=36, compileSdk=36, minSdk=26；反馈设备 Android 8.1 老平板、Android 16  
**文件**: `AppWebDav.kt`

**现象**:
- 首次安装后依次填写 WebDAV 服务器地址、账号、密码，保存密码时提示“webDav应用验证失败”。
- 已配置成功的版本先输入错误密码会正常提示验证失败；随后重新输入正确密码，仍持续提示“webDav应用验证失败”。

**根因**:
- 旧 `upConfig()` 入口立即清空 `authorization` / `defaultBookWebDav`，校验失败会破坏旧的可用运行时状态。
- 旧逻辑直接用 `服务器地址 + AppConfig.webDavDir` 校验，应用子目录未创建时部分服务返回 401，被误判为账号密码错误。

**修复**:
- 仅当账号或密码为空时清空运行时配置；有配置时先校验再替换。
- 拆分 `serverWebDavUrl` 和 `rootWebDavUrl`，优先校验用户填写的服务器根地址，再创建应用子目录。
- `authorization` 和 `defaultBookWebDav` 只在校验通过并创建目录成功后替换。
- `checkAuthorization()` 失败不再删除已输入密码。
- 使用 `AtomicInteger configVersion` 忽略过期校验结果。
- WebDAV 日志只记录脱敏 URL、HTTP 状态码、401 标记和异常信息。

**回归点**:
- 首次安装新配置可用，默认子目录不存在时能自动创建。
- 错误密码后再输入正确密码，不应持续失败。
- 旧版本已有可用配置时，失败校验不能破坏远程书籍入口。

### 10. AGP 9.2.1 迁移后 split APK 打包冲突

**记录日期**: 2026-06-05  
**验证环境**: AGP 9.2.1, Gradle 9.4.1, targetSdk=36, compileSdk=36, minSdk=26  
**文件**: `app/build.gradle`, `gradle/libs.versions.toml`, `gradle/wrapper/gradle-wrapper.properties`

**现象**:
- `compileAppDebugKotlin` 通过。
- `assembleDebug` 到 `packageAppDebug` 阶段报 ZIP 异常或卡住。
- Debug split APK 应输出 `legado_all.apk`, `legado_arm64.apk`, `legado_armv7.apk`, `legado_x64.apk`, `legado_x86.apk`。

**根因**:
- AGP 9 已移除 `applicationVariants`，需要迁移到 `androidComponents`。
- `androidComponents` 的 `output.filters` 中 `filterType` 是 enum。写成 `it.filterType == "ABI"` 会永远匹配失败。
- ABI filter 匹配失败后，所有 split 输出都回退成 `legado_all.apk`，导致打包冲突。

**修复**:

```groovy
androidComponents {
    onVariants(selector().all()) { variant ->
        variant.outputs.forEach { output ->
            def abiFilter = output.filters.find { it.filterType.name() == "ABI" }
            def abiName = abiFilter ? (abiShortMap[abiFilter.identifier] ?: abiFilter.identifier) : "all"
            output.outputFileName.set("legado_${abiName}.apk")
        }
    }
}
```

**迁移注意**:
- AGP 9 内置 Kotlin 支持时，不要继续给 Android 模块显式应用 `kotlin-android` 插件。
- `signingConfig`, `minifyEnabled`, `splits.abi.enable`, `universalApk` 统一使用 Groovy `=` 赋值。
- 清理 AGP 9 已移除的 `gradle.properties` 属性。

### 11. 阅读器切到下一章偶发卡“加载中”

**记录日期**: 2026-06-06  
**验证环境**: targetSdk=36, compileSdk=36, minSdk=26；阅读页本地缓存章节/已从 WebDAV 下载到本地的书籍  
**文件**: `ReadBook.kt`

**现象**:
- 已经下载到本地的书籍，阅读到章节末尾切下一章时偶发一直显示“加载中”。
- 关闭 App 后重新打开同一本书可以恢复。

**根因**:
- `ReadBook.loadContent(index)` 和 `loadContentAwait(index)` 在 `chapterList` 非空时错误使用 `durChapterIndex` 取章节，而不是传入的 `index`。
- 预加载下一章时把下一章 index 加入 loading 集合，但实际完成的是当前章，导致下一章 loading 状态残留。

**修复**:
- 新增 `getChapter(index)`，统一按目标章节 index 从内存 `chapterList` 或数据库取章，并使用 `getOrNull()` 防越界。
- `loadContent()`、`loadContentAwait()`、`downloadIndex()` 统一走 `getChapter(index)`。
- `loadContentAwait()` 先取到章节再加入 loading 状态，避免无章节/越界时污染状态。

**回归点**:
- 已缓存在线书连续切章不应卡“加载中”。
- WebDAV 下载到本地的书籍连续翻章节不应需要重启。
- 首章上一章、末章下一章越界请求不能崩溃，也不能污染 loading 状态。

### 12. 提交前拦截本地环境/签名/AI 配置文件

**记录日期**: 2026-06-06  
**验证环境**: Git hooksPath `.githooks`；本地执行 `scripts/check-staged-local-files.sh`  
**文件**: `.githooks/pre-commit`, `scripts/check-staged-local-files.sh`

**目标**: 防止 `.sdk/`、`local.properties`、`app/gradle.properties`、`app/signing/`、`AGENTS.md`、`opencode.json` 等本地环境、签名和 AI 专用文件被误提交。

**实现**:
- 新增 `scripts/check-staged-local-files.sh`，只检查暂存区新增/修改/重命名文件。
- 新增 `.githooks/pre-commit` 调用检查脚本。
- 当前仓库启用方式: `git config core.hooksPath .githooks`。这是本地 Git 配置，不会自动同步到其他克隆。

### 13. 本地 Debug 编译验证脚本

**记录日期**: 2026-06-06  
**验证环境**: Gradle 9.4.1 + AGP 9.2.1；执行 `scripts/check-debug.sh`  
**文件**: `scripts/check-debug.sh`, `AI编辑必看架构图.md`

**目标**: 固定修改 Kotlin 后的最小本地验证入口，避免每次手写 Gradle task。

**实现**:
- 新增 `scripts/check-debug.sh`，自动切到项目根目录并执行 `./gradlew :app:compileAppDebugKotlin --no-daemon "$@"`。
- 脚本保留透传参数能力，后续可临时追加 Gradle 参数。

**维护注意**:
- 如果项目最小编译验证 task 改名，必须同步更新脚本和 `AI编辑必看架构图.md`。
- 该脚本只做 Debug Kotlin 编译验证，不替代 `assembleDebug`、单元测试或 Web assets 同步校验。

### 14. WebDAV 日志必须带上下文且避免敏感信息

**记录日期**: 2026-06-06  
**验证环境**: Gradle 9.4.1 + AGP 9.2.1；`scripts/check-debug.sh` 编译通过  
**文件**: `AppWebDav.kt`, `WebDav.kt`

**目标**:
- WebDAV 配置、认证、上传/下载阅读进度和全量同步失败时，日志能看出操作类型、路径阶段、HTTP 状态或进度文件名。
- 日志不能记录 WebDAV 账号、密码、Authorization header、URL query 和 fragment。

**实现**:
- `WebDav.safeLogUrl()` 统一脱敏 URL：移除 userinfo、query、fragment。
- `WebDav.check()` 和 `checkResult()` 输出脱敏 URL，认证失败仍保留 HTTP code / is401。
- `AppWebDav.upConfig()` 增加配置版本、server/root URL（脱敏）和过期配置日志。
- `uploadBookProgress()` / `getBookProgress()` 日志增加进度文件名、章节 index 和 pos；成功和跳过原因走 `AppLog.putDebug()`，失败走 `AppLog.put()`。
- `downloadAllBookProgress()` 增加远端进度文件数、匹配数、更新数，并捕获异常写入 WebDAV 上下文。

**维护注意**:
- 新增 WebDAV 日志时不要拼接 `Authorization.toString()`。
- 高频成功日志优先用 `AppLog.putDebug()`。

### 15. 本地 EPUB 读取权限丢失后阅读页卡“加载中”

**记录日期**: 2026-06-08  
**验证环境**: vivo V2458A, Android 16, WebView 148；targetSdk=36, compileSdk=36, minSdk=26  
**文件**: `BaseReadViewModel.kt`, `ReadBookViewModel.kt`, `RemoteBookActivity.kt`, `RemoteBookViewModel.kt`

**现象**:
- 打开本地 EPUB 时，阅读页一直显示“加载中”。
- 日志中先出现 `SecurityException: Permission Denial`，路径为 `content://com.android.externalstorage.documents/tree/...epub`。
- 同一轮操作里 WebDAV 进度文件请求返回 200；另有远程 EPUB 下载请求返回 401，需要单独排查服务端鉴权/路径规则。

**根因**:
- 本地书 `loadBookInfo()` 会先调用 `FileBook.upBookInfo(book)` 读取 EPUB 元信息/封面。
- SAF 目录授权丢失或书籍目录不匹配时，`ContentResolver.openInputStream/openFileDescriptor` 抛 `SecurityException`。
- 旧逻辑只在 `initBook()` 后面的 `checkLocalBookFileExist()` 和目录加载阶段处理权限失败；更早的书籍信息加载异常会走通用失败路径，阅读页 loading 状态收口不明确。
- “远程书籍”列表点击已在书架的非压缩包书籍时，旧逻辑只按文件名查本地书并直接打开，没有先验证本地 `bookUrl` 是否仍可读。

**修复**:
- `BaseReadViewModel.loadBookInfo()` 的本地书分支增加 `try/catch`，失败时设置 `curBook`、发布空章节列表并回调子类。
- `ReadBookViewModel.onLocalBookLoadError()` 将页面消息改为“打开本地书籍出错”，并在 `SecurityException` / `FileNotFoundException` 时触发 `permissionDenialLiveData`，引导用户重新选择书籍所在文件夹。
- `RemoteBookActivity.startRead()` 改为委托 `RemoteBookViewModel.readOrImport()`。远程书籍入口发现同名本地书时先验证可读；不可读时使用当前远程条目重新导入/下载。

**回归点**:
- 手动撤销/变更书籍目录授权后打开本地 EPUB，应提示重选目录，不应一直显示“加载中”。
- 重新选择正确目录后，应能重新加载目录和正文。
- WebDAV 远程书籍列表中，已在书架但本地 URI 失效的普通 EPUB/TXT，点击后应尝试重新下载。

### 16. WebDAV 恢复备份后 SAF 授权路径不一致导致本地书卡加载中并反复拉进度

**记录日期**: 2026-06-14  
**验证环境**: MI PAD 4, Android 8.1；targetSdk=36, compileSdk=36, minSdk=26  
**文件**: `BaseReadViewModel.kt`, `ReadBookViewModel.kt`, `AppWebDav.kt`, `RemoteBookViewModel.kt`

**现象**:
- 老设备从 WebDAV 恢复新设备备份后，打开本地书《我有一座恐怖屋》。
- 日志在 11:47:18 抛 `SecurityException: Permission Denial`，URI 形如 `content://.../tree/primary:yuedu/...epub`。
- 阅读页一直卡在“加载中”，随后多次打开 `ReadBookActivity`，并多次拉取 WebDAV 进度 `chapter=840 pos=205`。
- 老设备实际可重新授权的目录是 `primary:阅读`，SAF 树路径目录名与新设备不同。

**根因**:
- WebDAV 备份恢复的是完整数据库，书籍 `bookUrl` 仍指向新设备授权目录 `primary:yuedu`。老设备 SAF 授权丢失且路径不一致，读取时抛 `SecurityException`。
- `BaseReadViewModel.loadChapterList()` 本地书异常路径只 `postValue(emptyList())`，没有回调 `onLocalBookLoadError()`，导致阅读页错误状态收口不完整。
- `ReadBookViewModel.initBook()` 在章节列表为空/本地文件不可读时虽然 return，但 `ReadBook.initData()` 提前触发页面刷新，`ReadBook.msg` 尚未设置为错误文案，页面先进入“加载中”占位。
- `AppWebDav.downloadAllBookProgress()` 对本地书未做可读性校验，也未校验远端 `durChapterIndex` 是否在当前书籍章节数范围内，直接把远端进度写入不可读/章节不匹配的本地书记录。
- 远程书籍入口已具备“同名本地书不可读则重新导入”逻辑，但 401 下载失败与本地 `SecurityException` 混在一起记录，不利于区分服务端鉴权问题。

**修复**:
- `BaseReadViewModel.loadChapterList()` 本地书 catch 块保留空章节结果以阻止后续加载，同时调用 `onLocalBookLoadError(book, e)`。
- `ReadBookViewModel.initBook()` 对本地书先执行 `checkLocalBookFileExist()`，失败时立即 `isInitFinish = true` 并 return，确保 `ReadBook.msg` 已是错误文案，不进入“加载中”。
- 本地书章节列表为空时，设置“目录为空或无法加载”错误文案并触发目录重选。
- `AppWebDav.downloadAllBookProgress()` 写入进度前校验：所有书检查远端 `durChapterIndex` 是否落在 `0 until simulatedTotalChapterNum()`；本地书额外检查文件可读，否则跳过并记录明确日志。
- `RemoteBookViewModel.readOrImport()` / `addToBookshelf()` 错误处理区分 `WebDavException` 且消息含 `401` 的情况，单独记录“WebDAV 认证失败(401)”。
- **后续修复 (2026-06-16)**: WebDAV 在线恢复新增“仅恢复阅读进度”模式。该模式只读取 `bookProgress/*.json`，按当前设备已有书籍的 `name + author` 匹配，写入 `durChapterIndex`、`durChapterPos`、`durChapterTitle`、`durChapterTime`、`syncTime`，不下载备份 zip、不恢复 `bookshelf.json`，避免跨设备写入失效 `content://.../tree/...`。

**回归点**:
- 从 WebDAV 恢复备份后，若本地书 URI 授权丢失或目录不一致，打开该书应提示“打开本地书籍出错，请选择书籍所在文件夹”，并弹出目录选择器。
- 重新选择正确目录后应能加载目录和正文；不重新选择则不应卡“加载中”。
- 远端进度索引超过本地章节总数时，`downloadAllBookProgress` 应跳过并打日志，不写入数据库。
- 本地书不可读时，`downloadAllBookProgress` 应跳过该书，不写入任何进度。
- WebDAV 远程书籍下载返回 401 时，日志应明确标记“WebDAV 认证失败(401)”。
- Android 16 设备备份、Android 8.1 设备恢复时，选择“仅恢复阅读进度”不应修改本地书 `bookUrl`，也不应出现 `primary:yuedu` 的 Permission Denial。
- 未导入同名同作者本地书时，“仅恢复阅读进度”应跳过远端进度，不创建旧路径本地书。
