# 新功能踩坑记录 - 阅读页与翻页

> 返回主题索引: [新功能踩坑记录.md](新功能踩坑记录.md)
> 当前复核状态（2026-08-01）：原生阅读点击/翻页阈值、阅读性能采样、全文搜索和阅读器状态测试按当前代码路径重新归类；历史编译或真机结果仍保留各自验证日期，不因本次文档复核自动续期。
> 时间有效性：超过 30 天、SDK/核心依赖或阅读器架构变化后，必须按条目要求重新验证再引用。

---

## 原生文本阅读点击与滑动判定

- 记录日期: 2026-07-16
- 验证日期: 2026-07-16（定向 JVM 测试、Debug Kotlin 编译及 `scripts/check-debug.sh` 通过；老平板真机待复测）
- 适用环境: Android 原生文本阅读页，Cover/Slide/Simulation/Scroll/NoAnim 翻页
- 相关文件: `ReadView.kt`、`ReadTouchDecider.kt`、`MoreConfigDialog.kt`、`ReadTouchDeciderTest.kt`

**交互约束**:
- 普通 `scaledTouchSlop` 只用于取消长按和启动文本选区拖动；翻页使用 `scaledPagingTouchSlop` 或用户显式配置的阈值。不能再用普通阈值直接否决点击，否则老触屏抖动和自定义较大阈值之间会出现无响应区。
- 整页拖动越界状态必须在整个手势内保持；即使手指滑回起点或页尾没有内容可翻，也不能在抬手时执行九宫格点击动作。
- 动画中止后的处理按动作语义决定：菜单动作仍立即显示菜单，其他动作消费本次点击，避免一根手指同时完成旧翻页并执行新动作；九宫格配置值本身不被重置。
- 用户把 `pageTouchSlop` 设得比普通触摸阈值更小时，超过该阈值就应开始翻页并取消长按；设得很大时不能拖慢文本选区的启动。

**回归点**:
- 中心菜单、其他区域菜单和自定义目录/翻页动作分别覆盖无动画、动画进行中和锁屏恢复三种状态。
- 轻微抖动、超过阈值的水平/垂直/对角拖动、滑回起点、页首页尾、长按选字、图片点击、多指和取消事件分别验证，确保点击与拖动互斥。
- 设置页显示的系统默认像素值必须与运行时实际使用的 paging touch slop 一致；修改自定义值后当前阅读页应通过既有配置事件立即更新。

---

## 阅读性能日志定向优化

### 1. Web 阅读进度按需同步、低功耗上传与章节预取并发收敛

- 记录日期: 2026-07-11
- 验证日期: 2026-07-11（`pnpm run type-check`、`pnpm run build-only`、`:app:compileAppDebugKotlin` 通过；Web assets 已同步）。
- 适用环境: Web 服务书本翻页/连续滚动；手机端开启 WebDAV 阅读进度同步；本地 EPUB 通过 Web 服务阅读。
- 相关文件:
  - `modules/web/src/store/bookStore.ts`
  - `modules/web/src/views/BookChapter.vue`
  - `modules/web/src/views/BookShelf.vue`
  - `app/src/main/java/io/legado/app/api/controller/BookController.kt`
  - `app/src/main/java/io/legado/app/help/WebBookProgressUploadScheduler.kt`
  - `app/src/main/assets/web/index.html`

**日志证据**:
- 同一轮缓存跨章会在数毫秒内触发两次 `/saveBookProgress`，两次请求并发上传同一个 WebDAV 进度文件，日志出现 `423:Locked`。
- Web 进度保存慢样本中位数约 677ms、P95 约 1.82s；重复请求会放大网络等待和 WebDAV 服务端负载。
- 书本模式跨章后同时预取前后相邻章节，本地 EPUB 样本出现一个相邻章约 30ms、另一个相邻章 16.3s 的极端差异。

**优化约束**:
- 打开/搜索/切换分组书架时只读手机 Room；仅点击具体书籍或“最近阅读”时调用 `/syncBookProgress` 拉取这一册。远端进度只有在章节范围有效且位置更靠前时才应用；远端落后、缺失、同步关闭或拉取失败都继续使用手机本地进度。
- Web 端普通进度保存使用固定 5 秒 latest-wins 窗口：第一次变化启动计时，窗口内只覆盖最新位置，不因持续滚动重置计时；位置键包含书籍、章节、字符位置和章节标题，不包含时间戳。
- 手机端 `/saveBookProgress` 先更新 Room 并立即响应，再由 `WebBookProgressUploadScheduler` 从第一次 dirty 起固定 60 秒后上传最新位置；没有 dirty 不保留计时器，上传失败不自行循环重试。
- 切章、页面隐藏和路由离开用 `flush=true` 立即补交；页面关闭使用携带 `flush=true` 的 Beacon。上传中产生的新位置保留到下一窗口，不能被旧上传完成回调清掉。
- App 启动全量拉取、Web 单书拉取、Web Room 保存和调度器上传按书互斥，避免同一本书并发更新；不新增 WakeLock、轮询或常驻连接。
- 新增的 Web API JSON 请求/响应 DTO 必须保留 Release 字段名；本次 DTO 已加 `@Keep`，否则 Debug 正常但 R8 可优化掉 Gson 反射需要的字段。
- 缓存跨章不再先保存安全哨兵位置，等待新 `BookPageReader` 上报真实页位置后立即保存，避免安全位置和真实位置并发上传。
- 相邻章节预取改为串行队列，并让用户实际翻到仍在预取的章节时复用同一 Promise，不重复请求同一章。
- 预取 generation 失效后不得把旧请求结果写回当前缓存；清缓存不强制取消正在执行的底层 HTTP 请求。

**回归测试点**:
- 手机第 10 章、远端第 15 章时点击后从第 15 章打开；手机领先、远端越界、远端缺失或断网时不得回退或阻止打开。
- 旧 `readingRecent` 必须先走同一单书同步路径；连续双击只允许一个有效同步任务，书架加载和切换分组不访问 WebDAV。
- 5 秒内连续翻页只向手机提交最新位置；60 秒内持续阅读只上传一次最新位置，停在同页不产生重复 Room/WebDAV 请求。
- 上传期间继续翻页不得丢失新位置；失败后不自动每 60 秒重试，新进度、页面离开或重新打开书籍可以重新触发。
- 切章、隐藏、返回书架和 5 秒内关闭页面都应补交 dirty 位置；相同位置仅时间戳变化不得请求手机或 WebDAV。
- 缓存跨章时手机日志不应再出现同一毫秒两次 `/saveBookProgress`，WebDAV 不应返回 `423:Locked`。
- 快速跨多章时最终上传位置必须是最后一次阅读位置，中间旧位置可被合并但不能覆盖新位置。
- 页面关闭时 Beacon 仍能尽力保存最近进度；与尚未结束的普通保存交错时由后端按书串行处理。
- 书本模式进入一章后按“下一章优先、上一章随后”的顺序预取，两次本地 EPUB 正文读取不并发。
- 用户在预取完成前立即翻到目标章时应等待并复用当前预取，不再启动第二个相同章节请求。

### 2. Android Cover 翻页启动截图拆帧

- 记录日期: 2026-07-11
- 验证日期: 2026-07-11（`:app:compileAppDebugKotlin`、`scripts/check-debug.sh` 通过；真机耗时变化需用同一设备复测）。
- 适用环境: Android 原生文本阅读页 Cover 翻页；不影响 Slide、Simulation、Scroll、NoAnim 和漫画阅读。
- 相关文件:
  - `app/src/main/java/io/legado/app/ui/book/read/page/ReadView.kt`
  - `app/src/main/java/io/legado/app/ui/book/read/page/delegate/PageDelegate.kt`
  - `app/src/main/java/io/legado/app/ui/book/read/page/delegate/CoverPageDelegate.kt`

**日志证据**:
- `android.read.captureCover` 超过 8ms 的 NEXT 样本 230 个，中位数约 14.1ms、P95 约 24.6ms、最大 38.3ms；PREV 慢样本中位数约 8.7ms。
- Cover NEXT 启动时同步录制 `nextPage` 和 `curPage` 两张整页，成本集中在用户抬手到动画首帧之前；动画逐帧 `drawFrame` 和 `animCompute` 没有超过现有阈值的记录。

**优化约束**:
- 只对点击区域映射为下一页的 ACTION_DOWN 预录制当前页；ACTION_UP 或移动越过阈值后启动动画时只补录下一页。PREV 本身只有单页截图且慢样本未超过一帧预算，保持原路径。
- 预录制结果必须同时匹配 NEXT 方向和同一个 `TextPage` 实例，并且只在 500ms 内有效；长按、页面已经切换、旋转或尺寸变化时回退为原有完整录制。
- 新增 `android.read.prepareCover` 指标单独记录按下阶段耗时，原 `captureCover` 继续衡量动画启动阶段，避免把总工作量转移后误判为消失。
- 音量键、自动翻页和没有命中翻页点击区的滑动仍走原完整录制路径，不改变既有行为。

**回归测试点**:
- Cover 模式点击下一页/上一页，动画方向、页面内容、页眉页脚和进度必须正确，首帧不应闪旧页或透明背景；上一页路径性能与行为不变。
- 按下超过 500ms 再拖动、长按选择文字、点击图片/菜单后再用按键翻页，过期预录制不得被复用。
- 从中间区域开始左右滑动、音量键翻页、自动翻页仍能正常录制并完成动画。
- 图片加载、主题/字号切换、横竖屏切换后首次翻页不得复用尺寸或内容已失效的页面。
- 同一设备复测时，点击 NEXT 的 `captureCover` 应接近单页录制耗时；`prepareCover + captureCover` 总耗时允许接近修改前，但不能集中阻塞动画首帧。

### 3. Web 纸质书翻页帧级采样

- 记录日期: 2026-07-12
- 验证日期: 2026-07-14（Vue 3.5.39 下 `pnpm run type-check`、`pnpm run build-only`、Web assets 同步检查通过）
- 适用环境: Web 服务书本翻页的纸质书/滑动效果；生产构建需开启 `localStorage.legadoReaderPerf = '1'`。
- 相关文件: `modules/web/src/components/BookPageReader.vue`、`modules/web/src/utils/readerPerformance.ts`、`app/src/main/assets/web/index.html`

**采样语义**:
- 每次动画用 `requestAnimationFrame` 采集实际回调帧数、最大帧间隔，以及超过 32ms/50ms 的慢帧数量。
- `expectedFrames` 用本次动画较快 25% 的帧间隔估算浏览器实际刷新节奏，兼容 60/90/120Hz；样本不足时按 60Hz 回退。
- `web.book.frames` 包含 `type=inner/external`、`direction=next/prev`、`effect=book/slide`、`actualFrames`、`expectedFrames`、`droppedFrames`、`maxFrameGap`、`over32ms`、`over50ms`。
- 为避免性能采样本身增加网络负担，`web.book.frames` 取代原 `web.book.flip`，每次翻页仍只上传一条性能日志；完成后清理对应 Performance Measure，避免长时间阅读持续累积。

**回归点**:
- 章内/跨章、上一页/下一页都应各产生一条字段完整的 `web.book.frames`。
- 未开启生产性能采样时不启动逐帧回调，也不发送日志；开启后不得增加每次翻页的日志请求数量。
- `maxFrameGap > 32ms` 或 `actualFrames < expectedFrames` 可作为浏览器渲染掉帧证据，不能用总动画时长 440ms 单独判断流畅度。
- 修改需要重新构建并安装 APK，旧 APK 不包含该采样。

---

## 阅读页流畅度测量

### 1. 问题范围

- 记录日期: 2026-07-09
- 验证日期: 2026-07-09（`./gradlew :app:compileAppDebugKotlin --no-daemon`、`scripts/check-debug.sh`、`pnpm run type-check`、`pnpm run build-only` 通过；Web assets 已手动同步并用 `cmp` 校验一致）。
- 适用环境: Android 原生阅读页；Vue 3.5 + Vite 5 + Pinia 的 Web 服务阅读页。

### 2. 目标

- 第一阶段只补可复测指标，不改变阅读交互、翻页动画类型、分页算法或进度保存语义。
- 指标覆盖绘制、动画推进、截图录制、章节取回、排版完成、Web 分页、跨章动画和进度保存。
- Android 只在 Debug 包或“记录日志”开启时输出；Web 在开发环境默认输出，生产构建需设置 `localStorage.legadoReaderPerf = '1'` 或 URL 带 `readerPerf`。

### 3. 实现要点

- Android 侧用 `ReaderPerformance` 包装慢路径，只记录超过阈值的耗时，避免每帧刷日志。
- `ReadBook` 用章节 index 记录加载起点，内容回到 `contentLoadFinish` 时输出 `contentReady`，排版和页面回调完成后输出 `layoutReady`。
- Web 侧用 `performance.mark/measure` 和控制台日志记录取章、分页、跨章目标章分页、翻页完成和进度保存耗时。
- Web 侧显式开启 `legadoReaderPerf` / `readerPerf` 后，会将同一条性能日志通过 `saveReaderLog` POST 回手机，并由手机端 `AppLog.putDebug` 写入日志；未开启手机“记录日志”时接口成功但不落文件。
- 修改 `modules/web/src` 后必须构建并同步 `app/src/main/assets/web/index.html`，否则 APK 内 Web 性能采样不会生效。

### 4. 回归测试点

- Debug/日志开关关闭时，原生阅读行为和性能不应出现可感知变化。
- 打开 Android 日志后，快速翻页、跨章、图片章节加载应能看到慢路径耗时，且不应持续刷低耗时帧。
- Web 开启 `legadoReaderPerf` 后，切章、书本翻页、无限追加和隐藏保存应能在控制台看到对应耗时。
- 手机端“记录日志”和 Web `legadoReaderPerf` 同时开启时，Web 性能耗时应写入手机日志。
- Web 构建后 assets 必须同步，且 `scripts/check-web-assets-sync.sh` 通过。

---

---

## 一、翻页动画功能分支

> 历史验证状态：下述分支边界是开发时的历史记录；2026-08-01 代码复核确认相关 `postInvalidateOnAnimation()` 优化仍存在于当前 `master`。⚠️ 本节最后一次编译验证为 2026-05-31，且当时 target/compileSdk=36；现已超过 30 天并升级为 target/compileSdk=37，修改前必须重新编译并补 120Hz 真机对比，旧验证结论仅供参考。

### 1. 分支边界

- 分支: `feature/page-turn-animation`
- 目标: 只做阅读页翻页动画相关改动。
- 主分支: `master` 继续保留兼容性修复和最终合并入口。
- 非目标: 不在本分支修改书源解析、数据库、WebDAV、朗读、漫画阅读等无关功能。
- 第一版范围: 只覆盖文本阅读页，不碰漫画翻页和滚动翻页。
- **方案调整 (2026-05-31)**: 从“新增景深动画”调整为“现有横向翻页动画高刷新平滑化”。不新增动画类型，不改设置 UI，不加新选项。优先让现有滑动、覆盖、仿真三类横向动画在 120Hz 等高刷新屏上更顺滑。

### 2. 现有扩展点

- 动画常量: `app/src/main/java/io/legado/app/constant/PageAnim.kt`
- 动画选择: `app/src/main/java/io/legado/app/ui/book/read/page/ReadView.kt`
- 动画实现: `app/src/main/java/io/legado/app/ui/book/read/page/delegate/*PageDelegate.kt`
- 设置界面: `app/src/main/res/layout/dialog_read_book_style.xml`
- 设置逻辑: `app/src/main/java/io/legado/app/ui/book/read/config/ReadStyleDialog.kt`
- 配置保存: `app/src/main/java/io/legado/app/help/config/ReadBookConfig.kt`

### 3. 设计约束

- ~~新动画必须作为新增模式接入，不替换现有 `cover`、`slide`、`simulation`、`scroll`、`noAnim`。~~
- **当前方案**: 不新增动画模式，只优化现有横向动画的刷新方式。
- 保留旧动画值不变，避免破坏用户已有阅读配置。
- 优先继承 `HorizontalPageDelegate`，复用触摸、截图、滚动、取消、章节边界处理。
- 不改分页核心、章节加载、内容排版和数据库结构。
- 第一版只覆盖文本阅读页，不扩展到漫画翻页，不修改 `ScrollPageDelegate` 和滚动翻页行为。

### 4. 时间有效性

- 记录日期: 2026-05-31
- 验证日期: 2026-05-31（编译通过 `:app:compileAppDebugKotlin`）。
- 适用环境: targetSdk=36, compileSdk=36, minSdk=26, 当前分支 `feature/page-turn-animation`。
- 复核条件: 超过 30 天、升级 Android Gradle Plugin/Kotlin/AndroidX Activity、修改阅读页分页架构、修改 `ReadView`/`PageDelegate` 基类时必须重新评估。

### 5. 已知风险

- ~~`dialog_read_book_style.xml` 的 RadioGroup 通过子项 index 映射动画值，新动画选项应追加到末尾，避免旧配置错位。~~ (当前方案不新增选项)
- 非滚动动画依赖 `curRecorder`、`prevRecorder`、`nextRecorder` 截图，快速翻页和取消动画需要检查截图刷新时机。
- 图片页、加载页、空章节页可能与普通文本页绘制差异较大，必须单独回归。
- 自动翻页、音量键翻页、鼠标滚轮翻页会调用动画入口，但不是完整触摸路径，必须验证。
- **高刷新率适配坑**: `postInvalidateOnAnimation()` 依赖系统 Choreographer，必须确保动画结束时不再重复请求，否则可能空转。已在 `PageDelegate.stopScroll()` 和 `abortAnim()` 中通过 `isStarted/isRunning` 状态控制。

### 6. 实现记录 (2026-05-31)

- **修改文件**:
  - `app/src/main/java/io/legado/app/ui/book/read/page/delegate/PageDelegate.kt`
  - `app/src/main/java/io/legado/app/ui/book/read/page/delegate/HorizontalPageDelegate.kt`
- **核心改动**:
  1. `PageDelegate` 新增 `protected open fun invalidateOnAnim()`，默认实现为 `readView.invalidate()`。
  2. `HorizontalPageDelegate` 覆盖为 `readView.postInvalidateOnAnimation()`。
  3. `PageDelegate.startScroll()` / `fling()` / `computeScroll()` 中动画启动和逐帧推进时，统一改为调用 `invalidateOnAnim()`。
  4. `PageDelegate.computeScroll()` 更新触摸点时显式传入 `invalidate = false`，避免重复 `invalidate`，随后由 `invalidateOnAnim()` 统一请求下一帧。
  5. `PageDelegate.postInvalidate()` 中 `readView.invalidate()` 改为 `readView.postInvalidateOnAnimation()`（仅对 `HorizontalPageDelegate` 生效）。
- **为什么这样改**:
  - `postInvalidateOnAnimation()` 将重绘请求对齐到系统 Choreographer 的 vsync 信号，在高刷新率设备上更容易充分利用 90Hz/120Hz。
  - 不改 `Scroller` 时间参数，动画速度保持一致；只优化“何时画下一帧”的调度精度。
  - `ScrollPageDelegate` 完全不受影响：它覆盖 `computeScroll()` 且继承 `PageDelegate`（非 `HorizontalPageDelegate`），仍走默认 `invalidate()`。

### 7. 回归测试清单

- 普通文本书: 点击下一页、上一页（滑动、覆盖、仿真三种动画各测）。
- 手势拖拽: 半途取消、跨过阈值完成翻页。
- 快速连续翻页: 下一页/上一页连点。
- 章节边界: 第一页上一页、最后一页下一页。
- 特殊内容: 图片页、空内容提示页、加载失败页。
- ~~配置切换: 在阅读样式弹窗中切换新旧动画。~~ (当前方案无新动画选项)
- 自动翻页: 开启后能正常前进，关闭后状态恢复。
- 方向变化: 横竖屏切换后动画尺寸正确。
- **高刷新设备**: 有条件时在 120Hz 设备上对比修改前后翻页顺滑度。

---

---

## 三、阅读页全文搜索

### 1. ViewModel 初始化必须同步 bookUrl

- 记录日期: 2026-06-06
- 验证日期: 2026-06-06（源码检查；编译验证见当前修改记录）
- 适用环境: 阅读页 `SearchContentActivity` / `SearchContentViewModel`
- 相关文件:
  - `app/src/main/java/io/legado/app/ui/book/searchContent/SearchContentActivity.kt`
  - `app/src/main/java/io/legado/app/ui/book/searchContent/SearchContentViewModel.kt`

**现象**:
- 阅读页打开“全文搜索”，无论输入什么关键词，都只显示“搜索内容为空，请检查净化或简繁设置”。

**根因**:
- `SearchContentViewModel.initBook()` 从 `IntentData.book` 取到了书籍对象，但没有同步设置 `bookUrl`。
- `SearchContentActivity.startContentSearch()` 使用 `appDb.bookChapterDao.getChapterList(viewModel.bookUrl)` 获取章节列表；`bookUrl` 为空字符串时查询不到任何章节，搜索循环不会真正执行。

**修复约束**:
- 初始化 `book` 时必须同步 `bookUrl = book.bookUrl`。
- 全文搜索只扫描本地书或已有缓存章节；如果后续改成边搜边联网拉取，必须单独评估网络并发、停止按钮取消、书源限流和离线书缓存策略。

**回归测试清单**:
- 已缓存在线书章节中搜索章节正文已有关键词，应出现命中结果。
- 搜索不存在的关键词，应只在扫描完成后显示空结果。
- 本地 TXT/EPUB 书搜索正文已有关键词，应出现命中结果。
- 进入搜索结果后返回阅读页，搜索结果定位和上下跳转仍可用。

### 2. Web 阅读页全文搜索必须由手机端按缓存边界执行

- 记录日期: 2026-07-15
- 验证日期: 2026-07-15（19 项定向 JVM 测试、Debug Kotlin 编译、Debug 检查、Web 类型检查、生产构建、APK assets 同步及 Release/R8 构建通过）。
- 适用环境: Web 服务当前书籍阅读页；本地 TXT/EPUB、在线书已缓存章节和 WebDAV 已落盘章节。
- 相关文件:
  - `app/src/main/java/io/legado/app/help/book/BookContentSearcher.kt`
  - `app/src/main/java/io/legado/app/web/socket/BookContentSearchWebSocket.kt`
  - `app/src/main/java/io/legado/app/web/WebSocketServer.kt`
  - `modules/web/src/api/api.ts`
  - `modules/web/src/components/BookContentSearch.vue`
  - `modules/web/src/components/BookContentSearchResult.vue`
  - `modules/web/src/views/BookChapter.vue`
  - `app/src/main/assets/web/index.html`

**搜索与位置语义**:
- 网页只通过 `/searchBookContent` WebSocket 提交当前 `bookUrl` 和普通文本关键词；不得循环调用 `/getBookContent` 搜整本书，否则在线章节缓存未命中会走书源下载。
- 本地非 WebDAV 书串行搜索全部目录章节并使用 `BookHelp.getContent`；在线书和 WebDAV 书只读取一次 `BookHelp.getChapterFiles(book)` 快照，再按章节缓存文件名精确过滤，并且只能用 `BookHelp.getCachedContent` 直接读取缓存。不能在 `hasContent` 或缓存快照检查后调用通用 `getContent`，否则 WebDAV 缓存在检查与读取之间被清理时会回退 `FileBook` 联网；搜索路径不得依赖或调用 `WebBook.getContentAwait`。
- 正文使用与 Web 取章相同的 `ContentProcessor.getContent(..., includeTitle = false)` 后再匹配。普通字面量匹配区分大小写，不解释正则；摘要只返回普通文本，图片 URL/属性不能成为命中内容。
- `chapterPos` 必须复刻 Web 的 UTF-16 段落累计规则：初值 `-1`，每段加可见字符数和一个换行，完整图片标签算一个字符，连续换行按 `split(/\n+/)` 折叠。图片正则不得跨 LF，否则多行异常标签会与前端分段顺序不一致；裸 CR 不会被 Web 拆段，仍按段内字符处理。
- 摘要上下文边界不能切断 emoji 等 UTF-16 surrogate pair；`queryIndexInChapter` 保留处理后原文坐标，`queryIndexInSnippet` 仅用于前端按文本 `span` 安全高亮。

**并发与生命周期约束**:
- 每个连接使用 `SupervisorJob + Dispatchers.IO` 且最多一个搜索 Job；新请求先取消并等待旧任务实际结束，防止不可中断的 EPUB/文件同步读取发生重叠。
- 逐章串行读取并在循环中检查协程取消；结果每 20 条发送一批，最多 500 条，达到上限后标记 `truncated` 并停止扫描。关闭 socket、发送失败、停止、重搜、组件卸载和路由离开都必须取消任务。
- WebSocket 信封统一使用 `start/results/progress/complete/error`；请求、结果和信封等 Gson 反射 DTO 必须加 `@Keep`，Debug 成功不能替代 Release/R8 验证。日志不得记录关键词或完整 `bookUrl`。
- 前端使用虚拟列表承载最多 500 条结果，摘要用三个文本 `span` 分段渲染，禁止把服务端字符串传给 `v-html`；搜索期间不启用全局 loading mask。
- 首次点击结果时先保存并 flush 搜索前进度，再进入“搜索结果预览”；预览期间浏览器可以翻页，但普通进度事件不得上传搜索结果位置，本地 `readingRecent`、页面隐藏、关闭和路由离开也都以原进度为安全回退。页面按手机端语义显示“是否恢复到跳转前的阅读进度？”，并提供“恢复原进度”和“保留当前位置”：前者加载原章节/位置，后者才提交当前结果位置；连续点击多个结果不能覆盖首次保存的原进度。
- 目标章节正文加载成功前延后更新浏览器进度，失败时恢复原章节、位置和滚动状态。书本模式重挂旧章节时必须把请求前的当前 `chapterPos` 作为 `initialPos`，不能复用只在切章时更新的陈旧 `bookInitialPos`，否则会退回旧页并覆盖已恢复进度。无限滚动追加在途时禁止并发跳转，避免旧追加响应混入目标章节。

**回归测试清单**:
- 本地 TXT/EPUB 能搜索全部章节；在线书和 WebDAV 书只命中已有缓存，搜索前后缓存文件数与网络请求数不增加；缓存快照后立刻删除 WebDAV 章节缓存也只能跳过该章，不能回退下载远端书籍。
- 结果分批出现且进度单调增加；停止、快速连续重搜、关闭面板、断开手机 Web 服务和路由离开后旧结果不得继续到达。
- 空关键词、超过 100 字符的关键词、无可搜索缓存、无匹配结果和超过 500 条结果分别显示明确状态。
- 滚动模式点击结果定位到目标段落，书本模式定位到包含该段落位置的书页；跳转后手机端仍保持搜索前进度，选择“恢复原进度”应回到原章节/原段落或原页，选择“保留当前位置”才同步结果位置。连续预览多个结果后仍应返回第一次点击前的位置。
- 在书本模式中间页模拟目标章加载失败后，必须仍停在原章节原页且手机端进度不变；预览未作选择时关闭页面或离开路由，重新打开仍从原进度进入。
- 同段多个命中、正文首尾命中、emoji 摘要、连续换行、图片标签及异常跨行图片样式都不能造成摘要乱码或 `chapterPos` 越界。
- PC/手机、日间/夜间主题正常；搜索过程不出现全局白色遮罩，结果列表不一次性渲染 500 个 DOM 节点。

---

## 四、阅读器核心测试

### 1. 章节 loading 状态先抽纯 Kotlin 类再测

- 记录日期: 2026-06-06
- 验证日期: 2026-06-06（`testAppDebugUnitTest --tests io.legado.app.model.ChapterLoadStateTest` 和 `scripts/check-debug.sh` 通过）
- 适用环境: 阅读页章节加载/预加载状态
- 相关文件:
  - `app/src/main/java/io/legado/app/model/ChapterLoadState.kt`
  - `app/src/test/java/io/legado/app/model/ChapterLoadStateTest.kt`
  - `app/src/main/java/io/legado/app/model/ReadBook.kt`

**目标**:
- 不直接硬测 `ReadBook` 全局单例，先把章节 loading 集合状态抽为纯 Kotlin 类，便于 JVM 单测。
- 覆盖“预加载下一章后用错误 index 完成，真正的下一章仍处于 loading，后续 start 被拒绝”的状态污染模式。

**已覆盖**:
- 同一 index 重复 `tryStart()` 会被拒绝。
- `finish(index)` 后同一 index 可以重新开始。
- 不同章节 index 的 loading 状态互不影响。
- 用错误 index `finish()` 不会清掉真正 loading 的章节。
- `clear()` 会清空全部 loading 状态。

**维护注意**:
- 后续收敛阅读器加载状态机时，优先扩展纯 Kotlin 状态类和 JVM 单测，再把 `ReadBook` 调用迁过去。
- 不要为了测试直接初始化 `ReadBook`、`appDb` 或 Android `appCtx`，否则测试会变慢且脆弱。

### 2. 章节加载状态显式区分 Idle / Loading / Failed

- 记录日期: 2026-06-06
- 验证日期: 2026-06-06（`testAppDebugUnitTest --tests io.legado.app.model.ChapterLoadStateTest` 和 `scripts/check-debug.sh` 通过）
- 适用环境: 阅读页章节加载/预加载异常路径
- 相关文件:
  - `app/src/main/java/io/legado/app/model/ChapterLoadState.kt`
  - `app/src/main/java/io/legado/app/model/ReadBook.kt`
  - `app/src/test/java/io/legado/app/model/ChapterLoadStateTest.kt`

**结论**:
- `ChapterLoadState` 现在显式提供 `Idle`、`Loading`、`Failed` 三种状态。
- `tryStart(index)` 只拒绝正在 `Loading` 的章节；`Failed` 允许后续重试并覆盖为 `Loading`。
- `finish(index)` 会清理状态并回到 `Idle`，避免成功章节状态长期累积。
- `ReadBook.loadContent()` / `loadContentAwait()` 的真实异常路径会标记 `Failed`，协程取消不标记失败。

**回归点**:
- 同一章节重复加载请求仍应被防重。
- 加载失败后再次触发同一章节加载应允许重试。
- 章节加载成功、取消或过期后不应残留 `Loading`。

### 3. 章节读取职责先从 ReadBook 抽小类

- 记录日期: 2026-06-06
- 验证日期: 2026-06-06（`testAppDebugUnitTest --tests io.legado.app.model.ReadBookChapterStoreTest` 和 `scripts/check-debug.sh` 通过）
- 适用环境: 阅读页章节列表、章节内容加载入口
- 相关文件:
  - `app/src/main/java/io/legado/app/model/ReadBookChapterStore.kt`
  - `app/src/main/java/io/legado/app/model/ReadBook.kt`
  - `app/src/test/java/io/legado/app/model/ReadBookChapterStoreTest.kt`

**结论**:
- `ReadBookChapterStore` 负责按 index 从内存章节列表或数据库函数获取章节，并统一处理越界。
- `ReadBook` 仍保留章节窗口、UI 回调、预下载和朗读触发，不在本轮拆动这些高耦合职责。
- 使用函数依赖注入 `getChapterCount` / `getChapter`，让章节读取逻辑可以 JVM 单测，不依赖 Room mock。

**已覆盖**:
- 内存章节列表存在时优先使用内存列表，不查 DB count。
- 内存章节列表缺失时回落到 DB count / DB chapter。
- 传入越界 index 时直接返回 null，不查询 DB。
- 按传入 index 获取章节，避免回退到当前阅读章节 index。

**维护注意**:
- 后续继续拆 `ReadBook` 时，优先抽可测试小类；不要一次性迁移章节窗口、翻页、朗读、同步进度。
- 如果章节来源增加新缓存层，应先扩展 `ReadBookChapterStore` 和测试，再改 `ReadBook.loadContent()`。

---

*Last updated: 2026-08-01*
