# 新功能踩坑记录 - 基础设施

> 返回主题索引: [新功能踩坑记录.md](新功能踩坑记录.md)
> 当前复核状态（2026-08-01）：离线缓存进度、安全导出、更新日志折叠、书籍来源边界、书架角标、自动更新和日志模块记录已按当前文件位置重新归类；未完成的真机、长任务和实际导出文件验证继续明确保留。
> 时间有效性：历史验证日期不因拆分改变；涉及缓存/导出、发布或日志生命周期的改动必须按对应条目重新回归。

---

## 离线缓存进度与可转移导出

- 记录日期: 2026-07-28
- 验证日期: 2026-07-29（缓存进度、完成通知门闩与导出计划 11 项定向 JVM 测试、Debug Kotlin 编译及 `scripts/check-debug.sh` 通过；本功能新增的 13 项 `MissingTranslation` 已清零，完整 lint 仍有 49 项历史错误；真机长任务和实际 TXT/EPUB 文件待安装包回归）
- 适用环境: Android 原生离线缓存页、缓存前台通知、TXT/EPUB 导出
- 相关文件:
  - `app/src/main/java/io/legado/app/model/CacheBook.kt`
  - `app/src/main/java/io/legado/app/model/CacheProgressTracker.kt`（同时定义 `CacheCompletionGate`，当前没有独立的 `CacheCompletionGate.kt`）
  - `app/src/main/java/io/legado/app/service/CacheBookService.kt`
  - `app/src/main/java/io/legado/app/service/ExportBookService.kt`
  - `app/src/main/java/io/legado/app/help/book/BookExportPlan.kt`
  - `app/src/main/java/io/legado/app/ui/book/cache/`

**交互与计数约束**:
- 缓存页顶部和每本运行中书籍显示本次任务进度，通知使用同一快照；书籍原有“已下载 X/Y”继续表示总缓存覆盖率，两者不能混成同一个百分比。
- 本次任务总数只在章节真正加入等待队列时增加；网络重试不重复增加，已有完整缓存、卷标题和成功下载都计为已处理，最终失败与取消分别保留计数。
- 目录尚未获取时没有可靠分母，页面和通知显示不确定进度；目录加载成功后切换为确定进度。目录加载失败登记为一项失败，不能显示伪 100% 成功。
- 缓存自然结束后保留可点击的完成通知，返回缓存页后继续使用每本书已有的导出入口；用户停止单书、停止全部或服务开始销毁时先关闭完成通知门闩并取消下载协程，不生成“缓存完成”通知。

**导出安全边界**:
- 缓存页导出前按实际章节范围检查在线书缓存，缺章时只提供“仅导出已缓存”或“先补齐缓存”；完整模式存在任一缺章就停止。
- `ExportBookService` 必须再次执行相同策略校验，不能只信任 UI；在线书和 WebDAV 来源书正文使用 `BookHelp.getCachedContent()`，检查后缓存文件被删除也不得回退联网。只有 `isPlainLocalBook` 才能直接读取原书文件，不能用包含 WebDAV 的广义 `isLocal` 放宽边界。
- 未缓存正文不得再以字符串 `null` 写入 TXT/EPUB。仅缓存模式跳过缺失正文但保留卷标题；没有任何可导出正文时明确失败，并删除本轮产生的不完整文件。
- 每次从缓存页导出明确选择“原始缓存正文”或“应用替换净化规则”，第一项为原始正文。这里的原始正文是书源规则解析后的落盘内容，不是网页原始 HTML；全局 `exportUseReplace` 缺省值继续保持 `true`，避免改变书架批量导出等旧入口的既有默认行为。
- EPUB 只嵌入已落盘图片；在线书封面仅允许读取 Glide 缓存，离线导出不应额外访问书源或封面网络地址。
- 新增的缓存/导出界面文案必须同步默认语言、三个中文目录及 vi/pt-rBR/ja-rJP/es-rES；只补中文会让每个缺失资源各产生一项 `MissingTranslation`，不能用 lint 历史基线掩盖新增错误。

**回归点**:
- 单书、多书、从当前章开始和全本缓存时，页面/通知的总数、已处理、下载中、等待和失败单调合理；重试期间总数不增长，停止单书和停止全部不残留运行状态。
- 目录首次联网加载期间显示不确定进度；目录失败、章节最终失败和全部成功三种完成通知文案准确，点击完成通知能进入缓存页。
- 完整缓存分别导出 TXT 和 EPUB，章节顺序、标题、正文、目录和已缓存图片正确；文件可复制到另一台设备并作为普通本地书导入。
- 部分缓存选择完整导出必须被拦截；选择仅缓存导出不得包含 `null` 章节。预检后删除一个缓存文件时导出失败且不保留本轮残缺文件。
- 原始正文模式不应用替换规则，净化模式按当前规则处理；两者都不做简繁转换或重新分段。在线书断网导出不产生正文或封面请求。

---

---

## 更新日志最近五个日期折叠

- 记录日期: 2026-07-15
- 验证日期: 2026-07-15（定向 JVM 测试、Debug Kotlin 编译及日志结构检查通过）
- 适用环境: App 内 Markdown 对话框与 GitHub 文件预览
- 相关文件: `app/src/main/assets/updateLog.md`、`TextDialog.kt`、`MarkdownDetailsSection.kt`

**设计约束**:
- 更新日志只直接展示最近 5 个日期；第 6 个及更旧日志按时间倒序收入文件末尾唯一的 `<details><summary>more</summary>` 区域。
- Markwon 4.6.2 的 `HtmlPlugin` 没有 `details/summary` 交互处理器；`TextDialog` 通过纯 Kotlin 解析器识别文件末尾的折叠区，首次只渲染预览并显示可点击的 `more`，点击后再渲染完整内容。
- 解析器只接受位于文件末尾、标签完整且摘要与内容均非空的单个折叠区；格式异常时回退为原样渲染，不丢失日志。

**回归点**:
- “关于 → 更新日志”和版本更新后的弹窗首屏只显示最近 5 个日期，点击 `more` 后显示全部历史日志。
- 没有文件末尾折叠区的帮助、隐私政策和其他 Markdown 对话框必须保持原样渲染。
- 新增日期后必须把旧的第 5 个日期移到 `more` 顶部，并继续保证每个日期最多 2 句话。

---

## 五、书籍来源边界命名

### 1. `isLocal` 是广义本地，WebDAV 来源要单独判断

- 记录日期: 2026-06-06
- 验证日期: 2026-06-06（`testAppDebugUnitTest --tests io.legado.app.help.book.BookLocalTypeTest` 和 `scripts/check-debug.sh` 通过）
- 适用环境: 书籍信息页、远程书籍导入/上传下载入口、阅读页本地文件判断
- 相关文件:
  - `app/src/main/java/io/legado/app/help/book/BookExtensions.kt`
  - `app/src/main/java/io/legado/app/ui/book/info/BookInfoActivity.kt`
  - `app/src/test/java/io/legado/app/help/book/BookLocalTypeTest.kt`

**结论**:
- `BaseBook.isLocal` 仍保持历史语义：普通本地文件和 `webDav::` 来源书籍都算本地书籍。
- 新增 `BaseBook.isWebDavBook` 表示 `origin` 以 `webDav::` 开头的 WebDAV 来源书籍。
- 新增 `BaseBook.isPlainLocalBook` 表示普通本地文件书籍，不包含 WebDAV 来源。

**使用建议**:
- 需要隐藏书源、禁用在线更新、读取本地文件内容时，可以继续评估使用 `isLocal`。
- 需要区分“上传到 WebDAV”和“从 WebDAV 下载到本地”的 UI 入口时，优先使用 `isPlainLocalBook` / `isWebDavBook`，不要手写 `origin.startsWith(BookType.webDavTag)`。
- 后续如果 `bookUrl` 和 `origin` 的 WebDAV 标记语义继续分化，先扩展 helper 和测试，不要在业务代码里散落字符串判断。


---

## 六、书架 RSS 订阅角标

### 1. 实现概述

- 记录日期: 2026-06-10
- 验证日期: 2026-06-10（`scripts/check-debug.sh` 通过）
- 适用环境: 书架列表/网格，所有书架样式（style1 列表、style1 网格、style2 列表、style2 网格）
- 相关文件:
  - `app/src/main/res/layout/item_bookshelf_grid.xml`
  - `app/src/main/res/layout/item_bookshelf_list.xml`
  - `app/src/main/res/drawable/bg_bookshelf_rss_badge.xml`
  - `app/src/main/res/values/strings.xml`
  - `app/src/main/java/io/legado/app/ui/main/bookshelf/style1/books/BooksAdapterGrid.kt`
  - `app/src/main/java/io/legado/app/ui/main/bookshelf/style1/books/BooksAdapterList.kt`
  - `app/src/main/java/io/legado/app/ui/main/bookshelf/style2/BooksAdapterGrid.kt`
  - `app/src/main/java/io/legado/app/ui/main/bookshelf/style2/BooksAdapterList.kt`
  - `app/src/main/java/io/legado/app/ui/main/bookshelf/style1/books/BaseBooksAdapter.kt`
  - `app/src/main/java/io/legado/app/ui/main/bookshelf/style2/BaseBooksAdapter.kt`

**结论**:
- 在 `item_bookshelf_grid.xml` 和 `item_bookshelf_list.xml` 中新增 `tv_rss_badge` TextView，覆盖在封面左上角，默认 `gone`。
- 角标背景使用 `bg_bookshelf_rss_badge.xml`（4dp 圆角，`@color/md_teal_700` 填充），文案使用新增的 `@string/bookshelf_rss_badge`（默认值 "RSS"）。
- 四个适配器均引入 `io.legado.app.help.book.isRss`，在 `convert`/`onBind` 中执行 `tvRssBadge.gone(!item.isRss)`。

### 2. 关键陷阱

- **style2 分组行必须显式隐藏角标**: `style2/BooksAdapterList.kt` 的 `GroupViewHolder` 复用 `item_bookshelf_list.xml`，必须在 `onBind(BookGroup)` 中显式调用 `tvRssBadge.gone()`，否则 RecyclerView 复用可能把 RSS 角标带到分组行。
- **Diff payload 需监听 type**: `BaseBooksAdapter` 的 `areContentsTheSame` 增加 `oldItem.type == newItem.type` 判断，`getChangePayload` 增加 `type` payload。四个适配器的 payload `when` 分支增加 `"type" -> tvRssBadge.gone(!item.isRss)`，这样用户编辑书籍类型后，书架不用整项刷新也能更新 RSS 标识。
- **布局定位**: 网格布局中封面外有 8dp margin，角标 `layout_marginStart=4dp`、`layout_marginTop=4dp` 相对封面定位；列表布局中封面 `layout_margin=8dp`，角标 `layout_marginStart=12dp`、`layout_marginTop=12dp` 对齐封面左上角（8dp margin + 4dp 偏移）。

### 3. 回归测试点

- style1 列表、style1 网格、style2 列表、style2 网格中 RSS 书籍都显示角标。
- 普通书籍不显示角标。
- style2 分组项不显示角标。
- 编辑书籍类型为 RSS/非 RSS 后，返回书架角标能刷新。
- 更新 loading、未读角标、最近更新时间不被遮挡或挤压。


---

## 七、关于页支持开发入口

### 1. 只跳转项目 GitHub 主页

- 记录日期: 2026-06-14
- 验证日期: 2026-06-14（源码检查；编译验证见当前修改记录）
- 适用环境: 关于页 `AboutFragment` / `about.xml`
- 相关文件:
  - `app/src/main/res/xml/about.xml`
  - `app/src/main/java/io/legado/app/ui/about/AboutFragment.kt`
  - `app/src/main/res/values/non_translat.xml`

**设计约束**:
- 关于页可以提供“支持开发”入口，但 App 内不展示收款码、不提供内置支付流程。
- 入口只打开项目 GitHub 主页，由 GitHub 页面承载后续赞助渠道信息。
- GitHub README 可展示 PayPal 二维码；国内收款渠道暂不添加。
- 不把赞助和功能、内容、书源、去广告、会员权益绑定，避免被理解为应用内数字内容交易。
- 不做弹窗、生日提醒或高频提示，保持低打扰。

**回归点**:
- 关于页显示“支持开发”条目。
- 点击后只打开 `https://github.com/hupohupochuan/legado`。
- GitHub README 的支持开发区块只包含 PayPal 渠道和自愿支持说明。
- 更新日志、检查更新、贡献者、许可、免责声明等原有关于页入口不受影响。


---

## 八、自动更新源

### 1. GitHub Release 仓库必须和当前维护仓库一致

- 记录日期: 2026-06-15
- 验证日期: 2026-06-15（源码检查）
- 适用环境: 关于页/启动自动检查更新；`AppUpdateGitHub`
- 相关文件:
  - `app/src/main/java/io/legado/app/help/update/AppUpdateGitHub.kt`
  - `app/src/main/assets/updateLog.md`

**现象**:
- App 提示有更新时，下载链接来自旧 fork 的 GitHub Releases，而不是当前维护仓库。

**根因**:
- 自动更新不会读取本地 `git remote`，而是通过 `AppUpdateGitHub` 中的 GitHub Releases API 地址检查版本。
- 关于页项目主页和自动更新 Release API 是两套入口；只改关于页链接不会影响自动更新下载地址。

**修复约束**:
- 自动更新仓库统一通过 `GITHUB_RELEASE_REPO = "hupohupochuan/legado"` 维护。
- 正式版检查 `https://api.github.com/repos/hupohupochuan/legado/releases/latest`。
- beta 检查 `https://api.github.com/repos/hupohupochuan/legado/releases/tags/beta`。

**回归点**:
- 手动"检查更新"不应再访问 `huajideshutiao/legado`。
- 发布正式版时，当前维护仓库必须存在 latest release 和 APK asset。
- 发布 beta 时，当前维护仓库必须存在 `beta` tag release 和 APK asset。

**分享文案与远程资源链接清理 (2026-06-15)**:
- 8 个语言的 `app_share_description` 统一指向 `hupohupochuan/legado/releases`。
- `RemoteAssetsUtils.kt` 背景图 CDN 路径改为 `hupohupochuan/legado`。
- ~~`rssSources.json` 默认图标 CDN 路径改为 `hupohupochuan/legado`。~~ 历史勘误（2026-07-15）：当前仓库没有该默认资源文件，仅在旧备份恢复中兼容读取 `rssSources.json`；本条不再代表当前运行时资源。
- 帮助文档 (`jsHelp.md`, `ruleHelp.md`) 中的源码链接改为 `hupohupochuan/legado`。
- `package.json`、`README.md`、`English.md`、`modules/web/src/*.ts` 注释中的源码链接已同步更新。
- 代码注释中引用旧仓库的历史 issue/PR 链接保留不动（不影响运行时）。

---

## 九、自动更新检查间隔与跳过版本 (2026-06-15)

### 1. 功能概述

将原来的 `autoCheckUpdate` 布尔开关替换为 `checkUpdateInterval` 下拉选择（不检查/每次启动/每周/每月），并新增"跳过此版本"功能。

### 2. 设计取舍

- **替换而非新增**: 原 `autoCheckUpdate` SwitchPreference 被 `checkUpdateInterval` NameListPreference 替换。旧 key 保留在 PreferKey/AppConfig 中但不再被 MainActivity 引用，避免迁移复杂度。
- **间隔值用天数存储**: "0"=不检查, "1"=每次启动, "7"=每周, "30"=每月。`toIntOrNull()` 解析，异常值回退到 1（每次启动）。
- **跳过版本仅影响静默检查**: `AppUpdate.check(silent=true)` 时过滤 `skipUpdateVersion`，手动检查（About 页面）不受影响，确保用户手动检查时总能看到所有可用更新。
- **`lastCheckUpdateTime` 在检查前写入**: 即使网络请求失败也记录时间，避免频繁重试。

### 3. 涉及文件

- `PreferKey.kt`: 新增 `checkUpdateInterval`, `lastCheckUpdateTime`, `skipUpdateVersion`
- `AppConfig.kt`: 新增对应 getter/setter
- `MainActivity.kt`: `onPostCreate()` 中替换为间隔判断逻辑
- `AppUpdate.kt`: `check()` 中静默模式过滤跳过版本
- `UpdateDialog.kt`: toolbar 菜单新增"跳过此版本"项
- `menu/app_update.xml`: 新增 `menu_skip_version` 菜单项
- `pref_config_other.xml`: 替换 SwitchPreference 为 NameListPreference
- `arrays.xml`: 新增 `check_update_interval` / `check_update_interval_value` 数组
- `strings.xml` (en/zh): 新增间隔选项和跳过版本文案

### 4. 回归测试点

- 设置页"其他" → 检查更新间隔下拉能正常切换
- 选择"不检查"后冷启动不弹更新对话框
- 选择"每周"后，首次启动检查，后续启动不检查（除非超过 7 天）
- 更新对话框中"跳过此版本"点击后对话框关闭，下次启动不弹同一版本
- 手动在"关于"页面检查更新，不受间隔和跳过版本限制
- `getPrefString` 返回 nullable，`checkUpdateInterval?.toIntOrNull()` 必须用安全调用


---

---

## 十二、应用日志模块重构

- 记录日期: 2026-07-12
- 验证日期: 2026-07-14（Debug Kotlin、3 个日志测试类共 33 项、完整 73 项 JVM 单测、Release/R8 与 5 个 ABI split APK 均通过）
- 适用环境: 全部场景：新安装、覆盖安装、阅读繁忙、切后台恢复、快速开关日志、外部缓存不可用
- 相关文件:
  - `app/src/main/java/io/legado/app/utils/LogUtils.kt`
  - `app/src/main/java/io/legado/app/utils/AsyncFileHandler.kt`
  - `app/src/main/java/io/legado/app/utils/LogFileWriter.kt`(新增)
  - `app/src/main/java/io/legado/app/App.kt`
  - `app/src/main/java/io/legado/app/ui/config/OtherConfigFragment.kt`
  - `app/src/main/java/io/legado/app/ui/main/my/MyFragment.kt`
  - `app/src/main/java/io/legado/app/ui/about/AboutFragment.kt`

**设计要点**:
1. `LogFileWriter` 独立单线程（`AtomicReference<ExecutorService>`），所有日志文件写入严格串行，不与阅读/图片回收共用 `globalExecutor`。
2. `flushAndAwait(timeout)` 在日志线程中先执行 `FileHandler.flush()`，再通过 `CountDownLatch` 等待，导出前调用防止最后几秒日志丢失。
3. `LogUtils.init()` 使用 `@Synchronized` 保证幂等和线程安全，避免并发重复创建 handler 或误删活动锁文件。
4. `App.onCreate` 中后台协程初始化；启动早期日志暂存 `ConcurrentLinkedQueue`（上限 500 条），handler 就绪后按序补写。
5. `LogUtils.d()` 只调用一次 `logger.log(record)`；系统 Logger 输出 logcat，挂载的异步 handler 完成文件写入，禁止再直接 publish 同一 record。
6. `setEnabled(false)` 先 `flushAndAwait` 再设置 level OFF，已入队记录不丢失。
7. `externalCacheDir` 不可用时回退 `cacheDir`；初始化失败保留真实异常到 `status=FAILED` + `lastInitError`。
8. 导出通过 `LogUtils.withLogThreadFlush` 在日志线程中 flush 并复制快照；快照目录 `cache/log-export/` 导出前清空；生成 `log-status.txt` 包含完整诊断信息。
9. 导出直接使用 `LogUtils.logDir`，内部/外部缓存切换时不会找错目录。
10. 导出校验 ZIP，无 appLog 时明确提示降级而非误报"已保存"。
11. 只有日志开关开启或 handler 正在初始化时才缓存预初始化日志；关闭时清空队列。设置页切换日志在 IO 协程执行文件操作。

**回归测试点**:
- 开启日志后 1 秒内存在 `appLog-*.txt`。
- 导出 ZIP 至少包含 appLog、logcat.txt、log-status.txt。
- 写入标记后立即导出，标记必须存在。
- 阅读繁忙时日志仍持续增长。
- 初始化失败可在 UI 和 log-status.txt 中看到原因。
- 未找到 appLog 时不误报保存成功。
- 连续运行半天不会只剩 logcat.txt。
- 空闲时不增加 WakeLock、轮询或常驻网络连接。
- 快速反复开关日志不崩溃、不产生重复 handler。
- 关闭日志前已入队记录不丢失。
- 日志仍进入系统 logcat，不破坏原有诊断兜底。
- 单次调用 `LogUtils.d()` 的唯一标记在文件中只能出现一次。
- Web 性能日志仍通过 `/saveReaderLog` 写入同一份日志文件。

*Last updated: 2026-08-01*
