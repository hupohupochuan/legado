# 新功能踩坑记录

> 项目: 阅读 (legado) Android 电子书阅读器
> 用途: 新功能设计、交互、性能、回归测试和历史演进记录的主题索引。
> 当前代码复核: 2026-08-04
> 维护规则: 每次完成新功能相关代码修改后，先更新本索引中的当前有效结论，再更新对应主题分卷；兼容性/API/构建/运行时问题写入 `适配踩坑记录.md`。
> 时间有效性: 各分卷保留原记录/验证日期；超过 30 天、target/compile SDK 改变、核心依赖升级或系统规则变化时，必须重新验证后再引用，不能只刷新文档日期。

---

## 主题索引

| 分卷 | 内容 | 当前复核状态 |
|------|------|--------------|
| [`新功能踩坑记录-阅读页与翻页.md`](新功能踩坑记录-阅读页与翻页.md) | 原生点击/翻页、阅读性能、全文搜索、朗读引擎、阅读器状态测试 | 2026-08-02 TTS Release/R8 选择链路复核；2026-05-31 翻页分支验证已过期，修改前须重测 |
| [`新功能踩坑记录-Web服务.md`](新功能踩坑记录-Web服务.md) | Web 单入口、网页传书、依赖、阅读页、书本翻页、WebDAV 与 WebService 演进 | 2026-08-04 全文搜索并行化、精确截断与 Android 17 模拟器 WebSocket 回归；历史浏览器/真机结果不自动续期 |
| [`新功能踩坑记录-基础设施.md`](新功能踩坑记录-基础设施.md) | 本地书身份、离线缓存与导出、更新日志、来源边界、书架、更新、日志 | 2026-08-03 同名本地文件并存、Room 84 迁移与书架条目身份复核；真机导入/迁移待回归 |

## 当前有效结论

- 原生文本阅读页以整页滑动阈值区分点击与翻页，普通触摸阈值只负责长按/选区；详细交互和回归点见阅读页分卷。
- 系统 TTS 引擎配置以兼容旧 `title`/`value` 字段的 JSON 保存，但必须通过 `TtsEngineSelection` 的 JSON 树入口编解码；不得重新依赖会被 Release/R8 擦除的 `SelectItem<T>` 反射泛型。HTTP TTS 继续保存数字规则 ID。
- Web 进度链路仍是“浏览器固定 5 秒窗口提交手机 Room，手机固定 60 秒窗口上传 WebDAV”；仅同步/恢复 WebDAV 进度使用 `RangeOnly`，不检查本地 SAF 文件可读性。
- Web 阅读页阅读时长已接入阅读记录：Web 端只负责章节级感知和切章上报，超过 10 分钟的已观察空闲间隔会让本章持续作废；持久化、服务端时间、单次时长边界、跨天拆分和开关统一由手机端管理，不发心跳、不常驻连接。
- 阅读记录主页面常驻双向 `SwitchCompat`，关闭时隐藏月度热力图、开启时显示，右上角菜单不再保留重复开关；App 基础主题为 `Theme.AppCompat`，普通布局不得直接换成会强制校验 `Theme.MaterialComponents` 的 `MaterialButton`。
- Web 前端只有 `modules/web/index.html` → `src/main.ts` 一条生产入口；纸质书效果以 `BookPageReader.vue` 的固定 `clip-path` 裁切动画为准。书本翻页模式只用左右方向键翻页，上下方向键必须保持无动作；运行时改动必须同步 APK assets。
- 网页传书统一走 `/#/uploadBook`，旧 `/uploadBook/index.html` 只做兼容跳转；多文件按队列逐个上传，HTTP 200 不能替代 `isSuccess` 判断，失败必须展示手机端 `errorMsg` 并可重试。
- 离线缓存页面和通知共用任务进度；`CacheCompletionGate` 定义在 `CacheProgressTracker.kt` 中，用户取消或服务销毁后禁止完成通知。在线书导出只能读取已落盘缓存，缺章不得写入占位正文或回退联网。
- Web 阅读页全文搜索已由章节串行改为固定 worker + 两倍并发有界滑动窗口，并发取用户 threadCount 且硬上限 16；完成结果经章节索引缓冲后按章有序上报，只有确认存在第 501 条才标记截断。Web 前端契约零改动，详见 Web 服务分卷第 11 节。
- WebService 停机必须先设置 `isStopping`，再在统一清理路径移除前台通知并取消通知 ID；最新运行时细节以 `适配踩坑记录.md` 0.5 为准。
- 本地书以 `localFileKey = SHA1(文件 URI + 内容 SHA1)` 区分，书名/作者仅是非唯一检索字段；书架 Diff 用 `bookUrl`，路径变更走 Room 事务搬迁。历史书签、阅读记录汇总和 WebDAV 进度文件仍有书名/作者键，不在本轮误宣称已隔离。
- 未在当前轮次重新执行的历史真机、浏览器、长任务或实际文件验证，继续以各分卷原验证日期和“待验证”标记为准。

## 文档同步检查

1. 新功能改动定位到一个主题分卷，更新相关文件、设计约束和回归点。
2. 若结论影响跨主题入口或必守红线，同步更新本索引和 `AI编辑必看架构图.md`。
3. 用户可感知变化再按规则更新 `app/src/main/assets/updateLog.md`；内部文档整理不新增用户日志。
4. 检查顶部复核状态、条目验证日期和末尾 `Last updated` 是否自洽，未完成验证不得写成通过。

*Last updated: 2026-08-04*
